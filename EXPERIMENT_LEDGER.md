# EXPERIMENT_LEDGER.md

Canonical experiment history. Last updated: 2026-09-17.

### HCH v4.4 optimization recovery — CLOSED, `HCH_V44_OPTIMIZATION_RECOVERY_NOT_SUPPORTED` (2026-09-17)

- Stage `experiments/current/hch_v44_optimization_recovery_20260917/` ran the bounded TRAIN+VAL-only recovery under full V2 TEST quarantine. Route to the token: R1 panel did not promote → `--r2-trigger` not satisfied → no R2 arm authorized → stage terminated. 24/24 registered fits (8-cell × 3-seed), independent verification **12/12**, `test_target_read_count = 0` everywhere. Evidence root `experiments/evidence/hch_v44_optimization_recovery_20260917/`, start at `RESULTS.md`.
- **R1 panel gate 3/5** (preregistered, seed-median VAL): gate 1 FAIL — 5/8 cells improve, needs 6; gate 2 FAIL — panel median **+0.2913 %**, needs 0.5 %; gate 3 PASS (≥5/8 cells with selected step > 0 and `mean|c|/mean|r| ≥ 0.05`); gate 4 PASS — worst degradation **−0.1478 %** against a −1.0 % floor; gate 5 PASS. Intermediate token `HCH_V44_RECOVERY_R1_PANEL_NOT_PROMOTED`. The 20-cell expansion was never run, because the protocol forbids expanding an un-promoted recipe.
- **R2 trigger unsatisfied**, so no single-factor arm (lr 3e-3, or tiny Level-readout activation) was tested: no condition held in ≥50 % of the 24 runs — ceiling 0.0 %, train-improving/val-flat 8.3 %, tiny-ratio-with-grad 29.2 %, EMA-lags-raw 41.7 %. The stage therefore ends as `NOT_SUPPORTED` rather than by tuning.
- **The step-budget repair is real but partial and non-uniform.** Seed-median VAL improvement over the frozen recipe: QINGHAI_DA/TimeMixer **+4.381 %**, SHAANXI_DA/PatchTST **+3.064 %**, NINGXIA_DA/iTransformer **+2.416 %**, GANSU_DA/LSTM +0.462 %, SHANDONG_DA/iTransformer +0.121 %, SHAANXI_DA/TimeMixer −0.009 %, SHANDONG_DA/PatchTST **−0.148 %**, GANSU_DA/PatchTST 0.000 %. Both large gains are cells where the frozen recipe gained essentially nothing, confirming the budget was binding there; but SHANDONG_DA/PatchTST — the frozen recipe's best cell at +1.975 % — is 0.148 pp **worse** under the longer budget, so epoch-0 selection was not universally under-trained.
- **The unrecovered cells are not optimization-limited.** GANSU_DA/PatchTST selects step 0 in 3/3 seeds, with a monotone divergent curve: TRAIN MAE 74.711 → 72.635 (`train_correction_ratio` 0.000 → 0.126) while VAL MAE 77.104 → **77.486**, worse at every check from step 100. The optimizer works and the correction is non-trivial; VAL moves the wrong way. This makes the earlier epoch-starvation diagnosis **necessary but not sufficient** — more optimizer steps is not a general repair for v4.4 G2.
- Measurement integrity: the recovery's VAL surface reproduces the frozen stage bit-comparably where they overlap (GANSU_DA/PatchTST step-0 VAL MAE 77.1041 vs frozen 77.1043, `n_val_rows` 42 both). The independent verifier rebuilt the W=7 rule, both row sets, the day-id hashes and the gate arithmetic without importing the runner, and proved `src/core` identity against the completed stage's recorded digest.
- No SOTA/generalization claim; the frozen `+0.00 %` V2 TEST result is unchanged and TEST remains quarantined. Next action is main-window adjudication of a closed lever, not further recovery tuning.

### HCH v4.4 China-5 full-panel G2 — registered TEST recipe is negative/neutral; bounded TRAIN+VAL optimization recovery authorized, V2 TEST quarantined (2026-09-17)

- Completed stage `experiments/current/hch_v44_china5_fullpanel_main_eval_20260917/` returned `HCH_V44_CHINA5_FULLPANEL_COMPLETE_FOR_ADJUDICATION` after lawful 80 shadow-OOF support refits, 60 G2 fits, checkpoint freeze, first TEST opening and independent verification 10/10. The result is a valid measurement of the frozen recipe, not a failed run.
- TEST outcome: G2 panel median gain vs the same V2 Host **+0.00%**, wins **16/20** but mostly <0.1 pp; only GANSU/LSTM (+4.31%) and SHANDONG/PatchTST (+2.09%) move materially. G2 beats the best same-setting offline selected baseline in **1/20**, with median gap **+2.493 MAE** (positive = worse). Registered simpler methods remain stronger on the same TEST support: M0 median +1.83% (66/80 wins), M1/M2 +1.78% (67/80 wins); v4.4−M0 by market is approximately GANSU −1.19 pp, SHANDONG −1.66, SHAANXI −1.01, NINGXIA −6.67, QINGHAI −1.69.
- Root-cause evidence does **not** support a plumbing-failure explanation. On `GANSU_DA/PatchTST`, a 400-step full-batch probe reduces TRAIN MAE 74.7107→69.3408 and mean|correction| grows ~0.007→18.73. Under the registered `lr=1e-3 / 50 epochs / patience=8 / bs=32` recipe, VAL improves beyond epoch 0 in only 26/60 runs and epoch 0 is selected in **34/60**, leaving the zero/near-zero readout and an identity-like repair. Scientific interpretation: the frozen recipe is under-trained/poorly budgeted in optimizer-step terms; this does not yet prove the v4.4 geometry is competitive when properly optimized.
- Main-window decision: do **not** abandon v4.4 immediately and do **not** broadly change initialization+budget+architecture. Register one bounded `TRAIN+VAL-only` optimization-recovery stage: `experiments/current/hch_v44_optimization_recovery_20260917/`. R1 changes budget semantics only (`max_steps=2000`, VAL every 50 steps, no early stopping before step 800, then patience 8 checks; step 0 remains a legal candidate) on a fixed 8-cell × 3-seed recovery panel. Conditional R2 is legal only if R1 is demonstrably optimization-limited and may change exactly one factor: first lr 3e-3, or tiny Level-readout activation if directly diagnosed.
- **Post-TEST contamination rule:** because V2 TEST has been exposed and motivated the recovery stage, it may not be read or used for any subsequent recipe selection. Any recovered recipe requires a separately authorized untouched confirmation surface before SOTA/generalization claims. No G0/G1/G3, optional target-day features, overseas/final, Host/baseline reruns, broad rescue/tuning or paper writes are authorized by this adjudication.

### v4.4 China-5 primary G2 pre-execution — blocked before fit; D1=A causal shadow-OOF support and D2 production harness authorized (2026-09-17)

- First execution of `experiments/current/hch_v44_china5_fullpanel_main_eval_20260917/` stopped correctly at the TEST-closed gate with token `HCH_V44_CHINA5_FULLPANEL_BLOCKED_BEFORE_FIT`: **7/10 gates passed, 0/60 primary G2 fits, 0 TEST target reads**. Gate 5 (TRAIN-only scales) was downstream of gate 4; the two root blockers were legal TRAIN history and absence of a production runner. Evidence: `experiments/evidence/hch_v44_china5_fullpanel_main_eval_20260917/PREEXECUTION_REPORT.md`.
- History blocker: all 20 frozen comparison Hosts were fit on the entire V2 TRAIN block with global VAL early stopping, so their TRAIN residuals are `in_sample` and illegal as W=7 history; no reusable OOF/prequential residual artifact exists. Main-window D1 verdict is **Option A**: register `V44_SHADOW_OOF_PREFIX_90_10_V1`. Split each cell's TRAIN days into B0..B4; B0 is warm-up; for each B1..B4 fit a support-only shadow Host on the strictly earlier prefix using the same architecture/hyperparameters/registered Host seed and `TorchHostAdapter.fit(prefix, valid=None)`. Its source-native chronological internal 90/10 prefix split is accepted as the causal selection rule; global V2 VAL is forbidden. Exact budget: **80 CPU shadow support refits = 20 cells × 4 predicted blocks**. Shadow predictions are provenance `oof`, are used only for TRAIN history, and never replace/report benchmark Host rows.
- Runner blocker: active `HCHV44Core` had no real-data production harness; the only full China-5 harness in the repo targets the archived signed-mass API. Main-window D2 therefore authorizes new stage-local code only under `experiments/current/hch_v44_china5_fullpanel_main_eval_20260917/implementation/**` and `/verification/**`, reusing V2 `cb_contracts/cb_cells` read-only. Required functions: shadow OOF, legal W=7 history, TRAIN-only scales, fixed G2+EMA training, checkpoint freeze, chronological prequential TEST scoring, raw predictions and independent verification. `src/core/**`, `src/backbones/**`, frozen Hosts, baseline CSVs, prior evidence roots and `paper/**` remain immutable.
- The stage/token sequence is unchanged. Resumed Phase A must re-pass with a stage-local non-mutating 12-check source verification, real production-path smoke, 20/20 legal history and TEST-read=0 before emitting `HCH_V44_CHINA5_FULLPANEL_PREEXECUTION_VERIFIED`; only then may the 60 G2 fits start. Final token remains `HCH_V44_CHINA5_FULLPANEL_COMPLETE_FOR_ADJUDICATION`. This entry records a zero-fit blocker/adjudication, not a numeric HCH result.

### v4.4 bounded source patch accepted by main-window review; China-5 primary G2 full-panel execution authorized (2026-09-17)

- Source prerequisite rechecked directly in the active repo: `python -m pytest experiments/foundation/tests/test_v44_cleanroom_*.py -q` → **125 passed**; `python experiments/current/hch_v4_4_cleanroom_bounded_patch_20260917/verification/independent_verifier.py` → **12/12 PASS**, token `HCH_V4_4_CLEANROOM_BOUNDED_PATCH_READY_FOR_SCIENTIFIC_REVIEW`. The seven bounded source defects are considered closed; no further architecture modification is authorized before the main experiment.
- Baseline prerequisite independently reviewed from `experiments/evidence/hch_china5_selected_baseline_completion_20260917/FINAL_REPORT.md` and `VERIFICATION_REPORT.json`: five selected baselines are 20/20 numeric each under `COMMON_BENCHMARK_701020_FULL_V2`; final completion verifier **8/8 PASS**. The earlier NaN Shandong attempt remains rejected/quarantined and must never enter comparison tables.
- New authorization only: run primary `G2_GEOM_COORD_CONTEXT` on China-5 × four frozen Hosts × seeds {7,17,37} = **60 new postprocessor fits** under `experiments/current/hch_v44_china5_fullpanel_main_eval_20260917/PROTOCOL.md`. G0/G1/G3, ablations, optional target-day China features, foreign/final and rescue utilities remain closed. This ledger entry records authorization/review, not a new numeric HCH result.

### China-5 selected baseline completion — 50 missing numeric cells executed and independently verified; the ten SHANDONG rows of a first attempt were rejected as NaN (2026-09-17)

- Scope: finish the domestic selected-baseline set under the frozen `COMMON_BENCHMARK_701020_FULL_V2` splits/Hosts. Exactly five methods; **50 new numeric cells** = PIR five markets × {iTransformer, LSTM} (10) + UEC-STD 5×4 (20) + OMPB 5×4 (20). δ-Adapter 20/20, COSA 20/20 and the existing 10 PIR rows were **reused by hash identity, not rerun**. No Host retrain, no re-split, no sixth method, `RESULTS_LONG.csv` untouched. Terminal token `CHINA5_SELECTED_BASELINE_COMPLETION_VERIFIED`; pre-execution token `CHINA5_SELECTED_BASELINE_COMPLETION_PREEXECUTION_VERIFIED` (Phase A 7/7). Evidence root `experiments/evidence/hch_china5_selected_baseline_completion_20260917/` (start with `FINAL_REPORT.md`); new rows land in `experiments/lab/common_benchmark_results_v2/BASELINE_COMPLETION_20260917.csv`.
- **Rejected first attempt (recorded, archived, not reused):** an earlier attempt emitted a success token while scoring 330 SHANDONG days instead of the frozen 329, which made **all ten SHANDONG `MAE` values `NaN`** (day `2026-07-17` has a non-finite 24/24 target), and its verifier passed them because `abs(nan-nan) > 1e-5` is `False`. It also ran on the system interpreter (`torch 2.13.0+cpu`, `cuda_available: false`) instead of `conda epf-2` (`torch 2.6.0+cu124`, RTX 4060 visible), so the required GPU lane never existed. Archived byte-preserving with a `REJECTION_NOTE.md`; its two superseded assertions were replaced by the differently named active checks `no_nan_or_inf_anywhere` and `metrics_and_scorable_days_recomputed`.
- Phase A findings worth keeping: for all 20 market×Host cells the V2 joint cache TEST block is **bitwise identical** to `08_joint_test/hosts/**/TEST.npz`; the frozen scorable-day rule (a registered TEST day is scorable iff its 24 h target is fully finite) reproduces the manifest exactly (SHANDONG 330/329, all other markets registered == scorable); and all **40** reused δ/COSA/PIR metrics recompute from the frozen predictions under that rule — the check that caught the defect above.
- Execution: per-method backend frozen from a TRAIN/VAL-only smoke **before** any TEST scoring — PIR → cuda (0.185 s vs 2.207 s cpu), UEC-STD → cpu (0.033 s vs 0.121 s), OMPB → cpu (0.888 s vs 2.070 s). GPU lane (10 PIR cells, serial) ran concurrently with a 6-worker CPU pool (40 cells, one BLAS thread each; worker count capped below the CPU-derived ceiling because only 4.1 GB RAM was free). Max per-cell peak RSS 0.98 GB, max per-cell 7.1 s, stage wall clock ≈ 40 s. A post-hoc probe (`13_BACKEND_INVARIANCE_PROBE.json`) re-ran PIR on both devices: max |ΔMAE| = 9.4e-7, so the lane policy is a resource choice, not a result choice.
- Results (new cells, TEST, gain vs the exact same V2 Host). PIR: GANSU +5.80/+6.28, SHANDONG +1.60/+0.22, NINGXIA −8.33/+3.55, QINGHAI −4.22/+28.39 (iTransformer/LSTM), and **SHAANXI −94.13/−152.40** where PIR's MAE reaches 248.0/301.3 against Host 127.8/119.4 — reproduced on CPU (301.338913 vs 301.338912), so a genuine retrieval failure on that market, not a device artifact. UEC-STD: essentially metric-neutral everywhere (−0.91 % … +1.02 %, median −0.20 %, beats Host 7/20). OMPB: strongly regime-dependent — hurts all of Shandong and Gansu (−1.7 % … −10.6 %) but helps QINGHAI LSTM by +20.4 % and NINGXIA TimeMixer by +16.1 % (median −0.52 %, beats Host 9/20). Median MAE context: reused δ-Adapter 94.17, reused COSA 93.25, reused PIR 91.34.
- Independent verification (`verification/independent_verifier.py`, does not import the executor): **8/8**. Recomputed in float64 from raw npz for all 50 cells; re-derived each cell's scorable-day set and re-matched `host_pred`/`y_true` to the frozen cache; proved no raw prediction exists for any reused coordinate and that the V2 adjudication/threshold/Host-metadata trees plus `RESULTS_LONG.csv`/`STATE.json` are unchanged; proved all 20 Host checkpoints and the joint↔08 Host predictions unchanged. Chronology was proved **behaviourally**: PIR re-run with every TEST target replaced by 1e6 gives bitwise-identical predictions, and OMPB re-run with the day-10 target mutated leaves days 0–10 unchanged while 17 downstream days move.
- Limitations: single seed per cell, one frozen split, reused rows keep the historical-exposure caveat, PIR's table mixes devices (shown metric-immaterial), OMPB uses its official config seed 42 while PIR/UEC-STD use project seed 7. No paper insertion, no headline promotion and no v4.4 scientific run is authorized by this stage.

### China-5 frozen-Host error-archetype diagnostic — VAL-only motivation evidence for paper Figures 1–2 (2026-09-17)

- Scope: descriptive analysis only on existing `COMMON_BENCHMARK_701020` frozen Host artifacts, China-5 × {PatchTST, TimeMixer, iTransformer, LSTM} = **20 cells**. **0 model fits, 0 TEST target reads, 0 Host/baseline retrains**. Analysis script: `experiments/current/hch_paper_figure_diagnostics_20260917/analyze_host_error_archetypes.py`.
- Timing diagnostic: per VAL day, circular 24h displacement is measured between actual and Host argmax/argmin hours. For each cell, compute the fraction of days with either peak or valley displacement ≥2h. Across the 20 cells, the median cell fraction is **0.900**, with cell range **0.545–1.000**. This supports timing/placement distortion as a common descriptive phenomenon; it is not interpreted as a causal phase-lag theorem because argmax/argmin can be unstable under flat/multimodal peaks.
- Daily-Level diagnostic: define `eta = H|b|/||r||1 = |b|/MAE_day`. Across the 20 cells, the median of cell-median eta values is **0.492**; **19/20** cells have median eta ≥0.3 and **9/20** have median eta ≥0.5. Thus whole-day signed Level bias is not a negligible corner case on the development surface.
- Objectively selected publishable-market examples for Figure 2: `SHAANXI_DA/PatchTST/2025-08-24` (VAL) for timing/placement mismatch: MAE≈118.10, eta≈0.257, circular peak shift 3h, valley shift 2h, centered shape correlation ≈0.689 and diagnostic +2h circular-lag correlation ≈0.836; `SHAANXI_DA/TimeMixer/2025-08-19` (VAL) for Level bias: MAE≈168.90, eta≈0.949, peak/valley displacement 1h, centered shape correlation ≈0.867.
- Figure authority: `paper/06_figures_tables/MOTIVATION_FIGURES_HOST_ERROR_ARCHETYPES_20260917.md`. These diagnostics justify the Introduction problem statement only; they are **not v4.4 performance evidence** and cannot be used as final TEST claims.

### HCH v4.4 source re-audit patch — second readiness token rejected by main-window adversarial audit (2026-09-16)

- Source-only stage; **0 scientific fits**, **0 TEST target reads**, **0 paper edits**, **0 optional target-day exogenous inputs**. Executor returned `HCH_V4_4_SOURCE_REAUDIT_PATCH_READY_FOR_SCIENTIFIC_REVIEW`; maintained v4.4 pytest subset is now green (`13 passed`). Evidence root: `experiments/evidence/hch_v4_4_source_reaudit_patch_20260916/`.
- The patch genuinely fixed several earlier defects: Host BiGRU total width 32; explicit Host absolute/relative batch fields; mass-weighted inactive-sign Shape loss; W=7 history builder; actual synthetic backward; all-equal full-horizon Shape fallback; archived rejected preflight; active canonical layout tests.
- Main-window adversarial probe still rejects readiness. Exact counterexample `logits=[2,1,9,9]`, `mask=[1,1,0,0]`, `b=0,B=1` yields `sum(S-)=0.142857`, `sum(c)=0.857143`, `||c||1=1.142857` instead of `1,0,2`, because invalid logits enter centering/denominators. G0 output changes when Shape history is mutated despite being registered as no-history. G3 output is exactly invariant to `history_day` mutation, so it lacks G2's day-history information. Initial `B_hat/s_B` is approximately `9.7e-5` rather than the frozen `0.01` because the negative softplus offset is applied twice.
- Additional source-contract defects: `CoordinateScales.fit` lets fully invalid rows collapse `s_b/s_B` (constructed probe gives `s_b≈1e-6` where legal support implies 10); `structured_loss(scales=None)` silently fits from the labels passed to it, allowing VAL-scale leakage if the future runner forgets TRAIN scales; history `eta` uses tensor width rather than per-day valid count (probe expected 0.5, emitted 0.6667); aggregated Shape history is redundantly replicated across W. Returned evidence also omits `SUPERSEDED_PREFLIGHT_AUDIT.md`, `MATH_ADVERSARIAL_REPORT.json`, `MODEL_FIDELITY_REPORT.json`, `VARIANT_PARITY_REPORT.json`, `PYTEST_REPORT.md`, and `BACKWARD_SMOKE.json`, although its protocol required them; verifier's `REQ` list never checks those files.
- Scientific consequence: **no 96-fit G0-G3 TRAIN/VAL campaign is authorized**. Final source-only authority: `docs/current/HCH_V4_4_SOURCE_FINAL_SEAL_AUDIT_20260916.md`; execution prompt: `experiments/current/hch_v4_4_source_final_seal_20260916/AI_SOURCE_FINAL_SEAL_PROMPT.md`. No new architecture family is introduced; final seal is implementation fidelity and simplification only.

### HCH v4.4 legal-anchor source preflight — returned readiness token rejected by main-window fidelity audit (2026-09-16)

- Source-only stage; **0 scientific fits**, **0 TEST target reads**, **0 paper edits**. Executor returned `HCH_V4_4_LEGAL_ANCHOR_SOURCE_READY_FOR_SCIENTIFIC_REVIEW` and wrote evidence under `experiments/evidence/hch_v4_4_legal_anchor_source_preflight_20260916/`.
- Main-window review does **not** accept the token. Direct adversarial probe shows all-equal Shape scores with `b_hat=0,B_hat=1` produce `sum(S+)=1`, `sum(S-)=0`, `sum(c_hat)=1` and `||c_hat||1=1`, violating required `sum(c_hat)=0` and `||c_hat||1=2`. A strictly positive target also receives nonzero inactive-negative-Shape W1 under current `structured_loss`.
- Source fidelity blockers additionally include: no TRAIN-frozen `s_r/s_b/s_B` loss normalization; invalid-horizon denominator dilution; `host_views()` unused by the model; Host BiGRU output width 64 rather than registered total 32; no exact W=7 day-geometry/per-hour Shape-history builder; no genuine Level/B/Shape coordinate-specific allocator; G3 direct control lacks matched hour-local information; `variant_support_parity` hard-coded true; independent verifier trusts runner booleans; synthetic smoke performs no backward step.
- Maintained test evidence independently contradicts readiness: `python -m pytest experiments/foundation/tests/test_canonical_core_layout.py experiments/foundation/tests/test_architecture_variants.py -q` gives **3 failed / 9 passed**, because active canonical layout tests still encode the superseded RCL core.
- Scientific consequence: **no TRAIN/VAL v4.4 ladder is authorized**. Repair authority: `docs/current/HCH_V4_4_SOURCE_READINESS_REAUDIT_20260916.md`; source-only repair stage: `experiments/current/hch_v4_4_source_reaudit_patch_20260916/`.

### HCH v4.4 feature-availability audit — conservative zero-admission result, protocol package incomplete and bounded repair required (2026-09-16)

- Scope: source/data-contract audit only across GANSU_DA, NINGXIA_DA, SHAANXI_DA, SHANDONG_DA plus inventory-only QINGHAI_DA; **0 neural fits**, **0 TEST target reads**, **0 outcome-driven feature selections**. Existing manifest contains 40 candidate forecast/schedule columns. Terminal token emitted: `HCH_V4_4_FEATURE_AVAILABILITY_INSUFFICIENT_STOP_FOR_ADJUDICATION`.
- Safe scientific consequence: no target-day `预测值` column is currently admissible for v4.4. The first-run manifest marks GANSU 8/8, NINGXIA 8/8, SHAANXI 9/9, SHANDONG 10/10, QINGHAI 5/5 as HOLD; `新能源总加预测值` and `竞价空间预测值` also remain HOLD. This is conservative and does not create leakage.
- **Audit-integrity defect:** the package is not protocol-complete. Only `DATE_FEATURE_MASK_QINGHAI_DA.csv` exists although five masks were required; `VERIFICATION_REPORT.json` records `mask_files=1`. `EVIDENCE_INDEX.json` contains only a placeholder unresolved evidence item. The runner hard-codes all G1/G2/G3 statuses to HOLD instead of evaluating them from indexed evidence, and `DERIVED_FEATURE_AUDIT.csv` records HOLD declarations without numerical wind+PV / bidding-space checks. The verifier does not independently reconstruct the required five masks or primary evidence decisions.
- Source-span correction from the actual manifest/source inventory: GANSU ends 2026-04-10, NINGXIA 2026-04-14, SHAANXI 2026-03-30, SHANDONG 2026-07-18, QINGHAI 2026-04-13; do not use rounded month-end dates as evidence ranges.
- Adjudication: preserve **ADMIT=0** for all scientific/model decisions until genuine evidence changes it, but do not call the feature audit complete. The only legal continuation is a bounded zero-fit evidence repair in place; no scientific model fit follows automatically. Repair prompt: `experiments/current/hch_v4_4_feature_availability_audit_20260916/BOUNDED_REPAIR_PROMPT.md`.
- Method consequence (design, not a scientific result): primary v4.4 is being narrowed to always-legal frozen-Host + deterministic calendar + strictly revealed causal residual-history context. Optional target-day exogenous trajectories become a future extension only if separately admitted. No TEST/QINGHAI promotion/foreign/final/paper execution is authorized.

### HCH unbalanced signed-mass capacity ladder — 96/96 fits executed, final token withheld after verification/code-audit defects (2026-09-16)

- Scope: fixed 8-cell TRAIN+VAL panel, seeds 7/17/37, U0 frozen v4.1 R2 reference plus four new variants U1/U2/U3/D3; exactly **96 new Stage-2 fits**, no new Stage-1 fit, TEST/joint/QINGHAI/foreign/final read count 0. Raw output is primary; CAL scalars are secondary only.
- Registered gate outputs from the executed artifacts: capacity `NETWORK_CAPACITY_MIXED` (5/8, panel +0.0503 pp, worst -0.6917 pp; D MAE improves 3/8); context `CONTEXT_EXPANSION_SUPPORTED` as executed (7/8, +1.0371 pp, worst -0.0052 pp); interaction `INTERACTION_BLOCK_NOT_SUPPORTED` (5/8, +0.0239 pp, worst -0.7252 pp); structured output `UNBALANCED_SIGNED_MASS_COMPETITIVE` (D3 wins 2/8, U3 non-worse within 0.5 pp in 7/8); development target `NOT_MET` (U3 vs Host 4/8, +0.0613 pp). These labels remain development-only and the final completion token was **not** emitted.
- Network-capacity interpretation: U1 is 135,524 params versus frozen U0/R2 22,244 (~6.1x) with bit-exact U0 input/target/loss parity, yet yields almost no panel gain. U3 is 387,428 params and adds almost no median gain over U2. Generic depth/width is therefore not supported as the main bottleneck. Qualification: U1 uses successor 60/10 training budget while frozen U0 used 50/8, so “capacity-only” is not literally a one-factor training comparison.
- Zero-sum interpretation: U1/U2/U3 genuinely emit `A+S+−A−S−`; emission identities verify. However `D=A+−A−=sum(q)=24*mean(q)` is barely more predictable than `D_hat=0`: the per-cell best variant is within 3.1% of zero in all 8 cells and within 0.9% in 6/8; U1 is worse than zero in 6/8. Breaking the representation constraint therefore does not solve signed mean/net-mass learnability.
- Existing verifier defect D29: all 24 D3 fits stored §11 coordinates from the wrong object (composed full correction instead of emitted Local) and tied RCL Shapes instead of Jordan Shapes. It changes no registered gate but correctly blocks `HCH_USM_CAPACITY_LADDER_COMPLETE_FOR_ADJUDICATION`; the coordinate block is recomputable from existing emissions with zero fits.
- Post-execution source audit found an additional registered-input defect: `calendar_channels` uses `days_since_epoch % 7` directly and then `dow>=5`, despite epoch day zero being Thursday. Hence Tuesday/Wednesday are flagged as weekend and Saturday/Sunday are not. U2/U3/D3 were trained on this wrong binary calendar feature; U0/U1 are unaffected. PROTOCOL §6 also registered U2 as dual Host + raw-summary history, while the executed U2 additionally contains seven calendar channels. Thus the executed positive context label is **not promotable as evidence for the intended U2 context design** until bounded replay.
- Original “complex domestic exogenous variables may need a stronger network” hypothesis remains untested: the authorized boundary exposed no legal forecast-known exogenous covariates beyond Host/calendar; outcome-like `context` was correctly excluded. U3's negative interaction result applies only to Host/calendar/residual-history interaction.
- RESULTS interpretation correction: B-MAE being smaller than E-MAE does not establish that target residuals are excess-dominated; those are prediction errors, not target `B*`/`E*` magnitudes.
- Evidence: `experiments/evidence/hch_unbalanced_signed_mass_capacity_20260916/`; execution summary: `experiments/current/hch_unbalanced_signed_mass_capacity_20260916/RESULTS.md`; controlling source audit: `docs/current/HCH_USM_CAPACITY_POSTEXECUTION_CODE_AUDIT_20260916.md`. No TEST/foreign/final continuation is authorized. If main-window repair is approved, calendar+D3 fixes require replay of U2/U3/D3 only (72 fits); U0/U1 remain reusable for those defects.

### v4.0 ↔ v4.1 existing-artifact reconciliation — verified completion (`HCH_V4_0_V4_1_RECONCILIATION_COMPLETE_FOR_ADJUDICATION`) (2026-09-16)

- Scope: existing-artifact attribution only on the same 8 development cells; **0 new neural fits**, 0 checkpoint updates, 0 TEST/joint/QINGHAI/foreign/final reads. P0 parity PASS 8/8 with bit-identical stored residuals and identical CAL/EVAL IDs. Parent evidence roots re-hashed unchanged (373 v4.0 files + 259 v4.1 files). Independent verifier: **24/24 PASS**, standalone solver/metrics, 905 independent reads in the published run.
- Both historical headline values are reproduced exactly, but they are not the same kind of number: v4.0 `C2_RAW` registered `eval_mae` already scores `alpha_L*level + alpha_Z*local` with two CAL-fitted nonnegative scalars, while v4.1 R1 headline scores raw `full_correction` and treats calibration as secondary only.
- Matched evaluation changes the interpretation: panel `gap_raw = -0.8206 MAE` (v4.0 raw better), while applying the same dual static calibration to both gives `gap_dual = +0.4145` (sign flips; v4.1 better). `R_cal = 0.4948857`, just below the preregistered 0.50 DOMINANT threshold, so `CALIBRATION_GAP=MATERIAL`, not DOMINANT. Calibration is the only factor crossing MATERIAL.
- Function-space decomposition shows the discrepancy is mainly in the **daily-mean coordinate**: centered-coordinate MAE is comparatively stable (~58.2–60.1), whereas mean-coordinate MAE ranges ~49.7–55.2. Neither stage shows reliable Level-miss→Local-mean closure.
- Other labels: `LEVEL_OBJECT_GAP=SMALL`; `LOCAL_EMISSION_GAP=SMALL`; `HISTORY_INFORMATION_LOSS=NOT_DETECTED`; `ABSOLUTE_HOST_LEVEL_CONFOUND=PLAUSIBLE_ONLY`; `PARAMETERIZATION_GAP=UNRESOLVED`; `V40_ADVANTAGE_INTERPRETATION=MULTIFACTOR_UNRESOLVED`. The latter means no declared factor passed the DOMINANT bar, not that all factors are equally important.
- Source audit confirms v4.0 preserves TRAIN-global absolute Host level while v4.1 uses within-day Host normalization, but the registered 8-cell proxy does not support outcome attribution to this channel. v4.0 Level comes from a full joint LevelLocalModel; v4.1 uses Level-only CoarseLevelModel; their OOF L1 quality is similar but scale/semantics differ.
- One reconciliation-only broadcast defect was found and fixed before final publication; a hard reproduction gate was added. The defect affected neither parent stage nor their recorded numbers. The cited docs/current authority file was absent during execution; `PROTOCOL.md` was self-contained. A post-execution adjudication file now records this provenance explicitly.
- Evidence: `experiments/evidence/hch_v4_0_v4_1_reconciliation_20260916/`. This stage promotes no method and opens no TEST/foreign/final continuation.

### HCH Residual-Complete Local v4.1 — verified completion, primary mechanism not supported (`HCH_RCL_V4_1_SCIENTIFIC_EXECUTION_COMPLETE_FOR_ADJUDICATION`) (2026-09-16)

- Scope: fixed 8-cell development panel (`GANSU_DA/{LSTM,TimeMixer,PatchTST}`, `SHANDONG_DA/{TimeMixer,iTransformer}`, `SHAANXI_DA/{TimeMixer,PatchTST}`, `NINGXIA_DA/LSTM`), seeds 7/17/37, TRAIN+VAL only. Phase-0 preexecution gate: **182/182 tests PASS**, 0 scientific fits, 0 TEST reads. Scientific campaign: **216 fits** = 96 causal Stage-1 OOF Level fits + 24 final-Level refits + 96 Stage-2 fits. Independent verifier: **4733 checks / 0 failed**. TEST target reads 0; no joint cache, QINGHAI, foreign/final, Host/baseline rerun, or `src/core` mutation.
- R1 vs Host, cell-median Overall MAE: GANSU/LSTM `+11.503%`; GANSU/TimeMixer `-1.995%`; GANSU/PatchTST `+1.130%`; SHANDONG/TimeMixer `+1.830%`; SHANDONG/iTransformer `-1.304%`; SHAANXI/TimeMixer `-0.413%`; SHAANXI/PatchTST `-1.605%`; NINGXIA/LSTM `-0.124%`. Positive only **3/8**, panel median `-0.2685%`, worst `-1.9951%`; `DEVELOPMENT_TARGET_NOT_MET`.
- R1 vs matched hard-zero-sum R0: **4/8** non-worse, panel median gain `-0.0249%`, worst `-1.5418%`; delta beats zero-delta only **2/8**, panel median mean-floor reduction `-1.934%`. All five preregistered residual-completeness support gates fail; verdict `MIXED/NOT_SUPPORTED`.
- Delta closure fails directly: R1 panel closure ratio `1.049661` where <1 means recovery of coarse-Level mean miss. Cell floor reductions are positive only GANSU/TimeMixer (`+0.208%`) and GANSU/PatchTST (`+3.254%`); six cells worsen/tie, including SHAANXI/PatchTST `-9.948%`. Thus the added `delta` degree is not reliably learning the Level predictor's remaining daily-mean error.
- R1 vs R2 native untied signed mass: 4/8 each, panel median R1 improvement `-0.0144%`; `LOCAL_PARAMETERIZATION_MIXED`. Orthogonal `delta+A(S+−S−)` is not established as superior to `A+S+−A−S−`.
- R1 vs R3 Direct-Q: R3 better **5/8**, panel median R3 advantage `+0.5876%`, but below the registered 1% dominance threshold; verdict `MIXED`. R3 is lower-capacity (`19984` vs R1 `23332` params), so structured coordinates are not clearly superior, but Direct-Q also does not establish decisive dominance.
- Mechanistic interpretation: v4.0's `CALIBRATED_ZERO_SUM=MATERIAL` remains valid as a representation statement. v4.1 demonstrates that the excluded nonzero-mean remainder is not automatically **learnable** from the current forecast-origin information. Representation capacity and conditional predictability are therefore distinct; the active bottleneck shifts to observability/predictability of the post-Level remainder mean rather than merely zero-sum expressivity.
- Tail/normal evidence is not a rescue. R1 Normal mean improves only 3/8 cells; `normal_harm` is positive by construction whenever any Normal point worsens and has panel median `+4.7309`, so no no-harm/safety claim is supported. Upper/failure-tail subsets are empty in several cells and are reported only where defined.
- 68 supplementary verifier defects are confined to two centred-target diagnostics: 59 float32 storage differences + 9 cancellation/normalization amplification cases. They do **not** enter section-8 metrics, ladder gates, or any verdict. Artifact set remains immutable; no post-hoc runner repair was applied.
- Evidence root: `experiments/evidence/hch_residual_complete_local_v4_1_20260915/`; summary `RESULTS.md`; independent verification `06_candidates/VERIFICATION_REPORT.json`. No automatic continuation, TEST opening, foreign validation, baseline comparison or paper promotion follows from this result.

### HCH v4.0 mechanism ladder — verified completion (`HCH_V4_0_MECHANISM_LADDER_COMPLETE_FOR_ADJUDICATION`) (2026-09-15)

- Scope: fixed 8-cell development panel (`GANSU_DA/{LSTM,TimeMixer,PatchTST}`, `SHANDONG_DA/{TimeMixer,iTransformer}`, `SHAANXI_DA/{TimeMixer,PatchTST}`, `NINGXIA_DA/LSTM`), seeds 7/17/37. Fresh neural candidates `C0_MATCH`, `C2_RAW`, `C2_CAL`, `C3_DIRECT` all use identical legal `MECH_TRAIN=B2∪B3∪B4`; total **96/96 candidate fits**. `C1_MATCH` is read-only dual-static calibration of C0 outputs. Independent verifier PASS: 16 required artifacts, identical target-day IDs, chronological CAL/EVAL, finalizer clean exit, warm-up/B1 excluded, `test_label_read_count=0`. No Host/baseline retraining, QINGHAI, TEST, foreign/final, `src/core` or paper modification.
- Final labels: `CALIBRATED_ZERO_SUM=MATERIAL`; `DUAL_STATIC_CALIBRATION=MIXED`; `PREDICTION_CONDITIONAL_IMBALANCE=MIXED`; `CALIBRATION_BEFORE_RESIDUALIZATION=NOT_SUPPORTED`; `REPRESENTATION=MIXED`; `EXTREME_REPAIR=MIXED`.
- Calibrated zero-sum result: all 8 cells remain material after strictly causal Level-only calibration. Calibrated median `|kappa|` range `0.471..0.816`, panel median ≈`0.634`; raw range `0.442..0.694`. Calibration increases median `|kappa|` in 7/8 cells and only slightly reduces SHAANXI/PatchTST (`0.533→0.512`). This confirms that the zero-sum floor is not explained away by raw-Level scaling error.
- C2-CAL vs C1-MATCH (Overall EVAL gains): GANSU/LSTM `+2.357%`; GANSU/TimeMixer `-0.012%`; SHANDONG/TimeMixer `+0.535%`; SHANDONG/iTransformer `+0.061%`; SHAANXI/TimeMixer `-1.047%`; NINGXIA/LSTM `0%`; GANSU/PatchTST `+1.853%`; SHAANXI/PatchTST `-0.563%`. Only 4/8 positive, hence imbalance is real but the minimal learned `kappa` mechanism is only mixed.
- C2-CAL vs C2-RAW: only 3/8 positive; panel-median gain ≈`-0.019%`, so `CALIBRATION_BEFORE_RESIDUALIZATION=NOT_SUPPORTED`. The mathematically cleaner calibrated target is not empirically better than raw prediction-conditional residualization on this panel.
- Dual static calibration: C1-MATCH vs C0-MATCH positive 4/8, panel median ≈`+0.183%`; below registered support gate, therefore MIXED. Frozen six-cell Stage-0 component map remains heterogeneous: GANSU/LSTM + SHANDONG/TimeMixer Level-dominant; GANSU/TimeMixer + SHANDONG/iTransformer complementary; SHAANXI/TimeMixer + NINGXIA/LSTM Local-dominant.
- Representation control: C3-DIRECT vs C2-CAL is MIXED. Direct is better in 4/8, but only modestly (`~0.05..0.84%`); structured C2-CAL is much better in GANSU/LSTM (`~13.79%` vs Direct), SHANDONG/TimeMixer (`~4.37%`) and GANSU/PatchTST (`~1.89%`). No evidence supports abandoning structured coordinates for an unrestricted residual-vector head.
- Extreme repair: MIXED. Median combined-extreme C2-CAL vs C1 gains ≈ GANSU/LSTM `+2.14%`, GANSU/TimeMixer `+7.32%`, SHANDONG/TimeMixer `-3.22%`, SHANDONG/iTransformer `-3.33%`, SHAANXI/TimeMixer `+0.81%`, NINGXIA `0%`, GANSU/PatchTST `+15.34%`, SHAANXI/PatchTST `0%`. Strongest difficult/extreme evidence is concentrated in GANSU, especially PatchTST.
- Development-wide Host context: C2-RAW is positive vs Host in 7/8 cells with panel-median gain ≈`+0.984%` (worst `-0.524%`); C2-CAL is positive in 4/8 with panel median ≈`+0.168%` and strongest single-cell gain GANSU/LSTM ≈`+12.44%`. These are matched TRAIN+VAL development-slice numbers, not final paper metrics.
- Baseline limitation: `04_baselines/EVAL_SLICE_BASELINE_TABLE.csv` contains `NO_SLICEABLE_FROZEN_BASELINE_ARTIFACT_ADMITTED` for all 8 cells, so no like-for-like δ-Adapter/PIR/MDR/COSA EVAL comparison exists in this stage. Do not claim baseline superiority from v4.0.
- Evidence root: `experiments/evidence/hch_v4_0_mechanism_ladder_20260915/`. Required files include `SUMMARY.json`, `RESULTS.md`, `03_eval/*`, `01_oof_calibration/*`, and `05_audit/VERIFICATION_REPORT.json`.

### Predicted-Level Remainder mechanism diagnostic — verified (`HCH_PREDICTED_LEVEL_REMAINDER_DIAGNOSTIC_COMPLETE_FOR_ADJUDICATION`) (2026-09-15)

- Scope: same six v3.9 TRAIN/VAL cells; strictly chronological TRAIN-only causal OOF Level generation after 40% warm-up, four OOF blocks, seeds 7/17/37 = **72/72 fits**. V2 TEST target read count **0**; independent verifier PASS. No new candidate architecture, Host/baseline retraining, QINGHAI, safety/gate/retrieval, or `src/core` change.
- `level_remainder_signal = SIMPLE_SIGNAL_NOT_DETECTED`: fixed causal Ridge is worse than ZERO in **6/6** cells, gain range `-33.77% .. -5.34%`, panel median about `-16.16%`; ACF(1)/ACF(7) panel medians are near zero. This rejects only the tested simple predictor/features, not all possible predictability.
- `zero_sum_restriction = ZERO_SUM_RESTRICTION_MATERIAL`: all **6/6** cells have median `|kappa|>=0.10`; actual medians range `0.442..0.694`, panel median about `0.600`, and about `88%..93%` of OOF days exceed `|kappa|=0.1`. Median raw zero-sum MAE floors are `31..49` price units while remainder MAE medians are `63..84`. Thus after raw OOF Level prediction, strict zero-sum Local excludes a large fraction of the remaining residual geometry.
- `shared_alpha = SHARED_ALPHA_BOTTLENECK_SUPPORTED`: separate static nonnegative Level/Local calibration improves the shared-alpha EVAL result in **4/6** cells; panel median gain `+0.874%`, worst degradation `-0.178%`, satisfying the preregistered gate. Joint optima often zero one component, showing that Level and Local directions are cell-dependent rather than merely differently scaled.
- Important adjudication: raw-kappa is **conditional on uncalibrated OOF Level**. Because the same diagnostic proves componentwise calibration matters, the large zero-sum floor may partly be Level calibration error. Before any untied `A+/A-` candidate is trained, recompute kappa after strictly causal Level-only scalar calibration using the persisted OOF predictions; this requires no new neural fits. If calibrated kappa remains material, the case for prediction-conditional untied Local strengthens; if it collapses, calibration rather than representation is the primary bottleneck.
- Authority: `docs/current/HCH_PREDICTED_LEVEL_REMAINDER_POSTEXECUTION_ADJUDICATION_20260915.md`; evidence: `experiments/evidence/hch_predicted_level_remainder_diagnostic_20260915/{LEVEL_REMAINDER_TABLE.csv,KAPPA_TABLE.csv,ALPHA_DIAGNOSTIC_TABLE.csv,SUMMARY.json,VERIFICATION_REPORT.json}`.

### HCH Level–Local v3.9 feasibility pilot — verified, not supported (`HCH_LEVEL_LOCAL_V3_9_NOT_SUPPORTED_STOP_FOR_ADJUDICATION`) (2026-09-15)

- Scope: isolated `src/mvp/hch_level_local_v3_9/`; same six TRAIN/VAL cells as v3.8, seeds 7/17/37 = **18/18 fits**. V2 TEST target read count remained **0**; `src/core` hashes were recorded unchanged; independent verifier PASS; Local zero-sum invariant passed in all artifacts.
- Cell-median gains vs Host / frozen M0: GANSU/LSTM `+4.929% / +5.070%`; GANSU/TimeMixer `+0.013% / -1.251%`; SHANDONG/TimeMixer `+0.710% / +1.178%`; SHANDONG/iTransformer `+0.534% / -0.770%`; SHAANXI/TimeMixer `+0.657% / -1.877%`; NINGXIA/LSTM `0.000% / +1.446%`. Gate: 6/6 non-worse vs Host, only 3/6 better than M0, Level coordinate only 2/6 better than zero reference; therefore no external-validation promotion.
- Important mechanism qualification: NINGXIA/LSTM is a pure abstention result (`alpha_0=0` for all three seeds), so its apparent M0 win is not evidence that the candidate correction is better. GANSU/TimeMixer also has a tiny median `alpha_0≈0.080`. Seed-median alpha values across cells are approximately `1.630, 0.080, 0, 0.355, 0.426, 0.432`, showing strong reliance on the single full-correction calibration scalar.
- Local amplitude is not shown to be solved: centered-amplitude gap improves in only 2/6 cells, worsens 3/6, ties 1/6; centered-shape MAE improves modestly in 4/6, worsens 1/6, ties 1/6. Level daily-mean repair is substantial only in GANSU/LSTM; SHAANXI worsens and NINGXIA is unchanged.
- Mathematical adjudication: the oracle identity `r=b*1+A*(S+*-S-*)` is exact when `b*=mean(r)` is known, but this does **not** imply the learned hard-zero-sum architecture is unrestricted. With `1^T z_hat=0`, the repaired mean error is `b*-alpha b_hat`, so daily MAE is lower-bounded by `|b*-alpha b_hat|`; a weak Level predictor therefore creates an irreducible bottleneck that Local cannot absorb.
- Successor hypothesis (design only, no new training authorization): use chronological/out-of-fold predicted-Level residualization. Construct `q*=r-b_tilde 1` from OOF Level predictions and decompose `q*` with untied signed masses. Then `A+*-A-* = H(b*-b_tilde)` exactly, so imbalance is supervised as the real Level prediction error left to Local rather than an unconstrained duplicate Level degree. Read-only diagnostics must precede any v4.0 training. Authority: `docs/current/HCH_V3_9_POSTPILOT_MATH_ADJUDICATION_20260915.md`; evidence: `experiments/evidence/hch_level_local_v3_9_pilot_20260915/VERIFICATION_REPORT.json`.

### HCH-LGR v3.8 Level–Gain–Local feasibility pilot — informative result, Full LGR stopped (`HCH_LGR_V3_8_PILOT_EXECUTED_INFORMATIVE_NOT_VERIFIED_STOP_FOR_ADJUDICATION`) (2026-09-15)

- Scope: isolated candidate implementation under `src/mvp/hch_lgr_v3_8/`; no intentional `src/core` edit. Six China-5 cells × seeds 7/17/37 = **18/18 fits**: GANSU/LSTM, GANSU/TimeMixer, SHANDONG/TimeMixer, SHANDONG/iTransformer, SHAANXI/TimeMixer, NINGXIA/LSTM. QINGHAI was not included. The benchmark split is the frozen V2 chronological 70/10/20; only TRAIN/VAL was used and the executor reported `test_label_values_read=0`.
- Architecture tested exactly the registered Full LGR candidate: `c_hat = b_hat*1 + g_hat*u + A_hat_rho(S+−S−)`, shared existing stem/Shape/magnitude branches, one 2-output Level/Gain affine readout, two-term loss `L_rec + L_coord`, one VAL `alpha_0`, no safety/gate/retrieval.
- Cell-median VAL gains vs Host / M0: GANSU/LSTM `+5.323% / +5.741%`; GANSU/TimeMixer `0.000% / −1.331%`; SHANDONG/TimeMixer `+1.462% / +2.094%`; SHANDONG/iTransformer `+0.521% / +1.353%`; SHAANXI/TimeMixer `+0.305% / −2.276%`; NINGXIA/LSTM `0.000% / +1.446%`.
- Feasibility gate: Host-non-worse **6/6 PASS**; M0 improvement **4/6 PASS**; worst degradation vs M0 `−2.276%` **FAIL** against ≤2%; Level coordinate sanity **3/6**; Gain coordinate sanity **0/6**; local-mass error improves **6/6**. Two weak cells show non-positive correction/residual alignment; one has `alpha_0=0`.
- Scientific conclusion: **Full LGR is not supported as the next-stage candidate.** The pilot does support that Local remains learnable and Level has partial signal; the current Host-aligned Gain coordinate is unsupported. No rescue variant, safety/gate, Gain-network expansion, V2 TEST access or foreign/final experiment is authorized. A future Level+Local design would require a fresh design decision and experiment authorization.
- Evidence boundary: this was an **informative stdout-only feasibility execution**; per-seed checkpoints/raw prediction artifacts were not persisted, so the result is explicitly **not paper-verified evidence**. Report: `experiments/evidence/hch_lgr_v3_8_pilot_20260915/PILOT_REPORT.md`; median table: `RESULTS_MEDIAN.csv`.
- Literature boundary tightened during this stage: Theil-style forecast-error decomposition and Hodson et al. bias–distribution–sequence decomposition already cover generic level/scale/timing decomposition. Therefore no novelty claim is made for decomposition alone; any successor must justify a narrower Host-relative correction object and avoid generic decomposition/post-processing collision.

### PIR-inspired minimal probe post-execution fidelity audit — RLR closed; RRC/LCR require bounded replay (2026-09-14)

- The original execution token `HCH_PIR_MINIMAL_PROBE_VERIFIED` remains valid for evidence integrity: 13/13 checks, TEST read count 0, unchanged core/Host/baseline hashes, exact four cells, 24 fits and no combined variant.
- Post-execution source/protocol audit found three verifier omissions. (1) RRC protocol requires `tilde_r / TRAIN residual scale`, but the runner appended raw `tilde_r` after base canonicalization; current RRC is therefore not the registered object. (2) RRC/LCR instantiate model parameters before `torch.manual_seed(seed)`, so nominal seeds 7/17/37 do not control initialization. (3) registered retrieval Spearman and signed-Shape-W1 diagnostics are absent. RLR additionally used the last M0 seed stream implicitly although registered as deterministic/no-seed.
- RLR was re-audited read-only over all three already-frozen M0 seed streams (no neural fit, no new data): cell-median relative Overall changes are GANSU/TimeMixer `+0.739%`, SHAANXI/PatchTST `-0.034%`, NINGXIA/TimeMixer `-6.053%`, SHANDONG/PatchTST control `-0.109%`. It still fails the registered challenge criteria and is **scientifically closed**.
- Mechanism diagnostic: Host-profile top-10 cosine is high (`0.903..0.965`), yet retrieved daily-level correlation with realised level is only `-0.437..+0.209`, and retrieved residual trajectory correlation only `-0.011..+0.277`. Naive Host-shape retrieval is therefore not residual-relevant enough to explain PIR.
- RRC/LCR executor `NOT_SUPPORTED` labels are suspended pending one implementation-fidelity replay only. LCR retains a conditional signal (`-1.755/+2.599/+0.755%` challenge; `-4.982%` control), but is not transferable as executed. Replay may correct only seed-before-init, registered RRC scaling, and missing diagnostics; all data identities/hyperparameters/gates stay fixed. No TEST, foreign/final access, RLR rerun, combination or rescue is authorized.
- Audit authority: `docs/current/HCH_PIR_MINIMAL_PROBE_POSTEXECUTION_AUDIT_20260914.md`.

### PIR-inspired minimal candidate-quality probe — VERIFIED, executor verdict partially superseded by fidelity audit (`HCH_PIR_MINIMAL_PROBE_VERIFIED`) (2026-09-14)

- Scope was exactly the registered TRAIN+VAL pilot: GANSU/TimeMixer, SHAANXI/PatchTST, NINGXIA/TimeMixer, SHANDONG/PatchTST; V2 TEST was never read (`test_label_read_count=0`), no Host/baseline retraining occurred, and `src/core` stayed hash-identical.
- Exactly 24 new neural fits ran on CUDA under the unchanged recipe: RRC `4×3=12`, LCR `4×3=12`; RLR used fixed `K=10` TRAIN-only retrieval and no neural fit. Independent verifier passed **13/13** with token `HCH_PIR_MINIMAL_PROBE_VERIFIED`.
- Preregistered support gate: RLR `NOT_SUPPORTED_IN_MINIMAL_PILOT` (challenge changes `+0.158/−0.034/−6.903%`, control `−1.415%`); RRC `NOT_SUPPORTED_IN_MINIMAL_PILOT` (challenge `−1.186/−0.334/−2.676%`, median Tail change `−7.817%`); LCR `NOT_SUPPORTED_IN_MINIMAL_PILOT` (challenge `−1.755/+2.599/+0.755%`, control `−4.982%`, median Tail change `−4.283%`).
- Evidence `experiments/evidence/hch_pir_inspired_minimal_probe_20260914/` contains frozen pilot IDs, zero-sum/retrieval diagnostics, raw per-seed predictions/metrics, comparison, gate verdict and independent verification. No combined variant, rescue, core promotion or foreign/final evaluation follows automatically.

### China-5 FULL V2 missing-target adjudication continuation VERIFIED (`CHINA5_COMMON_BENCHMARK_FULL_V2_JOINT_TEST_VERIFIED_WITH_MISSING_TARGET_ADJUDICATION`) (2026-09-13)

- The human-authorized continuation was strictly metric/provenance-only: no split recomputation, retraining, refitting, target imputation, day replacement, or COSA/M1/M2 replay. It re-aggregated the existing `08_joint_test` raw arrays under one finite-target day mask per market.
- Independent verifier: **12/12 PASS**. It explicitly proves `TEST_TARGET_COMPLETENESS_POLICY_APPLIED`, Shandong `330 registered / 329 scored / 1 unscorable`, common mask across methods, unchanged 20 Host + 60 candidate checkpoint identities, unchanged raw prediction identities, shared M0/M1/M2 streams, predict-before-reveal chronology, no retrain/reinference, old-lab isolation, and empty V2 lab before export.
- Final matrix export: 20 Host rows + 70 numeric baseline rows + 180 M0/M1/M2 seed rows = **270 numeric rows**, 50 inherited blockers, and 60 explicit `CELL_MEDIAN_7_17_37` summaries. Every numeric row carries `data_adjudication = V2_MISSING_TARGET_SCORABILITY_ADJUDICATION_20260913` and registered/scored/unscorable TEST-day counts. Shandong `2026-07-17` remains raw TEST provenance with status `NOT_SCORABLE_MISSING_TARGET / TARGET_VECTOR_NONFINITE_24_OF_24`.
- Evidence: `experiments/evidence/hch_china5_common_benchmark_701020_full_v2_20260913/10_missing_target_adjudication/`; active lab: `experiments/lab/common_benchmark_results_v2/`. The historical-exposure caveat remains, so this is standardized development/common-benchmark evidence rather than untouched final confirmation. No method redesign or rescue is authorized.
- **Scientific result shape.** M0 median Overall-MAE gain is **+1.656%** (17/20 positive, 19/20 non-worse; range `−0.652% .. +13.008%`). M1 median is **+1.652%** and reduces the worst cell to `−0.054%`, but vs M0 it improves 6 cells, worsens 10 and ties 4: it is a stabilizer, not an accuracy booster. M2 is numerically identical to M1 in every 20-cell median and every recorded cell×seed final-lambda summary, so Shape-weighted safety has no supported functional effect and is deleted from the leading design. The old QINGHAI 4/4 failure does not reproduce: M0 is positive on all four QINGHAI Hosts (`+0.021/+0.580/+3.468/+2.625%`). M0 beats delta-Adapter 18/20 but PIR only 3/10 legal overlaps; PIR median gain on legal PatchTST/TimeMixer coordinates is **+5.475%**, so broad PIR superiority is not supported. COSA is positive 20/20 in the prequential table; M1 beats COSA 15/20. M0 median Tail-MAE gain is ~`+7.276%`, M1 ~`+4.429%`, while max normal-region relative harm remains ~`+18%`, so no general/subgroup no-harm safety claim is allowed. Detailed adjudication: `docs/current/HCH_V2_RESULT_ADJUDICATION_20260913.md`.

### China-5 FULL V2 common benchmark — JOINT_TEST opened, not verified (`CHINA5_COMMON_BENCHMARK_FULL_V2_NOT_VERIFIED_STOP_FOR_ADJUDICATION`) (2026-09-13)

- Evidence: `experiments/evidence/hch_china5_common_benchmark_701020_full_v2_20260913/`; control: `experiments/current/hch_china5_common_benchmark_701020_full_v2/`. This was the user-authorized Host×baseline breadth stage plus the registered native candidate harness; it was not a rescue or a new structural route.
- Phase A passed the independent `CHINA5_COMMON_BENCHMARK_FULL_V2_PRETEST_VERIFIED` gate (14/14): full-eligible China-5 splits, 20 loadable Host checkpoints, 70 numeric + 50 inherited baseline coordinates, 60 candidate checkpoints on CUDA, and zero TEST-label reads.
- Phase B opened TEST once and wrote 20 Host TEST caches, 70 numeric baseline artifacts, 50 blockers, and 60 shared candidate streams for M0/M1/M2 (180 seed-variant rows). No architecture, seed, loss, split, threshold or W=7 change occurred after opening.
- The independent final verifier passed all checkpoint/stream/chronology/raw-metric/historical-isolation checks but failed `TEST_TARGETS_FINITE`: `SHANDONG_DA` TEST day `2026-07-17` has 24 missing `日前电价` target values. The four Shandong Host TEST arrays consequently contain non-finite targets and their headline metrics are undefined. This is a data-contract blocker, not a scientific method verdict.
- No imputation, row deletion, proxy or result-conditioned repair was performed. The active V2 lab was left with zero paper-facing numeric rows; partial TEST evidence was retained only for adjudication.
- **Post-run human adjudication (2026-09-13):** full-source audit finds this is the only non-finite target day across all five V2 TRAIN/VAL/TEST partitions, and it is the final Shandong TEST day. The frozen split is preserved rather than recomputed. `2026-07-17` remains a registered TEST day but is marked `NOT_SCORABLE_MISSING_TARGET`; Shandong metrics use the identical 329/330 finite-target day mask for every method, while all other markets score every registered TEST day. No retraining/reinference is required because no later prequential forecast depends on this final unavailable reveal. Continuation is metric/verifier-only under `docs/current/HCH_V2_MISSING_TARGET_ADJUDICATION_20260913.md`; the original NOT_VERIFIED token remains the historical outcome of the first verifier pass until an adjudicated re-verification succeeds.

### GEFCOM14P × LAGO_NP current-strict baseline matrix — `GEFCOM_NP_BASELINE_MATRIX_FROZEN_WITH_INHERITED_BLOCKERS` (2026-09-12)

- This is comparison/result-infrastructure, not a method experiment. No HCH / Signed-Mass / Residual-Alignment-Safety method was designed, trained, evaluated or rescued, and no successor is proposed. Evidence root: `experiments/evidence/hch_international_gefcom_np_baseline_matrix_20260912/`; control: `experiments/current/hch_international_gefcom_np_baseline_matrix/{PROTOCOL.md, AI_EXECUTION_PROMPT.md, MATRIX_TEMPLATE_56.csv}`.
- Scope executed exactly as preregistered: `GEFCOM14P / LAGO_NP × PatchTST / TimeMixer / iTransformer / LSTM × {Host, MatchedDirectResidual, delta-Adapter, PIR, UEC-STD, COSA, OMPB}` = **56 coordinates**, row-for-row equal to `MATRIX_TEMPLATE_56.csv`. **36 numeric + 20 inherited blockers** (18 numeric / 10 blocked per market). Nothing added, deleted, renamed or proxy-filled.
- **Q0 admission.** Both markets `ADMITTED`. GEFCOM14P source `BA4582BD38916C89ECB4602950D9726212D1F1C5E210F18B4FECCE35DBD9ED27` (`data/BENCHMARK_CONTROLS/GEFCOM14P/processed/GEFCom2014P_hourly.csv`, `price`, USD); LAGO_NP source `A077763129E9B01DC0F9D27EF4376DB4C6BBBE3E16D3BB7DF47A10B20D0FDEE7` (`data/NORDPOOL/LAGO_NP/NP.csv`, `Price`, EUR). **Neither source declares a timezone**, so both are written `BENCHMARK_LOCAL / SOURCE_UNSPECIFIED`; no CET/UTC was assumed and no DST normalisation was introduced. LAGO_NP re-audited independently (52,416 hourly rows, no duplicate, no hourly gap, no nonfinite, 168h causal context and exact 24h target available). GEFCOM14P has a **timestamp-only** irregularity — duplicated label `2013-03-10 01:00`, absent `2013-03-10 02:00` — and the two duplicate rows carry **different** prices (48.85 and 43.50), which is exactly why value-based deduplication is forbidden. The block was frozen from timestamp structure **before any outcome was opened**; every target day whose 24h target or 168h causal context intersects it was excluded. **8 days excluded (1 target + 7 context), and all 8 lie inside `PROTECTED_FINAL` — no scored row was removed.** Manifest `01_dataset_contracts/GEFCOM14P/GEFCOM_IRREGULAR_TIMESTAMP_EXCLUSION_MANIFEST.csv`. `load_system_fc`/`load_zonal_fc` (GEFCOM14P) and `Grid load forecast`/`Wind power forecast` (LAGO_NP) are registered `LEGAL_FORECAST_KNOWN_UNUSED_BY_BASELINE_HOST`; all four Hosts stay **price-only 168 → 24, channels = 1**.
- **Q1 splits frozen with exact counts, dates, day IDs and a hash.** GEFCOM14P eligible `1,067` days → `HOST_TRAIN 480 / HOST_VAL 53 / POST_TRAIN 160 / DEV_EVAL 53 / PROTECTED_FINAL 321`; LAGO_NP eligible `2,177` → `980 / 108 / 327 / 108 / 654`. `PROTECTED_FINAL` is **structurally** unreadable: unauthorised rows are never parsed at all, so `successful_read_count = 0` is a property of the reader, not a promise. Every scaler and every q05/q95/S90 statistic is training-role-only; the final verifier re-derives each threshold from `HOST_TRAIN` alone (`D1`) to prove it.
- **Q2 independent pre-execution verifier PASSED before any training**: `GEFCOM_NP_PREEXECUTION_CONTRACT_VERIFIED`, **14/14**, from a module that imports no production runner and asserts its own `sys.modules` is clean.
- **E1 Host gates 8/8 PASS.** All four Hosts fully inherit the frozen current-strict recipes with **no per-market architecture / seed / LR / epoch / hidden-size search and no `DEV_EVAL`-based tuning**. iTransformer keeps `ITRANSFORMER_OFFICIAL_SOURCE_NUMERICALLY_ANCHORED` at official commit `c2426e68ca13f74aaec08045c5c724d8ad328124`; LSTM stays `CONVENTIONAL_INTERNAL_HOST / COMMON_CONTRACT_IMPLEMENTATION` and is never called paper-faithful. DEV_EVAL MAE: GEFCOM14P `7.405215 / 8.862870 / 7.577844 / 8.298355`, LAGO_NP `1.978935 / 2.001852 / 2.119599 / 1.989730` (train-mean constants `14.9125` and `3.1039`).
- **E2/E3 methods.** MDR and δ-Adapter on all 8 legal cells; PIR only on PatchTST/TimeMixer (seed `2021`), with iTransformer/LSTM PIR inherited `Q-INCOMPATIBLE` (`PIR_FROZEN_BACKBONE_CONFIG_ABSENT_FOR_NEW_HOST` — no probe, no manufactured config, no ridge/proxy). UEC-STD stays `Q-FIDELITY::UECSTD_TRANSFER_BLOCKED` and OMPB stays `Q-DATA::OMPB_DATA_OR_FIDELITY_BLOCKED`; neither was reopened. COSA ran on all 8 legal Hosts in `ONLINE_TTA` with strict chronology (`predict day d → persist prediction + hash → reveal completed day-d target → update → predict day d+1`), proven by the same adversarial prefix/cut audit plus a liveness test: **8/8 chronology PASS**. COSA is reported only in the online supplementary table and is never pooled or ranked with offline methods.
- **E4 evidence.** Every numeric coordinate computes MAE/MSE/RMSE, gain vs its own Host, Tail/Upper-tail/Lower-tail MAE, Normal-MAE and normal harm, negative-price metrics, S90/extreme-day metrics, `n_days`, `n_hours`; each row persists target, method prediction, Host prediction, residual (`pred − y_true`), timestamp/day IDs, horizon, role, setting, fidelity, source hash, config hash, checkpoint identity, array content hash, and per-day 24h error and residual.
- **E5 independent final verifier PASSED**: `GEFCOM_NP_BASELINE_MATRIX_VERIFIED`, **45/45**, recomputing from written artifacts rather than echoing stored verdicts.
- **Result shape.** MDR loses to its own Host in **8/8** cells, heavily: `−40.65 / −45.73 / −45.73 / −25.99 %` on GEFCOM14P and `−15.90 / −16.47 / −3.94 / −8.37 %` on LAGO_NP. δ-Adapter is the **ranked-best offline baseline in 8/8 cells** but is itself only `−3.64 % … +0.81 %`, so "best" means least-harmful of a harmful set. PIR is `−2.77 … −39.89 %` across its 4 legal cells. COSA (`ONLINE_TTA`) beats its own Host in **6/8** cells, most on the weakest Host (LAGO_NP/LSTM, `−5.60 %`), and is worse on GEFCOM14P/iTransformer (`+0.72 %`) and LAGO_NP/PatchTST (`+0.11 %`). Every method's normal-region harm lies within **−0.24 % … +4.18 %** while overall harm reaches **−45.73 %** — the degradation is concentrated in the tail, not the body, in both markets despite their very different price levels.
- **Subsets.** Both markets' `DEV_EVAL` has `n_negative_hours = 0`, so the negative-price subset is `NOT_APPLICABLE_N0` on all 36 numeric coordinates — an empty subset, explicitly not a numeric zero, and no negative-price claim can be made from this stage in either direction. LAGO_NP has `n_lower_tail_hours = 0`, so `Lower_tail_MAE` is undefined in all 18 of its rows (and `Tail_MAE` coincides with `Upper_tail_MAE` there); GEFCOM14P has 2 lower-tail hours. `n_extreme_days` is 5 (GEFCOM14P) and 10 (LAGO_NP), both subsets `OK`.
- **Defects.** **No machine gate failed.** `q_impl_repairs.n = 0`: the Q2 contract verifier passed 14/14 on the implementation as first run and all eight Host gates passed. Two porting defects (a sibling-stage evidence root in `icells.py`; sibling-stage schema/label strings) were caught by reading the ported modules **before any E-stage ran**, affected 0 metrics, and are disclosed with `sha256_before = null` rather than a reconstructed hash. Not done, verified not done (`J3`/`J4`: `proxy_used = false` on every numeric and blocked row): no seed / LR / epoch / hidden-size / split / threshold / paper-anchor / method-variant change, no correction-strength tuning, no Host exogenous feature, UEC-STD and OMPB not reopened, PIR-new-host not re-probed, the GEFCOM14P timestamp anomaly not "fixed" from price values, no new proposed method.
- **Registry R1.** The Strict Results Laboratory now imports **four** admitted stages: **282 coordinates = 182 active numeric + 90 blocked + 10 superseded/historical**; `RESULTS_LONG.csv` **1652** rows (1480 numeric, 172 `NOT_APPLICABLE_N0`); 480 evidence pointers, 0 missing on disk; 11 markets; 40 market×Host paper cells. Independent `registry_verify.py` re-run: **16 checks / 0 faults → `STRICT_RESULTS_REGISTRY_VERIFIED`**. New non-vacuity harness `implementation/registry_verify_mutation_test.py` injects 26 registry defects and the verifier catches **26/26**, restoring the registry to a re-verified state; the first pass missed one (a market-level `paper_use_scope` narrowing) and the verifier was extended to close that gap before the snapshot was taken. GEFCOM14P carries the new data-handling tag `GEFCOM_IRREGULAR_BLOCK_EXCLUDED_BEFORE_OUTCOME` **without narrowing its paper scope or its Shape/WHERE eligibility**; LAGO_NP and NORD_DK1 carry `NONE_DECLARED`. New immutable snapshot `experiments/lab/strict_results/snapshots/STRICT_RESULTS_SNAPSHOT_20260912T131712Z.json`; the predecessor `…20260912T105648Z.json` is retained and never overwritten.

### Strict Results Laboratory bootstrap R0 — `STRICT_RESULTS_REGISTRY_VERIFIED` (2026-09-12)

- This is comparison/result-infrastructure, not a new model experiment. `experiments/lab/strict_results/` was bootstrapped only from already-frozen admitted evidence: China domestic breadth, NEM_SA1/NORD_DK1, and active LAGO_DE/LAGO_PJM rows from the closed transfer stage. No Host/baseline was retrained and no unfinished GEFCOM14P/LAGO_NP outcome was read.
- Current census: **226 coordinates = 146 active numeric + 70 explicit blockers + 10 superseded/historical**; `RESULTS_LONG.csv` has **1310 metric rows** (1174 numeric, 136 `NOT_APPLICABLE_N0`); 399 evidence pointers/hashes are registered. Paper-facing tables currently cover **9 markets / 32 market×Host cells**. All 64 online rows remain `ranking_included=False`; all blockers are proxy-free and carry no numeric artifact.
- Independent `implementation/registry_verify.py` was re-run after build: **16 checks / 0 faults**, terminal token `STRICT_RESULTS_REGISTRY_VERIFIED`. It independently re-hashes disk evidence, recomputes 114 gains, validates blocker/numeric separation, supersession, NEM caveat propagation, setting separation and `PROTECTED_FINAL=0`. Pre-stage immutable snapshot: `snapshots/STRICT_RESULTS_SNAPSHOT_20260912T105648Z.json`.
- Known provenance debt is explicit: **86 source-declared China aggregate `ARRAY_CONTENT_SHA256` values do not reproduce** under the recoverable content-hash rule. The registry stores recomputed and source-declared values separately and flags disagreement. This is not a metric failure: all available `pred/y_true` payloads reproduce the frozen MAE exactly; four non-prediction `segment/timestamp` per-array digest anomalies in two Host caches remain disclosed. The frozen China stage is not rewritten to cosmetically repair provenance.
- This laboratory is now the paper-facing aggregation authority. Raw evidence stays immutable under `experiments/evidence/**`. Future verified GEFCOM14P/LAGO_NP results and any later scientifically admitted Our Method rows append through this registry and must pass the registry verifier before replacing the current snapshot.

### Signed-mass 5-market × 4-Host development experiment — `HCH_SIGNED_MASS_DEVELOPMENT_NOT_SUPPORTED_STOP_FOR_ADJUDICATION` (2026-09-12)

- Scope was the complete domestic panel: `GANSU_DA / SHANDONG_DA / SHAANXI_DA / NINGXIA_DA / QINGHAI_DA` × `PatchTST / TimeMixer / iTransformer / LSTM` = **20 cells**, all 20 executed. Control: `experiments/current/hch_signed_mass_method_entry/AI_SCIENTIFIC_EXECUTION_PROMPT.md` (token `SIGNED_MASS_METHOD_EXECUTION_AUTHORIZED`), a **development-experiment authorization only**. Evidence: `experiments/evidence/hch_signed_mass_method_20260912/`. `PROTECTED_FINAL` was **not** authorized, was never opened, and its read count is **0** in every artifact of the run.
- The object is the frozen signed-mass decomposition: `r_h = y_h − ŷ_h^H`; `r_h^± = max(±r_h, 0)`; `A^± = Σ_h r_h^±`; `S_h^± = r_h^± / A^±`; exact identity `r_h = A⁺S_h⁺ − A⁻S_h⁻`; fusion only `c_h = Â⁺Ŝ_h⁺ − Â⁻Ŝ_h⁻`; final `ŷ_h^R = ŷ_h^H + α c_h` with no learned gate, bridge, attention, transformer, MoE or router anywhere in the promoted path. Inference setting is `FROZEN_PARAMETER_CAUSAL_PREQUENTIAL_HISTORY_POSTPROCESSING` — parameters frozen on `POST_TRAIN` before `DEV_EVAL` was opened, no gradient/update/TTA during `DEV_EVAL`, each day using only historically revealed residual windows from strictly earlier eligible origins. This is **not** online TTA and is never ranked against `ONLINE_TTA` rows.
- **Mandatory pre-run patch (PARTS A/B) closed before any `DEV_EVAL` number existed.** A1 direct per-sign branch outputs now drive the Shape/Amplitude mechanism metrics; A2 the Amplitude decoder uses a training-only robust scale `s_A`; A3 the high-mass thresholds `q90(A⁺)/q90(A⁻)` are frozen from `POST_TRAIN` with a fit-row hash — 20/20 cells froze, 0 degenerate, `NOT_APPLICABLE_N0` was never needed; A4 raw branch/prediction evidence is immutable with `RAW_PREDICTION_INDEX.csv`; A5 D0; A6 post-run independent verification. `runner.py verify` returned `SIGNED_MASS_CHINA5_METHOD_HARNESS_READY` **367 checks / 0 failed**, with 20 cells `READY_FROZEN`, 20 cells carrying a `DEV_EVAL` partition, 84 OOF folds, immutability `{added: 0, changed: 0, removed: 0}` over 79 tracked files, one market literal branch (`contracts.py`/`GANSU_DA`), and no protected/final name in any read set. Harness tests 111 passed; core purity/layout 47 passed.
- One implementation defect was found and repaired **before** it produced a number: `runner._dataset_for` returned `grid.dataset_for`'s `(dataset, audit)` tuple while both of its callers (the A3 threshold freeze and D0) consume a bare dataset. Nothing partial had been written; the seam is now covered by a test that feeds its return to both consumers.
- **D0** (`01_d0_geometry/`) ran on 20/20 cells on `POST_TRAIN` only, with `DEV_EVAL` never materialised: maximum reconstruction error **1.137e-13**; 20/20 cells have a non-empty positive-Shape *and* negative-Shape day set; 14/20 have at least one exactly-zero-mass day in some sign. The geometry is genuinely asymmetric and market-dependent, e.g. `GANSU_DA::LSTM` A⁺ median `9.44` against A⁻ `3270` (33/62 vs 62/62 defined Shape days), while `SHANDONG_DA` is nearly balanced (`846.6` vs `770.1`). D0 is descriptive only — no threshold in it gates anything.
- **Component screen (PARTS D/E):** 5 registered configurations × 20 cells × 3 seeds = **300 fits, 0 resumed, 0 failed, 561.93 s** (16 workers). Seeds were aggregated by **cell median** throughout; no best seed was ever selected. All four preregistered components were **DELETED** under the pre-registered five-condition rule, and no rescue variant was designed for any of them:
  - `shape_semantic_context` — primary `shape_w1_positive|shape_w1_negative` median delta `+0.876` (positive Shape) and `+0.653` (negative Shape), ON − OFF with positive = worse; the *pair* improves in **1 of 20** cells (`shape_w1_positive` alone in 2, `shape_w1_negative` alone in 1); all five conditions fail.
  - `use_tcn` — primary `tail_mae` median delta `+0.637`; **2 of 20** cells improved, 4 tied; all five conditions fail. The TCN was preregistered as an executor rather than a contribution, and it did not even clear that bar.
  - `untied_amplitude_heads` — the only component with real signal: primary `mass_negative_l1` median delta **−4.539** with **13 of 20** improved, `mass_positive_l1` 12 of 20, median Overall-MAE delta **−1.1e-5**, condition 1 TRUE and condition 3 TRUE. It still fails condition 2 (`upper_tail_mae` median delta `+1.564`, i.e. worse), condition 4 (a single cell gains `+8.85` normal-region harm percentage points from it alone, `GANSU_DA::LSTM`) and condition 5 (only **1** seed carries a majority; the rule requires ≥ 2).
  - `rare_mass_sampling` — primary `high_mass_positive_l1|high_mass_negative_l1` median delta `−0.082 / +0.036`; **9 of 20** improved; conditions 1, 4 and 5 fail.
  - Every one of the four fails condition 4, and in every case the harm is driven by the same cell, `GANSU_DA::LSTM` (A⁺ median `9.44` against A⁻ `3270` — the most asymmetric cell in D0).
- **Smallest surviving method (PART F):** the derived switch vector is the **empty vector — all four switches OFF**, route `FITTED_DERIVED_VECTOR`, label `FROZEN_SMALLEST`, `registered_config: null` (it is not one of the five registered configurations, so it was re-fitted through the registered grid path rather than reconstructed from the ablation rows): **60 fits, 0 failed**, 281.2 s of fitting and 0.7 s of inference over the 20 cells. Still a learned model — one shared feature stem, a window RNN, a softmax Shape head and a tied ± Amplitude head — at **24,051–24,931** parameters depending on the cell's feature count, with a per-cell `POST_TRAIN` scalar `α ∈ [0.886, 1.809]`.
- **Frozen comparison (PART G):** no Host and no comparator was retrained or re-run. `05_baseline_comparison/` joins the frozen strict table only. The best **setting-matched strict** comparator is `delta-Adapter` (`OFFLINE_STATIC_POSTHOC`) in all 20 cells. The rank is not thin: in the 10 PatchTST/TimeMixer cells `delta-Adapter` beat a second setting-matched candidate as well (`PIR`, also `OFFLINE_STATIC_POSTHOC`, numeric on its two frozen backbones), and in the 10 iTransformer/LSTM cells that PIR coordinate is `Q-INCOMPATIBLE` and has no row in the frozen table at all — the recorded thinness, not a new blocker. `MatchedDirectResidual` (`OFFLINE_STATIC_CONTROL`) is reported *beside* the rank and never inside it — it is a valid measurement of a different setting, and admitting it would *loosen* the gate, since it is worse than our method in 19 of 20 cells. No cross-setting "mixed best baseline" claim is made.
- **Result:** **16 of 20** cells beat their own Host. The gain range is `+0.697%` (`GANSU_DA::LSTM`) to `+15.930%` (`SHANDONG_DA::PatchTST`); median Overall-MAE gain **+8.434%**; median Tail-MAE gain **+7.624%**; **16 of 20** strict wins against `delta-Adapter`. The entire failure is concentrated in **one market**: `QINGHAI_DA` loses to its own Host on all four Hosts (−7.392 / −8.235 / −9.826 / −26.689 %) and to `delta-Adapter` on all four; every other market improves on every Host. Negative-price MAE is numeric only in the four `SHANDONG_DA` cells (`132.12`–`175.23` against Host `182.90`–`215.58`); the other 16 cells have an empty negative-price subset and report `NOT_APPLICABLE_N0` — an empty subset, explicitly not a zero. `QINGHAI_DA::LSTM` is also where the maximum normal-region relative harm appears (**+7.585%**).
- **Development paper-candidate gate: 1 FAIL, 5 FAIL; gates 2/3/4/6 PASS.** Gate 1 host-nonworse is **16/20** against a required **19/20**; gate 5 max cell-median normal-region relative harm is **+7.585%** against a cap of **1%**. Gates 2 (median gain ≥ 3%), 3 (≥ 14/20 strict wins), 4 (median tail gain ≥ 5%) and 6 (one identical recipe across every market and Host) pass. Terminal verdict, uniquely determined by the preregistered rule: **`HCH_SIGNED_MASS_DEVELOPMENT_NOT_SUPPORTED_STOP_FOR_ADJUDICATION`**.
- **Independent result verification (A6):** `verify-results` returned **PASS, 752 checks, 0 failed**, recomputing every metric from the raw artifacts rather than reading a stored verdict. 360 artifacts audited (300 screen + 60 frozen); 720 identity/hash checks; `n_algebra_violations: 0`; `n_metric_mismatches: 0`; **worst absolute recomputation difference `0.0` on every field**; the derived verdict equals the recorded verdict; `n_excluded_from_rank: 20` — exactly the twenty `OFFLINE_STATIC_CONTROL` rows, the only rows in the frozen table the setting rule excludes (the audit counts every exclusion of both kinds, so a rank over a table it never actually read cannot report the same number). The verifier recomputes the frozen label from `04_frozen_method/FROZEN_METHOD_CONFIG.json` and checks the frozen method's own 60 fits, which are not part of the screen table.
- **Disclosed instrument changes.** Three corrections were made to the *audit instrument* after its first pass, and are recorded in `06_audits/` and in the "Instrument changes made after the first audit run" section of `FINAL_AUDIT.md` rather than only in narration: (i) the Shape simplex tolerance was a hardcoded float64 `1e-9` applied to rows that are a **float32** softmax cast to float64, which failed all 300 Shape artifacts — measured across all 360 artifacts the residual is min `5.96e-8`, median `1.64e-7`, max `3.76e-7` with **0 above 1e-6** (float32 eps `1.19e-7`), so the tolerance is now `H · eps32` and a test still requires a denormalised Shape to fail at `O(1)`; (ii) the frozen method's own 60 fits were not previously audited; (iii) the frozen label was reconstructed from the switch vector as the placeholder `FROZEN` instead of being read from the frozen record, which silently emptied the comparison join. None of the three alters a metric, a component decision or a gate.
- Not done, by rule: no `PROTECTED_FINAL` read, no Host or baseline retraining, no rescue variant for a deleted component, no per-market exemption, no metric substitution after the fact, no reopening of any frozen blocker. This experiment produced a negative development result on a 20-cell panel; no paper claim stronger than that is made here.

### Post-freeze NEM/NORD data/result sanity audit — NORD accepted; NEM hourly-bin semantics amendment required (2026-09-12)

- This is a **read-only post-freeze diagnostic/audit**, not a replacement of the frozen `NEM_NORD_BASELINE_MATRIX_FROZEN_WITH_INHERITED_BLOCKERS` results. Controlling note: `docs/current/NEM_NORD_POSTFREEZE_DATA_SANITY_AUDIT_20260912.md`. No Host/baseline was retrained, no blocker reopened, no `PROTECTED_FINAL` value read, and no registered matrix metric was overwritten.
- `NORD_DK1` source sanity PASS: local canonical 2023 mean `86.8364 EUR/MWh`, min `-440.10`, max `524.27`, std `48.8149`, and `281` negative-price hours match the Danish regulator's published 2023 DK1 statistics essentially exactly. On frozen DEV targets, read-only previous-day persistence MAE `27.8177` and 7-day same-weekday persistence MAE `42.6435`; all four frozen Hosts (`19.8444/20.8733/20.5256/23.0470`) beat the previous-day diagnostic. Status `PAPER_BENCHMARK_READY_CURRENT_STRICT`.
- `NEM_SA1` raw AEMO source sanity PASS: raw project Q3-2024 five-minute RRP has mean `157.8446 AUD/MWh`, min `-563.63`, max `17500`, negative interval rate `28.2458%`, non-positive rate `29.3893%`, consistent with AEMO's Q3-2024 South Australia market statistics. Frozen DEV is a genuinely negative-heavy regime (`245/432` negative hourly values). Read-only persistence MAE `80.4059` (previous day) / `84.8928` (same weekday); TimeMixer `66.8525` and LSTM `71.1048` beat it.
- Data-semantic defect found: the canonical NEM hourly builder applies default `resample("h").mean()` to interval-ending AEMO `SETTLEMENTDATE`, so e.g. the canonical `01:00` row averages 12 stamps `01:00..01:55`; a strict wall-clock hour would use ending stamps `01:05..02:00`. The frozen `DATASET_CONTRACT.json` is also inaccurate in claiming 11 stamps `H:05..H:55`. This is not future leakage and does not break the internal comparison matrix, but can shift spike/horizon location.
- Read-only corrected-bin sensitivity (timestamps shifted `-5min` before hourly aggregation): frozen NEM DEV target mean abs difference `8.928`, median `0.8525`, p95 `9.611`, max `1458.492`; negative-hour count unchanged at `245`; target mean `23.6006 -> 23.6058`. Scoring existing frozen Host predictions only changes aggregate MAE by `+0.0820/+0.0417/+0.1695/+0.1235` for PatchTST/TimeMixer/iTransformer/LSTM. Therefore aggregate comparison conclusions are robust, but future signed-mass Shape/WHERE claims on NEM require a versioned corrected hourly product and **NEM-only** rerun first.

### International NEM_SA1 × NORD_DK1 current-strict Host × baseline matrix frozen — 36/56 numeric, 20 inherited blockers (2026-09-12)

- Token: `NEM_NORD_BASELINE_MATRIX_FROZEN_WITH_INHERITED_BLOCKERS`. Evidence: `experiments/evidence/hch_international_nem_nord_baseline_matrix_20260912/`. Stage: `experiments/current/hch_international_nem_nord_baseline_matrix/`. Scope was Host/baseline comparison infrastructure only — **no HCH / Signed-Mass Shape-Amplitude method was trained, evaluated, designed or rescued**, and no post-hoc rescue variant is proposed.
- Verification: the pre-execution contract verifier (independent, imports no production matrix module) returned `NEM_NORD_PREEXECUTION_CONTRACT_VERIFIED` **13/13 before any Host or DEV_EVAL run in either market**; the final verifier (`verify_final.py`, likewise non-importing, recomputing every metric from the written artifacts) returned `NEM_NORD_BASELINE_MATRIX_VERIFIED` **39/39**.
- Matrix: exactly **56/56** preregistered coordinates (2 markets × 4 Hosts × 7 rows), none added, deleted or proxy-substituted. **36 numeric, 20 blocked**, split evenly 18/10 per market. Blockers are the inherited recorded outcomes and were not reopened: UEC-STD 8 × `Q-FIDELITY` / `UECSTD_TRANSFER_BLOCKED`; OMPB 8 × `Q-DATA` / `OMPB_DATA_OR_FIDELITY_BLOCKED`; PIR 4 × `Q-INCOMPATIBLE` / `PIR_FROZEN_BACKBONE_CONFIG_ABSENT_FOR_NEW_HOST`. No blocked coordinate carries any numeric artifact of any kind, and none was probed, re-configured or proxied.
- Terminal token choice: `FROZEN_WITH_INHERITED_BLOCKERS` rather than `PARTIALLY_FROZEN_WITH_DISCLOSED_BLOCKERS`, because both markets were admitted and all eight Host gates passed — no market or Host **added** a blocker. The 20 blocked rows are exactly the current strict eligibility boundary, not this stage's failures.
- Market admission: `NEM_SA1` reconciled its 2026-08-29 `CONDITIONAL_HOLDOUT_REVIEW` against the current canonical registry and source to verdict **`ADMIT_CURRENT_STRICT_PANEL`** → `ADMITTED`, 359 eligible days, source SHA256 `06E0FB851A4AE6A6E752A71FADA9CA79CFBB2E5846E9FBDC44363ED790D9E20F`. `NORD_DK1` required no reconciliation, was independently audited and `ADMITTED`, 961 eligible days, source SHA256 `5DE0CE364EA56765E2F70951489226F81860410D10433401DD2DE0FCF2C1D2FC`.
- Day/timestamp semantics were **derived from source lineage, not copied from China**: AEMO NEM `SETTLEMENTDATE` is interval-**ending**, so after the per-source resample both markets resolve to an hour-**beginning** target day. Eligible days require a contiguous 168 h price context and an exact 24 h target. Splits are chronological `HOST_TRAIN / HOST_VAL / POST_TRAIN / DEV_EVAL / PROTECTED_FINAL` = NEM_SA1 `162 / 17 / 54 / 18 / 108` over 2024-01-08 → 2024-12-31 (DEV_EVAL 2024-08-28 → 2024-09-14); NORD_DK1 `432 / 48 / 144 / 48 / 289` over 2022-01-08 → 2024-10-04 (DEV_EVAL 2023-10-18 → 2023-12-12). `PROTECTED_FINAL` successful read count **0** throughout; it is closed in the dataset contracts and materialises in no execution path.
- Hosts: **8/8 gates PASS** on the frozen univariate price `168 → 24` contract with `torch.set_num_threads(1)`; NEM `TOTALDEMAND` was **not** used as a Host input. DEV_EVAL MAE vs the train-mean constant — NEM_SA1 86.1943 / 66.8525 / 82.8432 / 71.1048 against 104.8687 (PatchTST / TimeMixer / iTransformer / LSTM); NORD_DK1 19.8444 / 20.8733 / 20.5256 / 23.0470 against 109.0993. PatchTST/TimeMixer reuse the frozen current-strict recipe, iTransformer inherits its official-source numerical anchor, LSTM is `CONVENTIONAL_INTERNAL_HOST / COMMON_CONTRACT_IMPLEMENTATION`. Thresholds q05/q95/S90 were frozen **once** from `HOST_TRAIN` target and reused by every cell; the verifier re-derives them and additionally requires the resulting tail/extreme masks to be identical.
- Fidelity labels held as frozen, no re-search: MDR `PROJECT_DEFINED_MATCHED_DIRECT_RESIDUAL_CONTROL`; δ-Adapter `ACCEPTED_WITH_DISCLOSED_PARTIAL_FIDELITY`; PIR `PAPER_FAITHFUL_EXACT_ACCEPTED` on the two frozen backbones only; COSA `ONLINE_TTA`. No fidelity tolerance was relaxed to produce a number.
- Result shape, consistent with the domestic panel: (1) **MatchedDirectResidual loses to its own Host on 8/8 cells**, −26.9 % to −182.9 %; (2) **δ-Adapter beats its Host on exactly 1 of 8** (NORD_DK1/PatchTST +1.51 %) and is within ±2.6 % everywhere, three cells within 0.7 % of parity; (3) **PIR's official fidelity does not travel** — positive on 1 of 4 legal cells (NEM_SA1/PatchTST **+2.97 %**) and −9.21 % / −15.13 % (NORD_DK1) and −35.77 % (NEM_SA1/TimeMixer) on the rest, i.e. the same frozen implementation spans a ~39-point gain range; (4) **COSA is nearly inert** — it engages on nearly every day yet moves the aggregate by < 0.18 % on 7 of 8 cells, the one exception being NORD_DK1/LSTM **+2.972 %**. The damage is concentrated in the **normal region**, not the tails (e.g. NEM_SA1/δ-Adapter/LSTM: tail MAE 156.8 vs normal-region MAE 63.9, Normal harm +1.89).
- Setting separation: `03_results/STRICT_OFFLINE_TABLE.csv` = 28 rows across `OFFLINE_HOST` / `OFFLINE_STATIC_CONTROL` / `OFFLINE_STATIC_POSTHOC`; `03_results/ONLINE_SUPPLEMENTARY_TABLE.csv` = 8 rows, all COSA `ONLINE_TTA`. 28 + 8 = 36 is an exact partition of the numeric rows; COSA is never merged into the offline ranking, and two verifier checks (`G4`, `G5`) enforce exactly that. No cross-setting "mixed best baseline" claim is made anywhere.
- COSA chronology: strict `predict day d → persist/hash the prediction → reveal the completed day d target → update → day d+1`, verified three ways — by construction, by an adversarial per-cut prefix audit (targets from day k onward replaced with 1e6), and by a non-vacuity liveness check requiring later days to move at every cut and the buffer seed to matter. All 8 cells PASS. Determinism confirmed by re-execution under identical `array_content_sha256`.
- Market character is not comparable and no cross-market subset metric is claimed: NEM_SA1 DEV_EVAL has 245/432 negative-price hours and 2/18 extreme-spread days; NORD_DK1 has **0/1152** and **0/48**, so its negative-price and extreme-day cells are reported `NOT_APPLICABLE_N0` — an empty subset, explicitly **not** a zero and not averageable across markets.
- Repairs: exactly two evidence-writing defects, both class **`Q-IMPL`**, repaired in place and recorded in `04_audit/HOST_RESIDUAL_SIGN_REPAIR.json`: (a) the eight `Host.npz` files stored `residual = y_true − pred`, the negation of the stage-wide convention `pred − y_true` declared in `irows.py` and used by every method row *and* every daily CSV — so one coordinate carried opposite signs in its array and its daily table; (b) four PIR sidecars recorded seed 7 while PIR trained under the frozen `pir_transfer.ANCHOR_SEED = 2021`. Neither affects any metric (no metric of this matrix reads the residual array; the seed field is provenance). No prediction, metric, seed value, configuration, split or threshold changed, `pred`/`y_true` were asserted bit-identical across the residual repair, the re-executed PIR rows reproduced identical MAE values, and the whole frozen gate was re-run afterwards. No repair in the forbidden class (seed / LR / epoch / hidden size / split / threshold / paper anchor / method variant / correction strength / extra feature / reopening UEC-OMPB-PIR-new-host) was used.
- Disclosed caveats: (1) the two markets' `PROTECTED_FINAL` spans **overlap in calendar time** (NEM_SA1's legal window 2024-01-08…2024-09-14 sits inside NORD_DK1's protected span 2023-12-13…2024-10-04), so the zero-read scan attributes every date per market by path, or per row for the stage-wide tables; four stage-wide files have no determinable market and are checked against the union of the spans, disclosed as `unscoped`. (2) The `.npz` arrays are float32 while the daily CSVs and the frozen thresholds are float64, so a verifier reloading the containers can only reproduce a CSV value to the float32 round-off of the stored operands — the final verifier therefore uses `TOL_F32 = 1e-6` for those crossings and a magnitude-scaled `4·eps32·max(|p|,|y|)` bound for the residual, with sign agreement required away from zero. Both tolerances are disclosed as verifier decisions, not as relaxations of a scientific rule. (3) `npz` is not byte-deterministic, so prediction arrays are pinned by `array_content_sha256` in each sidecar and in `RAW_PREDICTION_INDEX.csv`, not by container file hash; for the plain-text daily CSVs the file hash **is** the equality test.
- No Host training or DEV_EVAL execution began before the independent pre-execution token, and in each market the Host gate ran before any baseline. No result-conditioned tuning, no new method, no proxy substitution (`proxy_used=false` everywhere), and no `PROTECTED_FINAL` read for any purpose.

### China domestic Host × post-hoc baseline full matrix frozen — 90/140 numeric, 50 disclosed blockers (2026-09-12)

- Token: `CHINA_DOMESTIC_HOST_BASELINE_MATRIX_PARTIALLY_FROZEN_WITH_DISCLOSED_BLOCKERS`. Evidence: `experiments/evidence/hch_china_host_breadth_expansion_20260912/`. Stage: `experiments/current/hch_china_host_breadth_expansion/`. Scope was Host/baseline comparison infrastructure only — **no HCH / Signed-Mass Shape-Amplitude method was trained, evaluated, designed or rescued**, and no post-hoc rescue variant is proposed.
- Verification: two independent, stdlib-only-and-numpy checkers, both written from the frozen PROTOCOL.md checklist rather than from the writers' own logic. Pre-execution gate → `CHINA_BREADTH_PREEXECUTION_QUALIFICATION_COMPLETE` (`00_protocol/PREEXECUTION_VERIFIER_RESULT.json`), which gated all E0–E5 execution with no further human confirmation. Final PROTOCOL.md §8 E5 gate → `CHINA_DOMESTIC_HOST_BASELINE_MATRIX_E5_VERIFIED`, **11/11** (`04_audit/FINAL_FREEZE_VERIFICATION.json`).
- Matrix: exactly **140/140** preregistered coordinates (5 markets × 4 Hosts × 7 rows), none added, deleted or renamed — verified against `MATRIX_TEMPLATE_140.csv` by both verifiers. **90 numeric, 50 blocked.** Numeric split: 48 imported-frozen, 42 new execution. Strict offline table `03_results/STRICT_OFFLINE_TABLE.csv` = 70 rows; `03_results/ONLINE_SUPPLEMENTARY_TABLE.csv` = 20 rows (COSA, `ONLINE_TTA`), disjoint and never pooled into a strict rank.
- Blockers, all with explicit codes and **no** numbers, no proxy and no re-training for table symmetry: UEC-STD 20 rows `Q-FIDELITY` / `UECSTD_TRANSFER_BLOCKED` (the single permitted bounded recovery pass was consumed; the source-native gate fails at 7.6062% against a frozen 7.5%, and both `concrete_implementation_defect_remains` and `legal_qimpl_repair_lever_available` are false); OMPB 20 rows `Q-DATA` / `OMPB_DATA_OR_FIDELITY_BLOCKED` (`official_data_lever_available` false — no proxy manufactured); PIR 10 rows `Q-INCOMPATIBLE` / `PIR_FROZEN_BACKBONE_CONFIG_ABSENT_FOR_NEW_HOST` (the frozen PIR backbone configuration does not exist for iTransformer/LSTM; the probe ran 0 cells and recorded 0 numbers).
- Hosts: PatchTST and TimeMixer **import-first, never retrained** in all five markets (parent gate PASS on the four China-5 markets, frozen GANSU foundation for GANSU). New Hosts are iTransformer and LSTM, trained only where they did not exist — `GANSU_DA` is the only market where PatchTST/TimeMixer are **not** new; every new row pins `torch.set_num_threads(1)`. iTransformer passed the pinned official-source ETTh1 96→96 anchor (commit `c2426e68ca13f74aaec08045c5c724d8ad328124`, paper target frozen before the run, both MSE and MAE within 5%) → `ITRANSFORMER_OFFICIAL_SOURCE_NUMERICALLY_ANCHORED`. LSTM is `CONVENTIONAL_INTERNAL_HOST / COMMON_CONTRACT_IMPLEMENTATION` and is never called paper-faithful.
- Fidelity labels held as frozen: MDR `PROJECT_DEFINED_MATCHED_DIRECT_RESIDUAL_CONTROL` (not an external reproduction); δ-Adapter `ACCEPTED_WITH_DISCLOSED_PARTIAL_FIDELITY` (admitted Ada-Y, no anchor re-search); PIR `PAPER_FAITHFUL_EXACT_ACCEPTED` (full official implementation; no ridge/proxy path) on the two frozen backbones only; COSA stays `ONLINE_TTA` with strict `predict day d → persist → reveal completed day d target → update → day d+1` chronology, audited for all 20 numeric rows. PIR's Q0 label was **superseded downward** at E3 on the two new Hosts — a downgrade, disclosed by the verifier rather than suppressed.
- Result shape: (1) Host strength is market-dependent and does not order consistently — LSTM is the worst Host in GANSU (121.16 vs 72.05), SHANDONG, SHAANXI and QINGHAI but the **best** in NINGXIA (75.73 vs PatchTST 82.47), and iTransformer is never best; (2) MatchedDirectResidual is worse than its own Host in **19 of 20** cells, median −27.2%, worst −76.0% (NINGXIA/LSTM), single improvement GANSU/LSTM +20.4%; (3) δ-Adapter is within **±1.61%** of its own Host in every cell (better in 13, median +0.08%); (4) PIR is worse in **all 10** runnable cells, −5.94% to −50.63% (worst QINGHAI/TimeMixer), and is never the best strict method; (5) COSA improves **19 of 20** cells by +0.01%…+1.71% (median +0.22%), with the largest gains on the weakest Host per market (GANSU/LSTM +1.71%) and near-zero gains where the Host is already strong (NINGXIA, QINGHAI).
- Strict-offline best per market × Host: δ-Adapter 13, Host 6, MatchedDirectResidual 1 (GANSU/LSTM), PIR 0.
- Immutability evidence: parent panel **136/136** files byte-identical to the Q0 snapshot; imported frozen evidence **45/45** byte-identical to the pre-execution E0 snapshot; `PROTECTED_FINAL` **read count 0** under `SEALED_STRUCTURALLY_NOT_READ`; 86/86 raw arrays re-hashed under `array_content_sha256` and 86/86 daily CSVs re-hashed byte-for-byte; Host checkpoint hashes verified against each host's own before-image (E1 gate for the 10 new hosts, parent registry for 8 China-5 imports, and for the 2 GANSU imports whose frozen origin persisted no checkpoint hash, a required recorded reason).
- Repairs: 17 entries in `00_protocol/REPAIR_LOG.md` (R-001…R-017), every one class **`Q-IMPL`** except R-014 which is a recorded blocker. No seed, learning rate, epoch, hidden size, split, threshold, anchor, method variant or correction strength was changed after an outcome and no fidelity tolerance was relaxed. R-015 and R-016 are evidence-writer defects (an off-taxonomy `blocker_class` token on 40 rows; a computed disclosure field silently dropped by the CSV field list) and **R-017 records four defects in the final verifier's own rules** — three over-strict rules that would have forced cosmetic edits to correct evidence, one mis-keyed lookup. C3's replacement rule is strictly stronger than the one it replaced, and it is what exposed R-015.
- Disclosed caveats: 4 `GANSU_DA` rows are `METRICS_ONLY` (no surviving per-day array; carried under the parent's original seed-17/3-seed contract and **never** rebased onto this stage's threshold record, which uses seed 7 single-seed). Residual signs differ by source (daily CSVs `pred − y_true`, matching the frozen parent `s7_report.py:155`; `.npz` arrays `y_true − pred`) — no reported metric depends on either. One blocker code is coined at execution and grounded through its frozen class and §11 status token. No cross-setting "mixed best baseline" claim is made anywhere.

### China-5 post-hoc baseline panel frozen — 32 strict cells + 8 COSA cells, 山西 absent (2026-09-12)

- Token: `CHINA5_BASELINE_PANEL_PARTIALLY_FROZEN_WITH_DISCLOSED_BLOCKERS`. Evidence: `experiments/evidence/china5_posthoc_baseline_panel_20260912/`. Pre-registration: `00_protocol/PROTOCOL_FREEZE.md` (written before any Host was trained; extreme-day / tail definitions frozen there first). This is a precomputation substrate, **not** a new-method experiment and **not** a method search.
- Semantics: day-ahead price → day-ahead price next-24h hourly. No historical DA→RT target or cache reused. Chronological roles `HOST_TRAIN/HOST_VAL/POST_TRAIN/DEV_EVAL/PROTECTED_FINAL`, no random split, no imputation. `PROTECTED_FINAL` never read (verified per Host cache: only the four open roles present). Thresholds q05/q95/S90 fit on `HOST_TRAIN` only: 山东 −80.00/647.32/840.04, 陕西 40.00/541.80/861.43, 宁夏 40.00/384.01/353.12, 青海 80.00/650.00/570.00.
- Hosts: 8/8 gates PASS (development MAE vs train-mean constant) — 山东 PatchTST `104.5452`/`150.1011`, TimeMixer `103.7590`; 陕西 `101.6388`/`136.7429`, `99.7407`; 宁夏 `82.4741`/`131.2150`, `92.5665`; 青海 `110.8599`/`200.9550`, `114.1517`. GANSU_DA accepted recipe reused verbatim; no per-province architecture/loss/hyperparameter search.
- 32 strict offline cells (4 markets × 2 Hosts × {Host, MatchedDirectResidual, δ-Adapter Ada-Y, PIR}) and 8 COSA online cells, each with complete raw prediction / target / residual arrays and per-day 24h loss (all 24 absolute errors and 24 residuals, per protocol §7.1). `RAW_PREDICTION_INDEX.csv` = 44 rows, all hashes verified against on-disk artifacts.
- Result shape, consistent across all four provinces: only δ-Adapter ever matches or beats a Host — 山东/PatchTST `+1.51%`, 陕西/PatchTST `+1.61%`, 宁夏 `+1.02%`/`+0.74%`, 青海 `+0.08%`/`−0.18%`. MatchedDirectResidual is harmful in every cell (`−13.23%` to `−37.88%`). PIR is harmful in 7/8 cells (`−5.94%` to `−50.63%`), worst on the smallest-`POST_TRAIN` markets 宁夏 and 青海. COSA (supplementary, online) is near-Host by construction and is **not** comparable to the offline table.
- Harm concentrates in the normal-price region, not the tail (e.g. 山东/PatchTST PIR Normal harm `+6.88` against Tail `+2.14`), and grows as the baseline training block shrinks — the same signature as the `LAGO_PJM/PatchTST` failure that closed the transfer stage, now reproduced in four independent markets.
- Coverage: 山西 `SHANXI` is `ABSENT_FROM_REGISTRY` — no Shanxi dataset in `data/CHINA/` or `data/MARKET_INDEX.csv`; it exists only as an archived `C_diagnostic_only` candidate with normalised 0-1 features and an undisclosed licence. Not invented, not substituted. `UEC_STD` `HOST_FIDELITY_BLOCKED` and `OMPB` `STANDALONE_DATA_BLOCKED`; no proxy manufactured for either. 宁夏 `DEV_EVAL` has zero extreme / upper-tail / negative-price hours, so those subsets are `NOT_APPLICABLE_N0`, not zero.
- Existing strict `GANSU_DA` evidence imported by integrity/provenance audit only: `GANSU_DA_IMPORT_OK_NO_RETRAINING`, host npz re-hashed with matching hash, `target_column` `日前电价`, S3/S4 absent, frozen; re-derived MAE vs published MAE abs diff `0.0`. δ-Adapter / MatchedDirectResidual raw arrays were never persisted by that frozen run — imported as metrics-only and disclosed, not reconstructed.
- Fidelity adjudications unchanged: PIR `PAPER_FAITHFUL_EXACT_ACCEPTED`; δ-Adapter `ACCEPTED_WITH_DISCLOSED_PARTIAL_FIDELITY`; COSA `ONLINE_PARTIAL_FIDELITY / PAPER_NATIVE_METHOD_UNRESOLVED` carried as `TRANSFERRED_IMPLEMENTATION / PAPER_FIDELITY_UNRESOLVED`. Strict main table limited to Host/MatchedDirectResidual/δ-Adapter/PIR; COSA and the two blocked tracks carry `setting` + fidelity disclosure in the supplementary table. No `best strict baseline` claim is made across mixed settings.
- Caveat recorded, not repaired: `np.savez_compressed` stamps write time into the zip header, so `.npz` is content-deterministic but not byte-deterministic. The COSA track was re-executed once to restore NINGXIA/QINGHAI preflight proofs a partial invocation had clobbered; every MAE reproduced to 4 decimals and all registry hashes were refreshed and verified. See `FINAL_AUDIT.md` §G.

### Frozen HCH-S1 vs audited admitted offline baselines — transfer not competitive enough; new idea required (2026-09-12)

- Token: `HCH_BASELINE_TRANSFER_NOT_COMPETITIVE_NEW_IDEA_REQUIRED`. B0 validity `PASS`; B1 GANSU hard-case competitiveness `PASS`; B2 international parity `FAIL`; B3 overall `FAIL`.
- Controlling protocol: `docs/current/HCH_FROZEN_METHOD_BASELINE_TRANSFER_ADJUDICATION_20260911.md`. Evidence: `experiments/evidence/hch_frozen_method_baseline_transfer_20260911/`. Executor reports commit `8ff39ec` pushed after evidence verification.
- Frozen method remained `S1 Daily-Patch GRU32 Shape + pooled exact OOF MAE alpha`; no S1/alpha/state/Gate/baseline rescue was performed. Main admitted offline layer: Host, frozen MatchedDirectResidual, audited delta-Adapter Ada-Y and exact full-official PIR.
- HCH is strict best in both GANSU cells: gap to best admitted baseline `-1.6006%` (PatchTST) and `-6.7486%` (TimeMixer). HCH is also strict best on `LAGO_DE/PatchTST` (`-10.4529%`), `LAGO_DE/TimeMixer` (`-8.6885%`) and `LAGO_PJM/TimeMixer` (`-5.0763%`).
- The single decisive harm cell is `LAGO_PJM/PatchTST`: HCH MAE `3.0458`, best admitted delta-Adapter `2.9756` (`+2.3564%` HCH gap), frozen Host `3.0196`, so HCH is also `0.8658%` worse than its own Host. This breaks the preregistered 2% international band and makes Host-nonworse only 5/6.
- Paired 7-day-block bootstrap is robustness reporting only and not a gate. The predeclared pairing uses HCH seed 17 and each baseline's contract seed (PIR 2021); no winner-conditioned seed pairing was allowed. On `PJM/PatchTST`, the bootstrap CI crosses zero, while the three international HCH wins have intervals below zero.
- Provenance: an earlier run was discarded because the running process had loaded a source version whose hash no longer matched the frozen stage source; no PIR cache from that run was reused. Three implementation defects reached only after deeper execution were fixed before final evidence: `_access_audit.json` tuple-key serialization, missing per-row prediction payload for day-MAE bootstrap, and `finalize.py` dependence on runner constants. Final V1-V9 verification passes, including out-of-process official PIR checkout validation.
- Scientific consequence: freeze the current branch. The new research question is applicability / harm region rather than another accuracy rescue. The leading discussion hypothesis is `Forecast Repairability / Residual Predictability`, but it is not yet an accepted method. New-window discussion handoff: `docs/history/post_baseline_transfer_reset_20260912/README.md`. **No new scientific experiment is authorized.**

### Host-Relative State Interaction final structural closure — not supported, freeze S1 (2026-09-11)

- Token: `HCH_HOST_RELATIVE_STATE_INTERACTION_NOT_SUPPORTED_FREEZE_S1`. Gates: R0 `PASS`, R1 `FAIL`, R2 `FAIL`, R3 `FAIL`, R4 `PASS`.
- One global `beta in R^5` is shared by all six cells. `Z[h,k]=b_hat[h]*R[h,k]`; `u_HRSI=normalize(u_S1+Z beta)`; HRSI alpha is refit with the same exact weighted-median rule. No per-market/per-Host/per-hour coefficients, Gate, conditional Amplitude or structural rescue exists.
- Validity: S1 metric replay max abs diff `1.421e-14`; `beta=0` direction replay `1.192e-07`; frozen Host/splits/Shape target/loss/state schema/normalization preserved.
- Mechanism fails: GANSU HIGH-state Shape cosine changes `-0.0109/-0.0241`; only 2/6 cells improve cell-median Shape cosine; GANSU PatchTST wrong-hemisphere worsens by 4.76 pp.
- Forecast effect fails: HRSI strictly improves S1 in only 1/6 cell medians and is nonnegative in only 1/6; median gain vs Host `5.61%`; only 3/6 cells reach `>=5%`; max Normal-MAE harm `1.0227%`; 0/4 international cells strictly improve S1.
- Efficiency passes: exactly 5 parameters for the whole panel, median beta fit `0.9885 s`, median inference overhead `2.19 us/day`.
- Scientific consequence: freeze `S1 Daily-Patch GRU32 Shape + pooled exact OOF MAE alpha` as the terminal candidate for this branch. No further structural rescue is authorized.
- Next authorized stage: `docs/current/HCH_FROZEN_METHOD_BASELINE_TRANSFER_ADJUDICATION_20260911.md`, a no-tuning six-cell comparison against the audited admitted offline baseline suite.

### Host-Relative State Interaction final structural closure — not supported, freeze S1 (2026-09-11)

- Token: `HCH_HOST_RELATIVE_STATE_INTERACTION_NOT_SUPPORTED_FREEZE_S1`. Gates: R0 `PASS`, R1 `FAIL`, R2 `FAIL`, R3 `FAIL`, R4 `PASS`.
- Controlling protocol: `docs/current/HCH_HOST_RELATIVE_STATE_INTERACTION_FINAL_CLOSURE_20260911.md`. Evidence: `experiments/evidence/hch_host_relative_state_interaction_20260911/`. Code: `experiments/current/hch_host_relative_state_interaction/`. Prior independent HSA adjudication remains `experiments/evidence/hch_horizon_aligned_state_residual_20260911/adjudication/INDEPENDENT_HSA_ADJUDICATION.md`.
- Panel: `GANSU_DA / LAGO_DE / LAGO_PJM × PatchTST / TimeMixer`, seeds 7/17/37. S1 Daily-Patch GRU32, Host, splits, S1 OOF/final directions, Shape target/loss, role schema, state normalization and the pooled exact-MAE alpha form were frozen and not retrained: S1 metric replay max abs diff `1.421e-14`, `beta=0` direction replay `1.192e-07`.
- Only new trainable object: **one global `beta in R^5` shared by all six cells**, on `Z[h,k]=b_hat[h]*R[h,k]` where `b_hat` is the L2-normalized same-hour mean of the existing seven-day normalized Host-residual history and `R` is the fixed standardized five-role state table. Trained on the cross-market stacked chronological OOF pool with cell-macro-balanced Shape loss `L = mean_cells(mean_rows(||u_HRSI-u*||^2))`; `u_HRSI = normalize(u_S1 + Z beta)`; `alpha_HRSI` refit per cell by the original exact weighted-median rule. No decay, MAD, clipping, alternate window, per-hour or per-market parameter, or role selection.
- The shared beta destroys the only mechanism that worked. GANSU Shape cosine changes `+0.0002` / `-0.0105` versus HSA's `+0.0666` / `+0.1170`; GANSU HIGH-state tertile cosine falls `-0.0109` / `-0.0241`; HIGH-state wrong-hemisphere is unchanged.
- Failure: Overall-MAE gain vs S1 is nonnegative in only 1/6 cell medians and strictly better in only 1/6; worst cell `-0.6151` pp (LAGO_DE/PatchTST), worst seed `-1.3676` pp; 0/4 international cells strictly better; only 1/6 cells non-worse in `>=2/3` seeds. Median gain vs Host `5.61%` with only 3/6 cells `>=5%`; max Normal-MAE harm `1.0227%` exceeds the 1% gate.
- Learned beta is stable across seeds but set by the four international cells: seed7 `[0.109, 0.172, 0.000, -0.265, -0.134]`, seed17 `[0.048, 0.154, 0.000, -0.313, -0.050]`, seed37 `[0.054, 0.144, 0.000, -0.328, -0.047]`. `beta_SUPPLY_MARGIN_FC` is exactly `0.0` for every seed because that role is structurally absent in all three markets, so its interaction column is identically zero panel-wide.
- Interpretation: HSA's GANSU gain came from per-market coefficient freedom. The constraint that makes the interaction universal — one five-parameter beta for the whole panel — is exactly the constraint that removes it, and the pooled least-squares direction then degrades four cells to buy a `+0.15` pp GANSU/PatchTST change. The transferability conflict is in the coefficient, not the representation; making the input Host-relative does not resolve it.
- Efficiency: `5` parameters for the entire six-cell panel, `0` per-cell beta vectors, `6` cells per beta; median beta-fit `0.9885 s`; median inference overhead `2.19 us/day`; total `8.98 us/day`. Efficiency is not the limiting factor.
- Alpha row-set confound measured, not assumed: HRSI's alpha lives on B2–B4 and frozen S1's on B1–B4, a discrete weighted-median shift of up to `26.39%`. Rescoring S1 on HRSI's exact row set makes HRSI look worse (3/6 non-worse, worst cell `-0.7848` pp, worst seed `-1.3998` pp), so the negative is not a comparison artifact. Disclosed in `alpha_rowset_sensitivity.csv`; never a gate input.
- Independent recomputation: `verify_gates.py` re-derives R0–R4 and the token from the written evidence without importing the runner — 50/50 integrity checks pass and all five gate values plus the token match `HRSI_VERDICT.json`.
- Three implementation defects were caught by the fail-closed guards and fixed before the frozen run: a 2-D-vs-1-D bias-availability comparison made `np.array_equal` unconditionally false, the stale-checkpoint guard conflated a frozen-input change with a reporting-recipe bump, and the seed-level aggregate tables medianed the `seed` column into the cell rows. The first is covered by T06 and the `host_bias_direction` assertions, the second by the split fail-closed branches, and the third by `cell_median`.
- **Consequence: `S1 Daily-Patch GRU32 Shape + pooled fixed alpha` is frozen as the final structural candidate.** No further structural rescue is authorized; the next stage is human adjudication, then the full public-China/international comparison. No SHAANXI/NINGXIA/QINGHAI, Shandong, full-panel, or protected/final data was touched.
- Baseline-comparison preparation is complete for the current paper stage: all selected methods have completed strict protocol/official-code/split/training/metric auditing, with admissible implementations and explicit fidelity boundaries recorded in the final baseline adjudication. Later direct comparisons must use the same dataset/task/Host/evaluation contract.

### Horizon-Aligned Semantic State Residual closure — not supported, keep S1 (2026-09-11)

- Token: `HCH_HORIZON_ALIGNED_STATE_RESIDUAL_NOT_SUPPORTED_KEEP_S1`. Gates: H0 `PASS`, H1 `FAIL`, H2 `FAIL`, H3 `FAIL`, H4 `PASS`.
- Controlling protocol: `docs/current/HCH_HORIZON_ALIGNED_STATE_RESIDUAL_SHAPE_CLOSURE_20260911.md`. Evidence: `experiments/evidence/hch_horizon_aligned_state_residual_20260911/`. Code: `experiments/current/hch_horizon_aligned_state_residual/`.
- Panel: `GANSU_DA / LAGO_DE / LAGO_PJM × PatchTST / TimeMixer`, seeds 7/17/37. S1 Daily-Patch GRU32, Host, splits, Shape target/loss and S1 OOF chronology were frozen and not retrained; S1 metric replay max abs diff `1.421e-14`, `beta=0` direction replay `1.192e-07`.
- Only new trainable object: `beta in R^5` on a deterministic `R_d in R^(24x5)` role table (mean pool of audited `primary_channel=True` forecast-time columns into the five fixed semantic roles; absent role = structural 0; `SUPPLY_MARGIN_FC` absent in all three markets). `u_HSA = normalize(u_S1 + R_d beta)`; `alpha_HSA` refit by the original exact weighted-median rule on HSA OOF directions.
- Positive mechanism: both GANSU_DA Hosts improve Shape cosine (`+0.0666` / `+0.1170`) and the frozen GANSU HIGH-state tertile (`+0.0347` / `+0.1189`), cutting HIGH-state wrong-hemisphere by 12.5 / 18.2 pp; GANSU Overall-MAE gains vs S1 `+1.30` / `+4.31` pp, vs Host `3.55%` / `11.04%`.
- Failure: the gain is not universal. Shape cosine rises in only 4/6 cells and wrong-hemisphere is non-worse in only 3/6; Overall-MAE gain vs S1 is nonnegative in only 4/6 cell medians (worst `-0.87` pp, LAGO_DE/PatchTST); median gain vs Host `6.73%` with only 3/6 cells `>=5%`; max normal-MAE harm `1.02%` exceeds the 1% gate; only 3/6 cells hold in `>=2/3` seeds.
- `beta` is fitted per market on its own chronological OOF rows with one shared five-role vocabulary. No market/Host ID, role embedding, per-hour beta, bias, hidden layer, optimizer/LR/step/seed search, excursion/MAD/clipping, conditional Amplitude, Gate/Verification, or market-specific rule. Structurally absent roles keep `beta=0` exactly.
- Efficiency: 5 extra parameters; median beta training `1.73 s/cell`; median inference overhead `1.22 us/day`; total `7.06 us/day`. Efficiency is not the limiting factor.
- Independent recomputation: `verify_gates.py` re-derives H0–H4 and the token from the written evidence without importing the runner — 39/39 integrity checks pass and all five gate values plus the token match `HSA_VERDICT.json`.
- Two implementation defects were found and fixed before the frozen run: HSA alpha was initially refit over the S1 row set (B1–B4) instead of the rows carrying an HSA direction (B2–B4), and the per-cell loop passed the whole multi-market schema dict where a single-market role mapping was required. Both are now covered by T12 and by the fail-closed assertions in `role_table`/`calibrate_hsa_oof`.
- No SHAANXI/NINGXIA/QINGHAI, Shandong, full-panel, or protected/final data was touched; no rescue variant is authorized from this result.

### Final baseline-fidelity closure — workstream complete with mixed admission status (2026-09-11)

- Token: `HCH_BASELINE_BREADTH_RECOVERY_COMPLETE_FOR_ADJUDICATION`.
- Independent final adjudication: `experiments/evidence/hch_baseline_paper_fidelity_20260911/adjudication/FINAL_BASELINE_FIDELITY_ADJUDICATION_20260911.md`.
- Final method statuses: δ-Adapter Ada-Y = `ACCEPTED_WITH_DISCLOSED_PARTIAL_FIDELITY`; PIR full official = `PAPER_FAITHFUL_EXACT_ACCEPTED`; COSA = `ONLINE_PARTIAL_FIDELITY / PAPER_NATIVE_METHOD_UNRESOLVED`; UEC-STD = `HOST_FIDELITY_BLOCKED / NOT_PAPER_FAITHFULLY_ADMITTED`; OMPB = `STANDALONE_DATA_BLOCKED / NOT_REPRODUCED`.
- UEC source-native recovery corrected the prior project-side custom loader/AR path and used official ETTh1 loader/fixed borders/train-only scaler/fresh TimeMixer-96/official AR336. Host MSE/MAE `0.500369/0.457737` vs paper `0.465/0.449`; MSE relative error `7.606%` narrowly fails the frozen `7.5%` gate. UEC correction and Weather rescue were correctly not entered.
- OMPB exact TCN/96 paper rows were frozen before outcomes; registry SHA256 `29F4C445045C912536714F41BF3A44AEC3979E34D54FA5865CAA0E8AA1714EDB`. Official dataset bundle unavailable; source/target reads `0`, training `0`, no fabricated metric.
- Mandatory offline comparison layer is considered sufficient/frozen for later HCH unified-DA transfer: Host, direct residual, δ-Adapter, PIR. Supplementary COSA/UEC/OMPB statuses are disclosed and do not block HCH.
- Future reopen conditions are external-evidence only: UEC official/source artifact resolving Host discrepancy; OMPB official/provenance-equivalent dataset; COSA explicit online/TTA paper requirement. No anchor/tolerance/seed rescue search is authorized.

### UEC source-native Host recovery + standalone OMPB breadth reproduction authorized (2026-09-11)

- This is an auxiliary baseline-fidelity continuation only; it does not replace or modify the active HCH Horizon-Aligned Semantic State Residual closure and does not authorize HCH transfer.
- Source audit amends the previous U-A1 interpretation: `run_uecstd_after_authority.py` forced ETTh1 through `Dataset_Custom` / `--data custom` and implemented a project-side AR336 rollout. Official UEC source instead maps `ETTh1 -> Dataset_ETT_hour`, official ETTh1 scripts pass `--data ETTh1`, and official long-horizon evaluation/training is exposed through modes 2/5/6. Historical Host/UEC MSE `0.596529/0.588395` remain valid custom-runner evidence but are not accepted paper-native anchor metrics.
- New UEC protocol: `docs/current/HCH_UECSTD_HOST_FIDELITY_RECOVERY_PROTOCOL_20260911.md`. First train a fresh official native-96 TimeMixer on source-native ETTh1 and evaluate AR336 only through official mode 2. Host MSE must be within 7.5% of paper `0.465` before any UEC corrector is trained. If Host passes, run the fixed U-A1 mode5/6 anchor; Weather U-A2 is entered only after U-A1 PASS. No split/seed/epoch/beta/kernel rescue is allowed.
- The UEC paper text's generic 70%-train statement conflicts with the released ETTh1 fixed-border loader. This is explicitly registered as a paper-code reproducibility discrepancy; if the released-source Host still fails, do not automatically run an alternative ETTh1 split.
- OMPB is fully decoupled from UEC. It must proceed to its own legal terminal state regardless of UEC PASS/FAIL/BLOCKED. Fixed anchors remain TCN/96 `ETTh1->ETTh2` and `Weather_train->Weather_test_far`; exact final-paper rows and official dataset provenance must be frozen before outcomes, and future-label perturbation must confirm predict-then-update causality. Protocol: `docs/current/HCH_OMPB_STANDALONE_MINIMAL_PAPER_FIDELITY_PROTOCOL_20260911.md`.
- Combined orchestration: `docs/current/HCH_UECSTD_OMPB_FIDELITY_RECOVERY_EXECUTION_PLAN_20260911.md`. Launcher: `experiments/current/hch_baseline_fidelity_reproduction/CODEX_UECSTD_HOST_RECOVERY_AND_OMPB_STANDALONE_20260911.md`. New evidence roots are under `experiments/evidence/hch_baseline_paper_fidelity_20260911/**`; old 2026-09-10 evidence is immutable.
- Terminal token after both independent tracks reach a legal terminal state: `HCH_BASELINE_BREADTH_RECOVERY_COMPLETE_FOR_ADJUDICATION`. This token does not imply both methods PASS.

### UEC-STD + OMPB minimal fidelity execution — partial/failed; UEC Host anchor not reproduced, OMPB unrun (2026-09-11)

- Token: `HCH_UECSTD_OMPB_MINIMAL_FIDELITY_PARTIAL_OR_FAILED`.
- U-A1 ETTh1/TimeMixer/336 completed under the fixed paper-authority protocol. Host MSE/MAE `0.596529/0.547638` vs paper `0.465/0.449`; UEC-STD `0.588395/0.544841` vs paper `0.449/0.444`. Host MSE error is ~28.3%, so the registered Host gate fails before UEC method fidelity can be judged. Reproduced MSE gain is 1.363% vs paper ~3.44%; paper beta `0.3`, current official auto-beta diagnostic `1.0`.
- U-A2 Weather/TimeMixer/336 did not complete; partial checkpoint excluded.
- OMPB was not entered; no target registry was generated, no source/target data were read, and no OMPB anchor exists.
- Scientific consequence: UEC-STD remains `NOT_PAPER_FAITHFULLY_REPRODUCED`; current evidence points first to TimeMixer Host/data/config mismatch, not a validated UEC method failure. OMPB remains `NOT_REPRODUCED`.
- This does not alter mandatory offline admission: δ-Adapter partial-fidelity accepted; PIR exact accepted. COSA remains partial/unresolved.
- Evidence: `experiments/evidence/hch_baseline_paper_fidelity_20260910/05_uecstd_ompb_minimal/FINAL_MINIMAL_BASELINE_FIDELITY_SUMMARY.md` and `UECSTD_FIDELITY_SUMMARY.md`.

### UEC-STD minimal fidelity preflight — valid blocker, then paper-authority resolution (2026-09-11)

- Executor token: `HCH_UECSTD_PROTOCOL_BLOCKED`. U0 failed closed before any data read/training because official helper materials conflicted on seasonal/trend weights, Weather kernel size, a broken `--is_training 9` path, and correction-strength candidate grids. This blocker was scientifically correct and remains recorded.
- Subsequent paper/appendix audit resolved season/trend, beta and correction-training details without result-conditioned search: 100 UEC steps; batch 64; Huber/SmoothL1; true AR; official `ecm=linear` two-stage MLP; kernel `25` for both selected TimeMixer anchors. Its earlier assumption of a common 70/10/20 backbone split is superseded for ETTh1 by the 2026-09-11 source-fidelity amendment: official released ETTh1 path uses `Dataset_ETT_hour` fixed ETT borders; Weather remains `Dataset_Custom`.
- U-A1 ETTh1/TimeMixer/336: `lambda_s/lambda_t=0.8/0.2`; paper-reported STD beta `0.3`; targets unchanged Host `0.465/0.449`, UEC `0.449/0.444`.
- U-A2 Weather/TimeMixer/336: `lambda_s/lambda_t=0.6/0.4`; paper-reported STD beta `0.1`; targets unchanged Host `0.265/0.292`, UEC `0.256/0.290`.
- Candidate-grid inconsistency is not resolved by search: current official auto-beta selection is diagnostic only; exact paper anchor is evaluated at the paper-reported beta and the match/mismatch is disclosed.
- UEC is now unblocked for exactly these two anchors. No third anchor/rescue variant is allowed. OMPB proceeds only after UEC summary under its original source→target online-shift protocol.
- Adjudication: `experiments/evidence/hch_baseline_paper_fidelity_20260910/05_uecstd_ompb_minimal/adjudication/INDEPENDENT_UECSTD_BLOCKER_ADJUDICATION_20260911.md`.
- Paper-authority resolution: `docs/current/HCH_UECSTD_PAPER_AUTHORITY_RESOLUTION_20260911.md`.


### HCH State-Excursion Shape closure — valid rejection; horizon-aligned semantic residual reopened (2026-09-11)

- Token: `HCH_STATE_EXCURSION_SHAPE_NOT_SUPPORTED_KEEP_S1`.
- X0 PASS. X1 FAIL: both GANSU HIGH-state Shape cosines decline (`-0.0307/-0.0906`) and TimeMixer wrong-hemisphere rises `+6.06 pp`; only 2/4 preregistered Shape-limited cells improve.
- X2 FAIL despite international gains: S2 is Host-nonworse 6/6 and improves S1 in 4/6, but median gain is only `5.380%`, max Normal-MAE harm is `3.175%`, and both GANSU cells are worse than S1.
- X3 PASS: all four international cells improve S1; X4 FAIL under the preregistered relative latency ratio, though absolute latency remains microseconds/day.
- Scientific consequence: reject signed same-hour excursion as a universal executable Shape input. Do not tune MAD factor/window/clipping or market-specific role subsets. Independent adjudication: `experiments/evidence/hch_state_excursion_shape_20260910/adjudication/INDEPENDENT_STATE_EXCURSION_SHAPE_ADJUDICATION.md`.
- New authorized single-factor closure: `docs/current/HCH_HORIZON_ALIGNED_STATE_RESIDUAL_SHAPE_CLOSURE_20260911.md`. It freezes S1 and fits exactly five horizon-aligned semantic-role coefficients on existing S1-standardized state levels; no excursion, conditional Amplitude, Gate, deeper network or market-specific architecture.

### HCH Market-State Coupling diagnostic — valid; state-distance hypothesis rejected, high-state Shape limitation supported (2026-09-10)

- Token: `HCH_MARKET_STATE_COUPLING_NOT_SUPPORTED`.
- Independent adjudication: `experiments/evidence/hch_market_state_coupling_20260910/adjudication/INDEPENDENT_MARKET_STATE_COUPLING_ADJUDICATION.md`.
- C0 PASS and C1 PASS_NONDEGENERATE: S1/fixed-alpha replay is exact; forecast-time state is legal/nondegenerate on the six-cell D0 panel.
- Primary `rho_shape -> a_O2-alpha` coupling is weak/mixed: GANSU `+0.070/-0.055`, DE `+0.150/+0.105`, PJM `-0.095/-0.010`; top-bottom primary bootstrap CIs cross zero. Only DE/PatchTST is classified `DISTANCE_STATE_COUPLED`.
- C4 one-feature chronological ridge improves the constant-zero distance baseline in only 2/6 cell medians; the preregistered state-Amplitude trigger fails and `A_state1` is not trained. Do not reopen nonlinear/state-aware Amplitude from this evidence.
- Stronger upstream result: 4/6 cells are `SHAPE_LIMITED`. Both GANSU Hosts show high-state Shape degradation: cosine high-minus-low about `-0.062/-0.185`, wrong-hemisphere `+25/+20 pp`. PJM cells are also Shape-limited under the registered attribution.
- Scientific consequence: retain pooled fixed alpha and deleted Verification; return to Shape representation. One new closure is authorized under `docs/current/HCH_STATE_EXCURSION_SHAPE_CLOSURE_20260910.md`: same S1 GRU32/target/loss/OOF, append only already-audited signed same-hour seven-day state excursions to the current-day Shape patch. No topology/depth/module expansion.
- Evidence root: `experiments/evidence/hch_market_state_coupling_20260910/`.

### Minimal UEC-STD + OMPB paper-fidelity breadth stage authorized (2026-09-10)

- User requested one final bounded reproduction pass for UEC-STD and OMPB; this does not reopen full-table baseline reproduction.
- UEC-STD old wrapper is not paper-faithful: default 2 epochs, wrapper-specific 70/30 split/target normalization/cache residual construction, and only selected official decomposition/linear ECM components. New anchors must use official AR residual-data generation + STD decomposition + Huber UEC training + validation coefficient selection + AR evaluation.
- Fixed UEC anchors: ETTh1/TimeMixer/336 paper Host `0.465/0.449`, UEC `0.449/0.444`; Weather/TimeMixer/336 Host `0.265/0.292`, UEC `0.256/0.290`. Maximum two anchors.
- OMPB remains online source→target shift only; no static HCH wrapper. Fixed preferred anchors: TCN/96 ETTh1→ETTh2 (20 backbone epochs) and Weather_train→Weather_test_far (5 epochs). Exact final-paper rows must be extracted/hash-frozen before running; unresolved row means fail closed.
- Controlling protocol: `docs/current/HCH_OMPB_UECSTD_MINIMAL_PAPER_FIDELITY_REPRODUCTION_20260910.md`; launcher: `experiments/current/hch_baseline_fidelity_reproduction/CODEX_UECSTD_OMPB_MINIMAL_FIDELITY_20260910.md`.
- No δ/PIR/COSA rerun and no HCH transfer is authorized by this stage.

### HCH Anchored Compact Amplitude closure — valid; Shape-only conditional distance rejected (2026-09-10)

- Token: `HCH_ANCHORED_COMPACT_AMPLITUDE_COMPLETE_FOR_ADJUDICATION`.
- Independent adjudication: `experiments/evidence/hch_anchored_compact_amplitude_20260910/adjudication/INDEPENDENT_ANCHORED_COMPACT_AMPLITUDE_ADJUDICATION.md`.
- A0 PASS: S1 replay max error `1.42e-14`, Shape cosine replay error `8.33e-17`; 26 trainable Amplitude parameters; Host/split/input/OOF contracts frozen.
- A1 SUPPORTED but local: compact OOF utility improves fixed-alpha OOF in 4/6 cells and is nonnegative in 6/6.
- A2 FAIL: Host-nonworse 5/6; improves frozen S1 final Overall-MAE in only 3/6; median relative Overall-MAE gain `5.79% < 7%`; >=5% in 3/6; PJM/PatchTST remains below Host; max Normal-MAE harm `1.06% > 1%`.
- A3 NOT_SUPPORTED: exact O2 per-day scalar oracle remains `10.78%--22.73%` relative gain, but compact median O2-over-fixed-alpha capture is only `2.01%`, positive in 4/6. Cell-median amplitude-oracle Spearman is approximately `-0.044 ... +0.126`, i.e. essentially no stable distance ordering.
- A4: compact beats the best frozen development comparator in 4/6, but this does not rescue A2/A3 and is not a final baseline-fidelity claim.
- Scientific consequence: reject the tested `[u_hat || ||v||] -> scalar` learned Amplitude and return the leading executable method to S1 Shape + pooled fixed `alpha`. Do not deepen the same Shape-only Amplitude input.
- Interpretation: scalar Amplitude as a mathematical object remains strongly supported by O2. The new bottleneck is missing conditional-distance information. Because normalized Shape supervision deliberately removes residual scale and `||v||` measures direction concentration rather than magnitude, forecast-time market state is the leading evidence source to audit next.
- Next design only: `docs/current/HCH_POST_AMPLITUDE_MARKET_STATE_COUPLING_DESIGN_20260910.md`. No execution is authorized by this ledger entry.

### Baseline paper-fidelity workstream closure — mandatory offline anchors sufficient; online/secondary baselines frozen incomplete (2026-09-10)

- Human adjudication stops the long-running baseline anchor program for the current HCH method stage. Closure: `experiments/evidence/hch_baseline_paper_fidelity_20260910/adjudication/BASELINE_FIDELITY_WORKSTREAM_CLOSURE_20260910.md`.
- Mandatory offline headline status is sufficient: δ-Adapter Ada-Y = `ACCEPTED_WITH_DISCLOSED_PARTIAL_FIDELITY` (Weather/Traffic PASS; Electricity formal borderline FAIL retained); PIR full official = `PAPER_FAITHFUL_EXACT_ACCEPTED` on both mandatory ETTh1/96 anchors.
- COSA is not fully paper-native reproduced. Latest-code B3 retains C-A1 F/P PASS, C-A2 F PASS, C-A2 P FAIL. Paper-era provenance resolution was user-stopped after C-PE1 Host only: 10-seed Host MSE `0.435901 ± 0.004820` vs paper `0.4312`, relative error `1.0902%`, Host gate PASS; no paper-era COSA method or C-PE2 run. Status: `COSA_ONLINE_PARTIAL_FIDELITY / PAPER_NATIVE_METHOD_UNRESOLVED`.
- OMPB was not run and remains optional online-shift; UEC-STD remains secondary/not paper-faithful. Neither blocks Tier-A offline HCH comparison.
- No more paper-anchor reproduction is active. The next required baseline step, only when HCH is ready for final comparison, is frozen unified-DA transfer of admitted offline δ-Adapter/PIR under the HCH task/split/information boundary. COSA may be resumed only if an online/TTA paper block is explicitly required.

### δ-Adapter paper-anchor reproduction — 2/3 exact anchors pass; method-level fidelity accepted with disclosure (2026-09-10)

- Stage token from executor: `HCH_BASELINE_PAPER_FIDELITY_BLOCKED`; this is procedurally correct because the stop rule requires human adjudication after any anchor failure.
- D-A1 Weather/iTransformer/96: PASS. Paper Host/Method MSE `0.1731/0.1631`; reproduced `0.171949/0.162725`; reproduced gain `5.36%` vs paper `5.78%`.
- D-A2 Traffic/iTransformer/96: PASS. Paper `0.4437/0.4294`; reproduced `0.430100/0.418650`; reproduced gain `2.66%` vs paper `3.22%`.
- D-A3 Electricity/iTransformer/96: formal FAIL on relative-gain agreement only. Host error 4.45% passes; method absolute error 2.22% passes; direction is correct; reproduced gain `3.17%` vs paper `5.38%`, deviation `2.21 pp` vs registered tolerance `2.00 pp`.
- No threshold/tolerance/seed/epoch is changed and D-A3 is not rerun. Independent adjudication accepts δ-Adapter Ada-Y at the method level as `DELTA_ADAPTER_ANCHOR_ACCEPTED_WITH_DISCLOSED_PARTIAL_FIDELITY`, consistent with the pre-existing headline-admission rule requiring at least one exact passing anchor and no core substitution.
- The historical 2-epoch cache wrapper remains pilot-only and is superseded for strong-baseline work by this paper-anchored official-style implementation.
- Next authorized baseline stage: B2 full-official PIR anchors only. No HCH transfer, COSA or OMPB yet. Adjudication: `experiments/evidence/hch_baseline_paper_fidelity_20260910/adjudication/INDEPENDENT_DELTA_ADAPTER_ANCHOR_ADJUDICATION.md`; launcher: `experiments/current/hch_baseline_fidelity_reproduction/CODEX_CONTINUE_PIR_AFTER_DELTA_ADJUDICATION_20260910.md`.

### PIR full-official paper anchors — exact fidelity PASS on PatchTST + TimeMixer (2026-09-10)

- Token: `HCH_PIR_PAPER_FIDELITY_ANCHORS_COMPLETE_FOR_ADJUDICATION`.
- Independent adjudication: `experiments/evidence/hch_baseline_paper_fidelity_20260910/adjudication/INDEPENDENT_PIR_ANCHOR_ADJUDICATION.md`.
- P-A1 ETTh1/PatchTST/96: Host `0.409700/0.415855` MSE/MAE vs paper `0.410/0.416`; PIR `0.373362/0.399086` vs paper `0.375/0.400`; reproduced MSE gain `8.869%` vs paper `8.537%`. PASS.
- P-A2 ETTh1/TimeMixer/96: Host `0.383801/0.394497` vs paper `0.384/0.399`; PIR `0.370109/0.391756` vs paper `0.370/0.397`; reproduced MSE gain `3.567%` vs paper `3.646%`. PASS.
- Full-path fidelity confirms official PIR commit, pretrained Host loading, QualityEstimator, Transformer Refiner, retrieval-index/global revision and learned local/global combination; no ridge/sklearn proxy path was executed.
- Scientific status: `PIR_FULL_OFFICIAL_ANCHOR_ACCEPTED` / `PAPER_FAITHFUL_EXACT_ACCEPTED`. Historical HCH `src/baselines/pir.py` remains a limited proxy and cannot be retroactively renamed.
- Offline Tier-A anchors are now established: δ-Adapter accepted with disclosed partial fidelity; PIR exact fidelity accepted. Next separate baseline stage is COSA online/TTA, using final ICLR 2026 Table-2 targets and 10-run chronology-matched evaluation. OMPB and HCH transfer remain closed pending adjudication.
- Evidence root: `experiments/evidence/hch_baseline_paper_fidelity_20260910/02_pir_anchor/`.

### COSA latest-code 10-run anchors — partial pass; paper-era provenance mismatch discovered (2026-09-10)

- Token: `HCH_COSA_PAPER_FIDELITY_BLOCKED`; the registered stop is valid and no B3 result is rewritten.
- Latest official commit used: `527c0fe` (2026-08-13). C-A1 COSA-F PASS (`0.423314`), C-A1 COSA-P PASS (`0.424210`), C-A2 COSA-F PASS (`0.561657`), C-A2 COSA-P FAIL (`0.589729` vs paper `0.5320`; gain `-4.624%` vs `+4.299%`). Ten of ten C-A2/P seeds are harmful, so the failure is systematic under this protocol.
- All 40 formal runs passed strict no-leakage checks and preserved Host hashes; no seed/config/tolerance was changed.
- Independent provenance audit found the latest commit is post-paper and explicitly adds `POGT adaptation and MLP adapter`, materially introducing partial-ground-truth masks, delayed/full-horizon replay and prediction adjustment. The final ICLR paper/presentation says the reported method relies on full ground truth and treats partial-ground-truth deployment as future work. Thus latest-code B3 is a deployment-safe/current-code result, not necessarily the Table-2 generation path.
- The paper-era first public commit is `43a8c8d` (2026-03-04). It also hard-codes ETTh1 `TRAIN_RATIO=0.6`, `TEST_RATIO=0.2`, while the paper text states 7:1:2; this is a disclosed paper/code split discrepancy.
- Scientific status: COSA-F latest-code is supported on both anchors; COSA-P latest-code is supported at 96 but not 336. Overall paper-native COSA fidelity remains unresolved due source-version/protocol mismatch rather than being declared irreproducible.
- Exactly one paper-era official-code-native rerun is authorized: commit `43a8c8d`, native ETT split, paper-era full-GT batch semantics, fixed seeds 0..9 and unchanged K/S/B/LR, rerunning both C-A1/C-A2 F/P. No further source/config search afterward.
- Controlling adjudication: `experiments/evidence/hch_baseline_paper_fidelity_20260910/adjudication/INDEPENDENT_COSA_ANCHOR_ADJUDICATION.md`; protocol: `docs/current/HCH_COSA_PAPER_ERA_CODE_NATIVE_REPRODUCTION_20260910.md`.

### Baseline paper-fidelity audit/design — proxy status corrected; reproduction workstream opened (2026-09-10)

- No new baseline scientific result has been claimed yet; this entry records a source/paper audit and a preregistered reproduction protocol.
- Historical δ-Adapter Ada-Y wrapper is not paper-faithful: it uses residual-std scaling instead of official bounded `delta`, LR `1e-3` instead of paper/source `1e-4`, a clipped small hidden width, full-batch fitting, and historically only 2 epochs. Old results remain pilot evidence.
- Historical PIR wrapper is explicitly a cache proxy: it reuses `QualityEstimator` but substitutes the official local Transformer refiner with ridge residual prediction and does not reproduce official retrieval-value semantics or quality auxiliary training. Old PIR proxy results must not be called PIR paper performance.
- Historical UEC-STD wrapper reuses selected source components but not the full source error-correction data construction/training/AR protocol; it is secondary pilot evidence.
- Exact paper anchors are preferred. Paper-derived transfer ranges are preregistered when exact data/backbones are unavailable and must be frozen before unified-DA evaluation. Exact reproduction checks both Host metric closeness and postprocessor metric/gain closeness.
- Offline headline candidates: Host, direct Linear, δ-AdaY, PIR; UEC-STD secondary if task-compatible. COSA is online/TTA only. OMPB requires a dedicated distribution-shift online track; NetBurst is Related Work only.
- Controlling design: `docs/current/HCH_BASELINE_PAPER_FIDELITY_AUDIT_AND_REPRODUCTION_DESIGN_20260910.md`.
- Execution protocol: `docs/current/HCH_BASELINE_PAPER_FIDELITY_EXECUTION_PROTOCOL_20260910.md`.
- Short launcher: `experiments/current/hch_baseline_paper_fidelity/CODEX_GOAL_PROMPT_20260910.md`.
- New evidence root: `experiments/evidence/hch_baseline_paper_fidelity_20260910/**`.

### Baseline paper-fidelity reproduction program registered (2026-09-10)

- New controlling design: `docs/current/HCH_BASELINE_PAPER_FIDELITY_REPRODUCTION_AUDIT_20260910.md`; executor: `experiments/current/hch_baseline_fidelity_reproduction/CODEX_GOAL_PROMPT_20260910.md`.
- Existing δ-Adapter/PIR numbers are explicitly retained as historical pilot/development evidence, not paper-faithful SOTA reproduction.
- δ audit: current wrapper uses official Ada-Y `PostProcessingNet` but materially changes LR, batching, residual scaling, hidden-size selection and validation/early-stopping semantics; increasing epochs is insufficient.
- PIR audit: current wrapper is a limited proxy because the official Refiner is replaced by ridge and retrieval/revision semantics differ; increasing epochs is insufficient.
- Reproduction doctrine: first pass paper-native Host + method anchors, then transfer a verified implementation to HCH. Host anchor tolerance defaults to 5%; method absolute metric tolerance defaults to 5% plus relative-gain agreement. No unregistered hyperparameter search to match paper numbers.
- Planned exact anchors: δ Ada-Y batch on Weather/Traffic/Electricity + iTransformer; PIR ETTh1 horizon96 + PatchTST/TimeMixer, with optional Electricity96. COSA/OMPB are separate online/TTA anchors, not ordinary offline baselines.
- HCH-transfer estimates must be derived and frozen from published same-backbone/closest-horizon relative gains before HCH outcomes. Current 10-epoch development comparators are not upgraded by this registration.
- New evidence root: `experiments/evidence/hch_baseline_paper_fidelity_20260910/**`; anchor stage must stop for human adjudication before any HCH full-transfer/final matrix.

### HCH Unified-DA Shape Engineering canary — valid material-gain pass; compact Amplitude reopened (2026-09-10)

- Token: `HCH_UNIFIED_DA_SHAPE_ENGINEERING_CANARY_COMPLETE_FOR_ADJUDICATION`.
- Independent adjudication: `experiments/evidence/hch_unified_da_shape_engineering_20260910/adjudication/INDEPENDENT_UNIFIED_DA_SHAPE_ADJUDICATION.md`.
- MG0 PASS: `GANSU_DA` uses a true day-ahead target and new target-compatible Host foundation; target-day DA/RT truth is not used as input; protected/final roles remain closed.
- S0 State-Linear misses the preregistered material bar, so S1 Daily-Patch GRU-32 Shape runs under protocol. S1 passes MG1--MG4: 5/6 Host-nonworse, 4/6 >=3% gain, 3/6 >=5%, median relative Overall-MAE gain 5.85%, maximum 10.74%, maximum Normal relative harm 0.85%, and 4/6 wins over the fixed development comparator. S1 uses only ~14,104--18,712 parameters.
- S1 gains are seed-stable in five cells. PJM/PatchTST is the real counterexample: S1 relative gain is about `-1.39%, +0.22%, -0.87%` across seeds and Shape cosine is consistently worse than S0. Do not hide this cell or explain it away as seed noise.
- The decisive new result is conditional ray-distance headroom. With the **same learned S1 direction**, exact per-day MAE-optimal scalar O2 gain is ~10.78--22.73%; extra O2-over-S1 headroom is ~8.5--13.5 percentage points in every cell. Therefore compact conditional Amplitude is scientifically reopened after having been correctly rejected in its former 648-D form.
- Next authorized learner is exactly one 26-parameter anchored compact Amplitude on `[u || ||v||]`, initialized to reproduce the existing pooled scalar `alpha` and trained only from chronological OOF S1 Shape under direct MAE ray loss. Shape is frozen; learned Verification remains removed; no raw-state Amplitude, hidden layer, Gate, Transformer/router/retrieval or hyperparameter search.
- Controlling design: `docs/current/HCH_ANCHORED_COMPACT_AMPLITUDE_DESIGN_20260910.md`.
- Evidence root: `experiments/evidence/hch_unified_da_shape_engineering_20260910/`.

### HCH Material-Gain + Market-Structure audit — valid; task-confounded China track retired; Shape capacity promoted (2026-09-10)

- Token: `HCH_MATERIAL_GAIN_MARKET_AUDIT_COMPLETE_FOR_ADJUDICATION`.
- Baseline-fidelity convergence: GANSU Ada-Y relative harm drops from ~13.2%/11.5% at 2 epochs to ~4.2%/1.1% at fixed 10 epochs; maintained PIR cache proxy remains ~25.5%/35.6% harmful. Therefore prior `generic GANSU adapter failure` is not supported; the 2-epoch adapter pilot was too weak for final baseline claims.
- The former Chinese task was `DA information -> RT price`. The user has now replaced it with the same main task as international benchmarks: **DA price -> next-24h DA price**. All GANSU_RT material-gain/state results remain historical diagnostics only and must not become main paper evidence.
- Track-A oracle ceilings are still valid and decisive: DE/PJM per-day Amplitude oracle gains are roughly 10–18%; Shape fixed-scale oracle gains roughly 9–31%; Gate headroom is much smaller. The main next bottleneck is proposal Shape quality, with Amplitude secondary.
- State-enriched linear Shape, with unchanged Shape target/loss and unchanged closed-form scalar calibration, reaches about 9.32%/7.37% relative Overall-MAE gain on DE and 1.51%/7.67% on PJM, proving that legal forecast-time data can materially improve Shape without adding scientific modules.
- Current method remains `Shape -> calibrated scalar`, with learned Verification removed. The old 648-D conditional Amplitude remains rejected.
- New authorized development design: `docs/current/HCH_UNIFIED_DA_MATERIAL_REPAIR_ENGINEERING_DESIGN_20260910.md`. Canary is `LAGO_DE / LAGO_PJM / GANSU_DA × PatchTST / TimeMixer`; S0 State-Linear first, followed only if needed by one S1 Daily-Patch GRU-32 Shape encoder. No simultaneous Amplitude change, no Transformer/router/retrieval/Gate.
- Final baseline paper-faithful reproduction is deferred. Current Ada-Y 10 epoch / PIR proxy 10 epoch runs are development comparators only.
- Evidence root retained: `experiments/evidence/hch_material_gain_market_audit_20260909/`.

### HCH Compact Verification closure — valid minimal test; learned Verification removed (2026-09-09)

- Token: `HCH_COMPACT_VERIFICATION_CLOSURE_COMPLETE_FOR_ADJUDICATION`; V0 PASS, V1 descriptive, V2 FAIL, V3 PASS, V4 `REMOVE_LEARNED_VERIFICATION`.
- Independent adjudication: `experiments/evidence/hch_compact_verification_closure_20260909/adjudication/INDEPENDENT_COMPACT_VERIFICATION_ADJUDICATION.md`.
- Proposal replay is exact to `~5.53e-8`; only one 49-parameter linear sigmoid verifier on `[v || delta_tilde]` was tested; no prohibited data/learner was opened.
- Selective execution is non-worse than calibrated always-repair in only 2/6 cell medians; median gain versus always is `-0.01172` native MAE; maximum relative Overall harm versus always is 1.21% and maximum Normal harm 1.38%.
- B4 utility ordering is positive in only 2/6 and inverse in 4/6. Although selected-utility mean is positive in all six cells and selective still beats Host in 6/6, this does not justify keeping a verifier because calibrated always-repair is better in 4/6.
- Scientific consequence: remove learned Verification from the leading implementation. Do not rescue with a larger verifier, threshold search, GRU/MLP/Transformer or extra confidence features. Utility/selective headroom remains diagnostic only.
- Materiality correction: the frozen calibrated-ray proposal improves Host Overall-MAE by only about `0.11%, 1.12%, 1.30%, 0.49%, 0.76%, 5.34%` across the six canary cells. It is proof-of-concept evidence, not yet a paper-strength SOTA result.
- Comparator caveat: current canary Ada-Y/PIR rows are not final SOTA evidence (`epochs=2`; PIR has a registered wrapper substitution; GANSU legal market-state covariates are not fully consumed). The next stage must audit baseline fidelity and market/task structure before any China-specific failure claim.
- Next authorized stage: `docs/current/HCH_MATERIAL_GAIN_AND_MARKET_STRUCTURE_AUDIT_20260909.md`; no new method module is authorized by this verdict.
- Evidence root: `experiments/evidence/hch_compact_verification_closure_20260909/`.

### HCH Calibrated-Ray closure — valid six-cell pass; conditional Amplitude removed (2026-09-09)

- Token: `HCH_CALIBRATED_RAY_CLOSURE_COMPLETE_FOR_ADJUDICATION`.
- Independent adjudication: `experiments/evidence/hch_calibrated_ray_closure_20260909/adjudication/INDEPENDENT_CALIBRATED_RAY_ADJUDICATION.md`.
- C0 PASS: Shape replay max diff `4.44e-16`, R2 constant replay max diff `4.30e-8`, Host/prior evidence hashes unchanged.
- C1 PASS: Calibrated-Ray Overall-MAE non-worse and strictly better than Host in `6/6`; Tail-MAE improves `5/6`; Normal-MAE improves in all six cell medians.
- C2 PASS as a development comparator gate: within 1% of the best admitted comparator in `5/6` Overall and `4/6` Tail cells. PJM/TimeMixer remains the main Overall comparator gap.
- C3 SUPPORTED: remove the 648-D conditional Amplitude network from the leading method. Freeze proposal as `Delta=s*alpha*u_hat`, with one pooled nonnegative MAE calibration scalar fitted from chronological OOF Shape/residual pairs. OOF utility is nonnegative in `5/6` cell medians.
- C4 human adjudication: KEEP Verification as a scientific object for exactly one compact learnability closure. Calibrated proposals remain harmful on roughly `29%–48%` of eval days; oracle selective headroom is positive in all six cells and about `0.8%–4.7%` relative MAE.
- Next authorized learner: one 49-param linear sigmoid verifier on `[v || delta_tilde]`, utility-weighted BCE, fixed `q>0.5`; no raw 624-D state, conditional Amplitude, MLP/GRU/Transformer, alternate verifier, threshold tuning, full-panel, Shandong or protected/final execution. If this compact verifier fails, learned Verification is removed rather than rescued.
- Evidence root: `experiments/evidence/hch_calibrated_ray_closure_20260909/`.

### HCH Minimal Repair M0 R2 — valid Amplitude revision; conditional map rejected, calibrated-ray closure promoted (2026-09-09)

- Machine token: `HCH_M0_R2_CANARY_COMPLETE_FOR_ADJUDICATION`; validity=true. Independent adjudication: `experiments/evidence/hch_minimal_repair_m0_20260909_r2/adjudication/INDEPENDENT_M0_R2_ADJUDICATION.md`.
- R2 is a valid one-factor follow-up: Shape replay differs from R1 by at most `~4.44e-16`, R1 Amplitude replay differs by at most `~2.28e-7`, Host caches are unchanged, and the only scientific change is removal of the `J>0` Amplitude-training filter.
- R2 improves vs R1 in 4/6 cells for OOF utility, ray regret, amplitude-oracle gap and always-repair MAE, but learned OOF proposal utility is positive in only 2/6; full conditional M0 remains not supported.
- The preregistered chronological constant-Amplitude probe is valid and decisive: it uses only earlier OOF blocks for each prediction block and DIAG_FIT-only OOF information for final calibration. Cell-median constant OOF utility exceeds learned R2 utility in 6/6 cells; learned R2 beats constant in 0/6 cell medians.
- Learned Amplitude does not rank the exact per-instance ray oracle: cell-median Spearman is about `-0.267 .. +0.073`; ray-oracle capture is negative in four cells, near zero in one, and only ~0.107 in PJM/TimeMixer. The 648-D state-conditioned linear+Softplus Amplitude map is therefore rejected as the leading implementation.
- Scalar ray Amplitude is **not** rejected. The diagnostic constant normalized amplitude applied to the same final learned Shape improves DIAG_EVAL Overall-MAE vs Host in 17/18 seed×cell runs and all 6/6 cell medians (median gains roughly +0.022 to +1.160 native MAE depending on cell).
- Scientific consequence: promote a one-parameter calibrated ray `Delta=s_d*alpha*u_hat`, with `alpha` fitted exactly from chronological OOF Shape predictions as the pooled nonnegative MAE weighted-median solution. This is tested next in `docs/current/HCH_CALIBRATED_RAY_CLOSURE_EXPERIMENT_20260909.md`.
- Verification remains unresolved rather than automatically retained: R2 verifier ordering is positive in only 1/6 cells and selected utility mean is negative in all six, but proposal quality is confounded. The calibrated-ray closure must quantify oracle selective headroom before deciding whether Verification survives.
- Capacity consequence: do not run GRU next. The registered ladder is calibrated-ray closure -> optional 24-D Shape-state compact conditional heads -> optional GRU-32 only if Shape representation remains limiting.
- Evidence root: `experiments/evidence/hch_minimal_repair_m0_20260909_r2/`.

### HCH Minimal Repair M0 R1 canary — valid run, not supported for full expansion (2026-09-09)

- Machine verdict: `HCH_M0_R1_CANARY_NOT_SUPPORTED_FOR_FULL_EXPANSION`; G0=`PASS`, G1=`PASS`, G2=`FAIL`, G3=`PASS`, G4=`FAIL`.
- Independent adjudication: `experiments/evidence/hch_minimal_repair_m0_20260909_r1/adjudication/INDEPENDENT_M0_R1_ADJUDICATION.md`; it agrees with the no-expansion verdict but narrows the failure attribution.
- G0 implementation/provenance is accepted: 22/22 targeted tests + compileall PASS; clean `src/mvp/hch_minimal_repair/**`; chronological stacked OOF; no historical HCH scientific import; raw/S3/S4/protected/final reads all zero; Host/base source immutable.
- G1 Directional Shape remains supported as an object: cell-median Shape cosine is positive in all 6/6 cells (`~0.0876–0.5249`) and positive-hemisphere rate exceeds 0.5 in all 6/6. Except PJM/TimeMixer, cosine remains modest, so M0 linear representation sufficiency is not established.
- G2 fails decisively: M0 Overall-MAE is non-worse than Host in 0/6 cells and strictly improves in 0/6; Tail-MAE improves 3/6; maximum Normal-MAE relative harm is 7.49%. Full-panel expansion is not justified.
- G3 PASS is comparator-relative only: M0 is within 1% of the best admitted post-hoc comparator on Overall/Tail in 5/6 cells, but the comparators themselves are usually worse than Host. Direct Linear and Ada-Y are harmful vs Host in all six Overall-MAE cells; PIR is harmful in five of six and improves only PJM/TimeMixer. This supports adapter-risk motivation, not M0 effectiveness.
- The strongest mechanism result is Amplitude-specific. For every one of the 18 market×Host×seed rows, the exact MAE-optimal nonnegative scalar along the **same OOF predicted Shape** has positive mean utility. Cell-median oracle scalar gains are positive in all six cells (`~+0.042 to +0.224` normalized), whereas learned OOF proposal gain is negative in five of six. Therefore one-scalar Amplitude is not rejected; the current state→scalar learner/training semantics are the primary failure.
- R1 trained Amplitude only on `J>0` rows but emitted Softplus amplitudes on all repair-available rows. Final Amplitude support is only 115–144 rows on LAGO and 22–23 on GANSU_RT for a 649-parameter head; verifier support is 147/26 rows for a 651-parameter head. Thus “linear” M0 is shallow but statistically high-dimensional.
- G4 learned Verification is not supported for the current proposal distribution: only 1/6 cells passes the full nondegeneracy gate; utility ordering is unstable. Verifier capacity is not yet isolated because five of six upstream OOF proposal distributions have negative mean utility.
- Method consequence: do not add GRU/Transformer or change Verification first. The leading R2 design revises Amplitude to the full nonnegative MAE ray projection `argmin_{a>=0} ||e-a u_OOF||_1` on **all repair-available OOF Shape rows**, allowing zero as a legitimate distance. R2 design: `docs/current/HCH_M0_R2_RAY_PROJECTED_AMPLITUDE_DESIGN_20260909.md`. No R2 execution is automatically authorized.
- Evidence root: `experiments/evidence/hch_minimal_repair_m0_20260909_r1/`.

### HCH Minimal Repair M0 first canary preflight — invalid input contract, no scientific run (2026-09-09)

- Verdict: `HCH_M0_CANARY_INVALID`; G0=`INVALID`, G1-G4=`NOT_RUN`.
- Scope audited: fixed `LAGO_DE / LAGO_PJM / GANSU_RT × PatchTST / TimeMixer` six-cell canary, using existing frozen Host caches only.
- LAGO_DE and LAGO_PJM have complete 168h original-origin historical Host-residual coverage on DIAG_FIT/DIAG_EVAL for both Hosts.
- GANSU_RT has incomplete historical Host-residual coverage for both Hosts: `30/62` DIAG_FIT rows and `4/21` DIAG_EVAL rows are incomplete. Missing periods are identical across PatchTST/TimeMixer, indicating a common historical forecast-origin/cache coverage issue rather than backbone-specific behavior.
- The preflight correctly stopped before creating `src/mvp/hch_minimal_repair/**`, before Shape/Amplitude/Verification training, and before any canary metric evaluation. Raw/S3/S4/protected/final reads = `0/0/0/0/0`; Host retraining = false.
- Scientific interpretation: **no method verdict**. The first implementation spec over-required a fully observed 168h residual history. The protocol is repaired globally, not by market exception: add a 168h residual-availability mask, zero-fill only masked residual coordinates, compute causal scale from available residuals, and exact Keep Host when zero residual history is available. Complete-history cells retain all-one masks.
- Original invalid evidence is immutable at `experiments/evidence/hch_minimal_repair_m0_20260909/`. The repaired R1 canary is authorized to write only under `experiments/evidence/hch_minimal_repair_m0_20260909_r1/`.

### HCH Repair Diagnostic Evidence Program — completed machine run + independent adjudication (2026-09-08)

- Machine completion token: `HCH_REPAIR_DIAGNOSTICS_COMPLETE`; evidence root: `experiments/evidence/hch_repair_diagnostics_20260908/`.
- Scope: PAPER8 public international electricity-price benchmarks plus registered China DA→RT stress track; PatchTST/TimeMixer Hosts; simple Linear/Tiny-MLP probes plus existing δ-Adapter/PIR wrappers. Host fitting used S1; diagnostics used DIAG_FIT/DIAG_EVAL only at the model/evaluation layer.
- Independent scientific adjudication: `experiments/evidence/hch_repair_diagnostics_20260908/adjudication/INDEPENDENT_DIAGNOSTIC_ADJUDICATION.md`; adjudicated machine-readable verdict: `adjudication/ADJUDICATED_VERDICT.json`. These supersede the machine-generated `verdict.json` where they differ.
- D1 Host-error heterogeneity: **SUPPORTED**. 21/24 public market×Host cells satisfy the diagnostic tail screen; median top-5% MSE concentration = 0.4071; median P95/median day-loss ratio = 2.2652.
- D2 Shape/Alignment: **SUPPORTED** with `HORIZON_DIRECTION_SIGNAL`. Across 144 public market×Host×candidate cells, median hourly WDR = 0.4693. Hourly sign correction improves MSE relative to the original candidate in 144/144 cells (median incremental recovery 36.0105 native squared-price units); a full 24h direction oracle adds further positive recovery in 144/144 cells (median +17.5930). Median sign share of total direction-oracle recovery = 65.9%, so sign alone is insufficient.
- D3 Amplitude: **SUPPORTED as a necessary object, final parameterization unresolved**. On direction-correct hours, median overshoot = 4.41% and median strong under-correction = 83.29%. Correctly recomputed scalar projection improves 129/143 public candidate cells but captures only median 32.64% of horizon-wise amplitude-recoverable error under the current simple-candidate directions. The original machine verdict `AMPLITUDE_SUPPORTED_SCALAR_PROJECTION` is rejected because its `scalar capture=2.9670` ratio used the wrong denominator and could exceed 1. Scalar-vs-horizon-wise must be retested after Shape is explicitly modeled as a normalized 24h direction.
- D4 proposal-level Verification: **HEADROOM SUPPORTED; LEARNABILITY UNRESOLVED**. Median harmful candidate-day rate = 60.28%; positive oracle selective headroom occurs in 144/144 public cells; always-repair beats Host in only 53/144 cells whereas oracle selective repair beats Host in 130/144. True realized Host-badness has median AUROC only 0.6263 for candidate benefit, and the worst-20%-Host subset still has median harmful-repair rate 0.50. This supports the scientific distinction `Host is bad != this proposal is beneficial`. The machine `VERIFICATION_LEARNABILITY_SUPPORTED` verdict is invalidated because the verifier was trained on in-sample DIAG_FIT candidate utility rather than OOF candidate utility.
- Introduction evidence: I1 error heterogeneity = SUPPORTED; I2 wrong direction material = SUPPORTED; I3 direction-correct not sufficient = SUPPORTED/qualified; I4 proposal utility differs from Host error = SUPPORTED; I5 market/regime repair-risk spectrum = UNRESOLVED; I6 China stress track extends that spectrum = UNRESOLVED. Exact admissible wording is registered at `adjudication/INTRO_EVIDENCE_REGISTER_ADJUDICATED.md`.
- Boundary correction: the runner read complete raw target columns before assigning the new diagnostic S1/S2/S3/S4 labels. Therefore the newly created diagnostic S3/S4 slices are **consumed/contaminated under strict read semantics** and may not later be presented as untouched protected final truth. This does not invalidate the DIAG_EVAL diagnostic evidence, but it changes future evaluation governance.
- Method consequence: retain a minimal `Frozen Host -> normalized horizon Shape/direction -> Shape-conditioned Amplitude -> concrete proposal -> proposal-level selective decision` research structure. Do not restore historical Bridge/Global/router/CORN/quantile/market-expert complexity. Immediate next stage is mathematical target derivation and Shape-conditioned/OOF utility design discussion; no new experiment is automatically authorized by this verdict.

### HCH Repair Diagnostics D0 preflight — infrastructure blocker, no scientific read (2026-09-08)

- Verdict: `HCH_REPAIR_DIAGNOSTICS_BLOCKED_PREFLIGHT`.
- Scope: registered `PAPER8 + CHINA5` × `PatchTST/TimeMixer` preflight only; D1–D4 were not executed.
- Result: the first executor found `0/26` compatible maintained frozen Host cache cells at the originally required path and therefore stopped before opening scientific raw observations. `scientific_raw_reads=0`; S3/S4/protected/reserved reads = 0; Shanxi was not invented/substituted.
- Scientific validity: **no Shape/Amplitude/Verification verdict**. This is an execution/protocol finding, not evidence against the method hypothesis.
- Independent audit found the protocol had over-constrained `frozen Host` into `pre-existing Host cache`. Maintained foundation code already supports the legitimate lifecycle `train Host on preregistered S1 -> freeze -> immutable cache`, so missing cache alone is no longer a blocker in the revised design.
- Additional preconditions identified before rerun: task/target semantics must separate PAPER8 DA from CHINA5 CN-ERB DA→RT, and timestamp/timezone/DST episode semantics must be bound reproducibly. Revised D0 is `D0-A contract -> D0-H Host foundation -> D0-C leakage closure`; D1–D4 remain pending.
- Evidence: `experiments/evidence/hch_repair_diagnostics_20260908/preflight/`.

### HCH V3.1 Shape Semantic Identification (SSI) — single authorized real execution (2026-09-05)

- Verdict: `HCH_V3_1_SSI_SOURCE_SIGNAL_PRESENT_TARGET_TRANSFER_FAILED` (Case B).
- Validity: `G0=True`; all registered provenance/immutability/data-boundary checks pass. Exactly one real runner call completed normally. Allowed real access counts: SOURCE_GTRAIN 12, SOURCE_GVAL 12, TARGET_D1 6, TARGET_D2 6, CAUSAL_HISTORY_CONTEXT 36. S2V/S3/S4/fresh successful access = 0/0/0/0; final Gate execution = 0.
- Source result: B1 extreme-repair semantics beats A1 generic residual semantics in all 3/3 target-excluded source G-Val settings. Seed-median ER-AUPRC ratios are approximately DE 1.199, NORD 1.822, PJM 1.215.
- Target D2 result: B1 beats A1 in only 2/6 cells. B1/A1 seed-median ER-AUPRC ratios are DE/PT 1.533, DE/TM 1.492, NORD/PT 0.962, NORD/TM 0.926, PJM/PT 0.729, PJM/TM 0.779; six-cell median = 0.944307 < registered 1.20. Therefore G1 fails and G2 is not promotion-evaluated.
- NormalDiversion diagnostic: B1 improves 4/6 cells; six-cell median 1.0000 -> 0.937291. This passes the diversion subcondition but cannot rescue G1.
- Direction diagnostic (report-only): pooled six-cell active-direction macro-F1 is 0 for B1 and B2 for seeds 7/17/37, with 469 active rows per seed (358 Down / 111 Up), indicating unresolved target-side posterior/Identity dominance.
- Scientific interpretation: the frozen V3 representation contains learnable extreme-repair signal, but target semantics + current three D1 Shape scalars do not transfer robustly across markets. The next bottleneck is minimal target-local Shape adaptation/sharing; do not move to Amplitude, Gate, class/focal/threshold rescue, or fresh panel.
- Evidence: `experiments/evidence/hch_v3_1_shape_semantic_identification/`; independent adjudication: `experiments/evidence/hch_v3_1_shape_semantic_identification/INDEPENDENT_SSI_SCIENTIFIC_ADJUDICATION_20260905.md`.

### HCH V3.1 D2 mechanism validation (2026-09-04)

- Verdict: `HCH_V3_1_D2_MECHANISM_VALIDATION_PASS_AWAITING_FRESH_PANEL_DESIGN`.
- Scope: one registered validation on the existing six development cells using
  only legal S1/S2T-derived source/G-Val/D1/D2 data; no consumed S2V/S3/S4
  truth access.
- `SIGNED_MAE_BAYES_GRID_V1` reused the frozen V3 learned distributions and
  changed only the parameter-free Gate action readout. Availability was 5/6,
  nonnegative D2 Overall-MAE gain was 6/6, and Normal-MAE harm violations were
  0. Historical V3 fixed-policy replay was preserved.
- Evidence:
  `experiments/evidence/hch_v3_demo_validation/v3_1_d2_mechanism_validation/`.
  This is a mechanism result only; it does not establish V3.1 effectiveness.
- Independent adjudication accepts the MAE-consistent action policy but withholds fresh-panel execution because the registered D2 selector remains Overall-MAE-first. D2 Tail-MAE gain vs Identity is approximately `0%, 0%, +0.10%, 0%, -0.50%, -0.22%` across DE/PT, DE/TM, NORD/PT, NORD/TM, PJM/PT, PJM/TM; the two PJM cells improve Overall-MAE while slightly worsening Tail-MAE. Independent verdict: `HCH_V3_1_D2_ACTION_POLICY_PASS / TAIL_OBJECTIVE_ALIGNMENT_REQUIRED_BEFORE_FRESH_PANEL`. Next registered development experiment is one D2-only `D2_TAIL_FIRST_SAFE_LEXICOGRAPHIC_V1` selector validation; no S2V/S3/S4 or fresh-panel execution is authorized yet.

| Stage | Primary verdict / status | Main result |
|---|---|---|
| V2.5 Phase D | OPEN_V2_6_UPSIDE_REPAIR | corrected target competitive only in selected cells; transfer LOMO/LOHO viable but double-unseen weak; repeated upper-branch weakness identified |
| V2.6-U | NO_V26_UPSIDE_CANDIDATE | Up structural variants U1-U4 failed; training often actively suppressed Up |
| V2.6-L | NO_V26_LEARNING_SIGNAL_CANDIDATE | L2/L3 increased positive correction and improved UpperTail but harmed Lower/overall; issue became selectivity/representation rather than capacity |
| V2.6 R0-R5 | MIXED_REPRESENTATION_FAILURE | large oracle geometry headroom; HES has incremental information; direct routing fails; safe release preferable but double-unseen weak |
| V2.6-SR | SELECTIVE_RELEASE_REJECTED | SR2 selected; universal S3 certificate failed; later consistency correction established real count 14/18 safe, 4/18 fail |
| V2.6-HCR | HIERARCHICAL_RELEASE_REJECTED | certifiability partially predictable; HCR4 failed universal S3-B certificate |

| V6.2 HERA H0 | INTERNAL_NOT_SUPPORTED | H0-A failed occurrence stop (day lift >1 in 3/6 cells; positive day+G1 cells 3/6); H0-B failed for Q1/Q2/R2; no S2-V/S3/S4 |

V6.2 machine root: `experiments/evidence/v6_2_hierarchical_extreme_risk_allocation/`. All three registered variants used legal previous-168h context, frozen host24, IAMR body24, OOF rank reliability, and empirical weighted-median actions. The NORD F1 zero-support condition was handled by the preregistered same-host fallback. Independent gate recomputation and final audit passed; no rescue or post-gate data access occurred.
| V2.6-HCA | HOST_CERTIFIABILITY_NOT_PREDICTABLE / bounded diagnostic | binary host certifiability weak; capacity target appeared predictable but did not justify host-family adaptation |
| V2.7-PCC | CERTIFIED_PREFIX_CAPACITY_NOT_VIABLE | certificate-aligned prefix capacity failed 4/6 viability requirement; universal controller line stopped |
| CFR | ONLINE_RECOVERABILITY_NOT_SUPPORTED | old S2-T proposal state had severe in-sample optimism; chronological cross-fitting fixed most inflation; expanding/ROLL28 online updates did not recover universal out-of-sample correctionability |
| V3-PLA | PRIOR_LOCAL_ARBITRATION_NOT_PREDICTABLE | Shared/Local complementarity is real and large-margin; soft router cannot capture enough oracle headroom |
| V3-HPA | HPA_NOT_SUPPORTED | A0 oracle margins real; L2-SP target adaptation failed; all 18 A1 runs selected lambda=infinity; old parameter-space partial pooling rejected |
| V4-DRQ | CONTINUOUS_CENTER_NOT_SUPPORTED | direct center residual MLP failed: Center-Z 1/6, median -7.17%; Center-R 1/6, median -4.15% |
| V5-IAMR | IAMR_TARGET_LOCAL_ONLY | monotone target-local calibration succeeds: IAMR2 M1 passed; IAMR3 hour extension passed 4/6 vs IAMR2; shared-prior function-space pooling failed 3/6 |
| V5.1-TLR | TLR_TAIL_SAFE_FREEZE_NOT_SUPPORTED | tail-aware Fourier-hour + alpha freeze attempt failed; all 6 cells selected lambda_h=infinity, coverage gates failed, and PJM PatchTST UpperTail safety failed |
| V5.2-APTC | APTC_DEVELOPMENT_NOT_SUPPORTED | upper slope-one continuation reduced but did not remove PJM tail harm; 2026-08-31 post-hoc source audit corrected a provenance error: actual junction was S1 **truth** q95, not reported S1 host q95; static raw junction was ~99.4%/~99.23% percentile in PJM S2-V and inactive in DE/NORD |
| V5.2 post-hoc junction audit | PROVENANCE_CORRECTED / MOVING_RAW_Q95_NOT_SUFFICIENT | V5.2 final numbers/verdict remain reproducible, but q95-source semantics were wrong; fixed S2-T and expanding host-q95 are too inertial, trailing-28 is responsive but unstable/market-dependent, and pure within-day q95 is legal but statistically misaligned |
| V5.3-RATJ | V5_3_RATJ_HOST_RANK_NOT_SUPPORTED | protocol-compliant R0 only: G0/G2/G3 passed but G1 failed; all cells selected 7d EW half-life; DE/PJM recovered ~5% occupancy, NORD remained ~12.1–12.3% with severe day/block clustering; R1 correctly not executed |
| V5.4 Conditional EVT Tail | V5_4_CONDITIONAL_EVT_TAIL_FEASIBLE | E0 existence/modelability result: honest S2-T cross-fitted IAMR3 residual tails persist in all six cells and all six show converged hourly/daily GP fits; PJM hourly xi reaches ~1.03–1.18 while daily-declustered xi is ~0.53–0.77, so mean-excess correction is forbidden; no S2-V correction prediction, S3 or S4 |
| V5.4-E1 Conditional Tail Distribution | V5_4_E1_TAIL_DISTRIBUTION_NOT_IDENTIFIED | G0/G4 passed; G1 failed on PJM/PatchTST Fold-A count (19<20), G2 failed on NORD Fold-A occurrence gaps, G3 failed on NORD severity coverage and PJM q90 overcoverage; M0 selected for DE/NORD and M1 for PJM; source re-audit found M1 conditional-coverage scoring used median sigma, but corrected recomputation still fails G3 and does not change verdict; no S2-V correction, S3/S4, mean-excess action or rescue |
| V6 ICDE Architecture Race | V6_R0_ARCHITECTURE_RACE_COMPLETE_NO_TOP2 | R0 completed on E0 honest S2-T only. A_CT_EQ and B_REMR were SHIFT_FAILURE (Fold-A positive/near-zero S but Fold-B negative); C_BEQA retained positive median UpperTail gain but large normal/MAE harm. In both NORD hosts every F3 7-day block was harmed by all three dense-action arms. No arm passed promotion, so no S2-V/R1, S3 or S4 was run. |
| V6.1 Certifiable Extreme Repair | V6_1_CER_INTERNAL_ONLY_NOT_SUPPORTED | Internal E0 screen completed: all three fixed variants have median release <1% and median MAE/UpperTail gain <=0; NORD cell day-miscoverage also exceeds the .25 certificate screen. No S2-V one-shot, S3 or S4 was authorized. |
| V6.2 HERA | V6_2_HERA_INTERNAL_NOT_SUPPORTED / REAUDIT_NARROWED | Strong F3 UpperTail gains but dense action causes MAE/Normal harm. Re-audit found F3 reused F1-only reliability state despite prefix-specific design; weighted-median non-event pool also leaks into ordinary residual-center correction. NORD sparse selected-slot sign precision remains poor. No S2-V/S3/S4. |
| V6.3 SDAER | SDAER_STATIC_NOT_SUPPORTED / P2_DIAGNOSTIC_ONLY | Prefix-correct sparse utility execution passed chronology but both static variants failed H0-B/NORD stop. S2-V remained unread. Direct utility and online rescue are closed as headline routes. |
| CMER | CMER_S2V_PILOT_NOT_SUPPORTED | Exactly one frozen S2-V one-shot executed; safety/action-density checks passed but median MAE gain -0.2527%, median UpperTail gain +0.9553%, <5/6 positive UpperTail cells, median selected residual-positive precision 48.04%, and NORD precision failures. No rescue/S3/S4. |
| HCH V3 authorized demo | HCH_V3_DEMO_NOT_SUPPORTED_AS_EXECUTED / GATE_ACTION_RISK_MISMATCH_SUPPORTED_ON_D2 | Authorized 3-market × 2-Host S2V demo executed once with frozen states. HCH full/global-only were pointwise exactly Identity across all 394 S2V days because the Gate never acted. Independent metric replay is exact; metric-specific external-peer comparison is MAE near-best 6/6 and Tail-MAE near-best 4/6. A D2-only no-retrain counterfactual supports one narrow V3.1 action-risk consistency correction; consumed S2V cannot be reused for V3.1. |
| CN-ERB | PROVISIONAL_PLAN / NOT_ACTIVE | Drafted China Provincial Extreme-Repair benchmark option after CMER; later strategic review clarified that the paper remains cross-market rather than China-only. SHANDONG is private/supplementary-only; public provincial markets remain candidate related-domain extensions. No CN-ERB run is authorized yet. |

## V5-IAMR corrected interpretation

- IAMR2 vs legal V2.5 endpoint: 5/6 positive, no >2% MAE harm, median gain ~1.34%.
- IAMR3 vs IAMR2: positive 4/6.
- Directly combining recorded M1/M2 machine gains implies IAMR3 vs legal V2.5 endpoint is positive in all six cells; approximate gains:
  - DE_EPEX/PatchTST +0.34%
  - DE_EPEX/TimeMixer +0.63%
  - NORD_FI/PatchTST +18.06%
  - NORD_FI/TimeMixer +10.35%
  - PJM_2020/PatchTST +1.56%
  - PJM_2020/TimeMixer +5.84%
  - median ~+3.70%.
- Tail caveat: PJM IAMR2/IAMR3 materially worsens UpperTail/Tail-MAE vs Identity; overall improvement is not yet a tail-safe headline result.
- IAMR is deterministic given host cache/S2; repeated seed rows in V5 do not represent independent calibrator training seeds.

## V5.1-TLR mechanistic readout

- Stage A replay: `V5_IAMR_REPLAY_AND_STATISTICAL_READJUDICATION_OK`.
- TLR3 selected `lambda_h=infinity` in all six original cells; the Fourier hour component was never deployed.
- Selected alpha values were heterogeneous and included exact Identity fallbacks, but PJM_2020 × PatchTST still selected full correction and failed UpperTail safety on S2-V.
- PJM S2-T upper events are temporally clustered: total counts can look adequate while individual chronological validation folds are too sparse for a reliable rare-tail eligibility rule.
- IAMR gain-by-host-rank diagnosis shows NORD_FI’s dominant gain occurs in the lowest host decile; symmetric tail fallback/continuation is therefore rejected.
- Upper-only raw slope-one continuation is the current mechanistic hypothesis because it can preserve high-host anomaly magnitude without deleting lower-tail calibration.

## V5.2-APTC mechanistic readout

- Historical D0: `V5_2_APTC_D0_AUDIT_OK`; D1 algebraic status: `D1_MATHEMATICAL_CONTRACT_OK`; D2 active-cell overall MAE remained attractive: DE PatchTST +2.18%, NORD PatchTST +18.87%, NORD TimeMixer +9.78%, PJM PatchTST +2.75%, PJM TimeMixer +3.78% vs Identity; DE TimeMixer fell back to Identity.
- PJM Tail/UpperTail remained unsafe: Upper relative harm about +9.72% / +9.95% for PatchTST / TimeMixer.
- **Post-hoc provenance correction (2026-08-31):** `tlr_runner.load_context()` computes `s1_q95` from S1 `y_true`; APTC consumes this value directly. Therefore the actual deployed junction was S1 truth q95. Historical D1/D0/report strings claiming `S1 host forecasts only` are semantically incorrect, and the final audit did not inspect upstream q95 provenance.
- Corrected actual-junction drift: DE/NORD APTC trigger rate is 0% on S2-V host predictions; PJM PatchTST threshold 103.33 is ~99.40 percentile with ~0.60% trigger, TimeMixer 103.33 is ~99.23 percentile with ~0.77% trigger. The earlier S1-host-q95 drift numbers remain a valid post-hoc host-only diagnostic, not the actual V5.2 junction.
- Continuation remains useful: relative to Identity, PJM IAMR3 UpperTail harm was ~+26.42%/+23.83%; APTC reduced this to ~+9.72%/+9.95%, removing about 63%/58% of that harm.
- Read-only host-rank diagnosis: fixed S2-T q95 triggers 0% in DE, ~17–24% in NORD and ~1.5% in PJM; expanding history is similarly inertial; trailing-28 improves DE/PJM to ~5.8–6.7% but still triggers ~15.6–17.4% in NORD and has large block-level swings. Thus a moving raw q95 is not equivalent to a stable rank coordinate.
- Formal-cache causality audit confirms each daily episode uses the prior 168h context to jointly produce the next 24 host forecasts and host `predict()` reads context only. The current-day 24h vector is therefore legally available for post-processing, but pure within-day q95 mechanically triggers 2/24 hours and poorly covers historical truth-upper events, so it is rejected as the sole coordinate.
- Hence static absolute-price tail definition remains rejected; the surviving object is a causal adaptive current-host CDF/PIT/rank coordinate coupled to the useful slope-one continuation.

## V5.3-RATJ mechanistic readout

- Independent contract replay: `7 passed`; final audit and protected-source checks are consistent with the user report.
- R0 gates: G0=true, G1=false, G2=true, G3=true. R1 was correctly blocked and no RATJ correction prediction was generated.
- All six cells selected 7-day EW half-life. This establishes strong evidence that stale-history forgetting matters, but does not justify an unregistered shorter-half-life search.
- EW-CDF pooled S2-V exceedance is near nominal in DE (~5.12/5.16%) and PJM (~5.28/5.63%), but remains ~12.07/12.33% in NORD.
- NORD excess occupancy is regime clustered: one 7-day block reaches ~28.57/30.36%; single days reach 18/24 and 17/24 flagged hours, with strong daytime/evening concentration. Hence pooled marginal host CDF conflates day-level shift and hour structure.
- Retrospective truth-only diagnosis after the frozen R0 verdict: selected EW flags cover 86.4% of historical upper events in both PJM hosts, versus 22.2%/100% in NORD PatchTST/TimeMixer. Host-tail alignment is not universal even when global host/truth rank is strong.
- Scientific verdict: adaptive forgetting is useful, but a **single pooled marginal host-percentile junction is rejected as a universal tail coordinate**. Do not rescue V5.3 with new q levels, shorter half-lives, host-only DSPOT junctions, or post-hoc PJM-only R1 on this panel.

## Current next research question

CMER completed exactly one frozen S2-V evaluation and received `CMER_S2V_PILOT_NOT_SUPPORTED`. The result is not a safety collapse: no cell exceeded 3% MAE harm, median Normal harm was only 0.69%, and median action rate was 6.37%. The failure is effectiveness/ordering: median MAE gain -0.25%, median UpperTail gain +0.96%, fewer than 5/6 positive UpperTail cells, median selected residual-positive precision 48.0%, with severe NORD precision failure. CMER is stopped; no rescue on its consumed S2-V panel is allowed.

New repository evidence broadens, but does not replace, the research setting: the provincial datasets contain a coherent set of forecast-time market fundamentals. SHANDONG is private/non-public and can only be supplementary; GANSU/SHAANXI/NINGXIA/QINGHAI are candidate public related-domain markets subject to provenance/admission review. Their existence motivates a possible cross-province/fundamentals extension, not an automatic China-only reset.

Current status is **STRATEGIC_CONSOLIDATION / NO NEW EXPERIMENT AUTHORIZED**. The next discussion must summarize the full V2.5→IAMR→V5/V6→CMER arc, clarify final ICDE framing, and decide how international heterogeneous markets and public Chinese provincial markets should be combined in the final cross-market benchmark.

The user added a hard paper-success requirement: on the ultimately selected datasets and headline metrics, the final method must beat the preregistered selected strong baselines/peers; beating Identity or an internal historical endpoint alone is not sufficient. No S3/S4 consumption or new architecture is authorized until that benchmark/method direction is explicitly frozen.

Stage synthesis:
`docs/history/v5_iamr/HCH_V2_6_TO_V5_2_STAGE_SYNTHESIS_AND_HANDOFF_20260831.md`

V5.2 post-hoc audit/review:
`docs/history/v5_tail/HCH_V5_2_APTC_POSTHOC_REAUDIT_AND_RANK_JUNCTION_REVIEW_20260831.md`

V5.3 R0 audit / next-direction adjudication:
`docs/history/v5_tail/HCH_V5_3_RATJ_R0_AUDIT_AND_CONDITIONAL_TAIL_ADJUDICATION_20260831.md`

V5.4 feasibility protocol:
`docs/archive/v5_tail_details/HCH_V5.4_EVT_TAIL_FEASIBILITY_AUDIT_PROTOCOL_v0.1_20260831.md`

V5.4 E0 re-audit / E1 registration:

- `docs/history/v5_tail/HCH_V5_4_E0_REAUDIT_AND_CONDITIONAL_GPD_ADJUDICATION_20260831.md`
- `docs/archive/v5_tail_details/HCH_V5.4_E1_CONDITIONAL_GPD_TAIL_MODEL_MATHEMATICAL_CONTRACT_v0.1_20260831.md`
- `docs/archive/v5_tail_details/HCH_V5.4_E1_CONDITIONAL_GPD_TAIL_MODEL_IMPLEMENTATION_DESIGN_v0.1_20260831.md`
- `docs/archive/v5_tail_details/HCH_V5.4_E1_CONDITIONAL_GPD_TAIL_MODEL_PROTOCOL_v0.1_20260831.md`
- `docs/archive/v5_tail_details/HCH_V5.4_E1_CONDITIONAL_GPD_TAIL_MODEL_PROMPT_20260831.md`

## V6.3 SDAER result (2026-09-01)

- Isolated root: `experiments/evidence/v6_3_sparse_decision_aware_extreme_repair/`.
- Static core variants were exactly `SDAER_S1(lambda=1)` and `SDAER_S2(lambda=2)`; `SDAER_P2` was kept as a separate online information setting.
- Contract tests: `10 passed`. F2 used F1-only OOF utility/day-score reference; F3 rebuilt its reference from F1+F2, with 588 explicit F2-evidence rows entering the F3 reference after chronological availability.
- H0-A independent recompute passed. Static H0-B failed for both variants. F3 diagnostic rank: S2 first (`median S=0.00534119`), S1 second (`0.00143156`); this is not a promotion because both failed H0-B and the NORD stop gate.
- F3 median UpperTail gain: S1 `1.4009%`, S2 `2.8676%`; median selected-slot residual-positive precision: S1 `54.9253%`, S2 `51.1696%`. NORD harm/precision failures, overall point-harm, 7-day block, and captured-benefit gates prevent static promotion.
- No S2-V one-shot was authorized; no S2-V/S3/S4 artifacts were generated. No historical artifacts or `src/hch_v2/**` were modified.
- P2 strict state audit passed on all 366 target-day rows. P2 is diagnostic-only, excluded from static gate/ranking, and must not be used to rescue the static result.
- Full machine result: `experiments/evidence/v6_3_sparse_decision_aware_extreme_repair/reports/SDAER_V6_3_FINAL_RESULT.md`; final audit: `provenance/final_audit.json`.

## CMER one-shot S2-V result (2026-09-01)

- Isolated root: `experiments/evidence/cmer_cross_market_extreme_repair/`.
- Exactly one CMER configuration was run on DE_EPEX/NORD_FI/PJM_2020 × PatchTST/TimeMixer. Shared rankers used same-host target-excluded source markets with equal-market weighting; local rankers used target S2-T; all six cells met the 5-event-day/20-event-hour local-support requirement.
- Contract tests passed (`15 passed`). The freeze barrier, input/code hashes, model states, rank fusion, support state, prevalence, K, active threshold, target q10 magnitude and hourly scales were sealed before the S2-V truth load. No S2-V search/retry, S3, or S4 occurred.
- Median S2-V MAE gain was `-0.2527%`; median UpperTail gain `0.9553%`; median Normal harm `0.6888%`; median action rate `6.3738%`; median selected residual-positive precision `48.0385%`.
- The strict pilot gate failed on point-gain, UpperTail gain/coverage, overall residual-positive precision, and NORD residual-positive precision. Safety checks for 3% cell MAE harm, Normal harm, action density, NORD MAE harm, freeze and protected history passed.
- Verdict: `CMER_S2V_PILOT_NOT_SUPPORTED`. No CMER rescue or paper-scale expansion is authorized from this pilot.
- Full report: `experiments/evidence/cmer_cross_market_extreme_repair/reports/CMER_FINAL_RESULT.md`; independent gate: `audits/independent_gate_recompute.json`; final audit: `provenance/final_audit.json`.


## HCH V3 authorized S2V demo (2026-09-04)

- Authorization: explicit `DEMO_EXECUTION_AUTHORIZED`.
- Scope: frozen DE_EPEX/NORD_FI/PJM_2020 × PatchTST/TimeMixer; P1 `DE_EPEX::PatchTST` operational canary followed by P2 all six cells.
- Chronology: 394 S2V days × 7 typed methods; predictions persisted and hashed before each day-scoped truth reveal, followed only by legal next-day HCH history update.
- Frozen artifacts: 3 LOMO Global checkpoints, 12 HCH states, 24 fitted peer states, 6 S2V-start histories, six D2 tables. All D2 tables remained `{0}` with zero Down/Up actions and zero coverage.
- Result: HCH full was no worse than Identity in all six cells and best/near-best versus headline external BestPeer (`PIR`/`delta-Adapter Ada-Y`/`UEC-STD`) in all six cells, but the all-Keep mechanism is degenerate.
- Verdict: `DEMO_MECHANISM_ONLY` (architecture-positive gate withheld because of degenerate mechanism; CRC remained `LIMITED_VARIANT_APPENDIX_ONLY`).
- No HCH/peer tuning, rescue, Host retraining, architecture/split/support change, or post-hoc S2V rerun was performed. S3/S4 truth were not read. This development demo does not establish HCH V3 validity.
- Artifacts: `experiments/evidence/hch_v3_demo_validation/authorized_real_execution/P2_all_six/`.
- Provenance: bundle `f551148a67cdba03fa219e16e0170e5873a5110c`, admitted source tree `802192fcdc2835a4daec651f0d09f135d8a553a34ed6f69aded008b840caa812`, protocol manifest `97717110ce5f319703d6f1f1c00b73909a64303e882bb42408b088c93ce56942`, metrics `31e22a01278de56282e196806bed1ab3f728b01e45c7632c7f8a9514b4485540`.

### Independent HCH V3 demo correction (2026-09-04)

- Historical machine token `DEMO_MECHANISM_ONLY` is preserved, but its fallback logic is not protocol-faithful: the implementation assigns mechanism-only whenever diagnostics were merely computed rather than requiring explicit positive mechanism evidence.
- Independent metric replay from raw truth + persisted predictions is exact (`max abs diff = 0`). `HCH-V3-full` and `HCH-V3-global-only` are pointwise exactly Identity on every one of the 394 S2V day artifacts, so the executed method has no realized repair effect.
- The verdict evaluator also has a BestPeer bug: it selects the peer by MAE once and reuses the same peer for Tail-MAE. Correct metric-specific external comparison gives MAE best/near-best `6/6` but Tail-MAE best/near-best only `4/6`. NORD_FI/PatchTST is ~14.43% worse than PIR on Tail-MAE; PJM_2020/TimeMixer is ~79.54% worse than PIR.
- Independent scientific adjudication: `HCH_V3_DEMO_NOT_SUPPORTED_AS_EXECUTED / GATE_ACTION_RISK_MISMATCH_SUPPORTED_ON_D2`.
- First-principles diagnosis: fixed directional `Q(0.75)` point actions are optimized for a different asymmetric loss but are judged by symmetric expected-MAE Gate risk. A legal D2-only, no-retraining counterfactual selecting the existing quantile-grid candidate minimizing the same MAE risk restores safe nonzero actions in 5/6 cells and positive D2 Overall-MAE gain in those five cells (largest ~+2.61%/+1.91% on PJM PatchTST/TimeMixer) with no Normal-MAE harm under the existing selector.
- This does **not** authorize rescue on consumed S2V. The next object is a minimal V3.1 parameter-free Gate action readout correction, validated first on source/D1/D2 only. Detailed audit: `experiments/evidence/hch_v3_demo_validation/INDEPENDENT_AUTHORIZED_S2V_RESULT_AUDIT_20260904.md`.

## HCH V3.1 tail-first safe D2 selector development validation (2026-09-04)

- Registered exactly one new selector: `D2_TAIL_FIRST_SAFE_LEXICOGRAPHIC_V1`; action policy remains `SIGNED_MAE_BAYES_GRID_V1` and learned states are unchanged.
- The selector uses the fixed four chronological 7-day cross-fit on each existing 28-day D2 cell. CandidateSet, Overall/Tail/Normal support scores and kappa are fit on other 21 days; held-out 7-day truth is evaluation-only.
- Gate: nonzero availability `5/6`; strictly positive cross-fitted Tail-MAE gain `1/6`; Overall-MAE harm violations `2`; Normal-MAE harm violations `2`; chronology/provenance/numerical failures `0`; historical V3 fixed replay `true`; historical V3.1 Overall-first replay `true`; consumed S2V/S3/S4 access `0`.
- Verdict: `HCH_V3_1_TAIL_FIRST_D2_NOT_SUPPORTED`. Full-D2 deployment kappa was not computed because the cross-fitted promotion gate failed. No fresh panel was created/run and no consumed S2V/S3/S4 truth was accessed.
- Independent audit accepts the failure as scientific, not implementation-related: 41/41 HCH tests and 27/27 demo tests pass; source tree and clean-bundle revision match the manifest. DE actions are entirely Normal; PJM retains ~10-12% action density but zero Tail action under cross-fit. Therefore D2 selector/margin rescue is frozen and the bottleneck moves upstream to learned repair ranking / Shape-state semantics / target locality / causal context. `SIGNED_MAE_BAYES_GRID_V1` remains accepted; no more selector/tau/threshold rescue is authorized. Independent audit: `experiments/evidence/hch_v3_demo_validation/INDEPENDENT_V3_1_TAIL_FIRST_CROSSFIT_AUDIT_20260904.md`.
- Artifacts: `experiments/evidence/hch_v3_demo_validation/v3_1_tail_first_d2_validation/`; cross-fit result SHA256 `c7159fa3971f958eb9c5eb64897412bd4729c6eecdbe8c5a37580e836132506b`; bootstrap SHA256 `2dc028ba897b29d1aadbbb6b246e033e94b38cf317fc47f496fda6492ed58e1b`.
- Controlling hashes: MAE-action design `6f4503059b63389c0806a58d2e64f5a4267a5c253a3d504a8032d429300ec174`; tail design `3e8ec4178317e16da55e5903adc82df76b5a3014ea6de4e5290e8b09e3b8ddbc`; tail prompt `730987f6be12bda95a91a7c808a7ec2d678b1b7804d09b926ef0d20b25ea6df4`.


