# Common-Benchmark Paper Tables

Generated from `../registry/` only. Offline and online/prequential settings are never merged into one ranking; every harm column declares its unit. Empty subsets carry a coverage status, never a numeric zero.
