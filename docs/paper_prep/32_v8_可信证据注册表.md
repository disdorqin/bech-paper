# v8 可信证据注册表

> Date: 2026-08-08
> Rule: a source is trusted only after bibliographic identity, normalized quote occurrence, and SHA256 all pass.

## 1. Current research boundary

- Verified problem evidence: negative prices form multi-hour episodes and frozen backbones often miss complete episodes.
- Supported literature signal: the checked correction/risk papers do not model negative-price episodes.
- Supported search statement: among the obtained full texts, no work reports an out-of-sample operational forecast of general `price < 0` spell endpoints or an episode edit relative to a frozen numerical forecast.
- Forbidden statement: no prior work exists globally.
- Current novelty status: evidence insufficient. A new problem setting is not yet a new algorithm.

## 2. Trusted identity, mapping requires repair

The following PDFs have correct identities and matching SHA256, but the existing `31a` row-level mechanism mappings are not trusted without revision:

| Source | Identity | Allowed use now |
|---|---|---|
| CRC `2512.22428` | Verified | Additive post-hoc correction nearest neighbor; do not claim unconditional non-degradation. |
| delta-Adapter `2601.20280` | Verified | Frozen-backbone adapter nearest neighbor; do not call delta an abstention gate. |
| UEC `2605.21088` | Verified | Fixed-backbone error-correction neighbor. |
| PnP-Corrector `2605.08935` | Verified | Frozen-engine state-correction neighbor; quoted page in `31a` is wrong. |
| Post-Training Corrections `2505.15354` | Verified | Affine/post-training correction neighbor with theorem conditions. |
| PIR `2505.23583` | Verified | Instance-aware soft revision neighbor; no deterministic base fallback. |
| RCPS `2101.02703` | Verified | Risk-control framework under its stated assumptions. |
| LTT `2110.01052` | Verified | Risk-calibration and parameter-selection framework, including selective examples. |

The common high-level observation `negative-price episode structure absent` survives for these eight papers. Their current quotes and six-field mappings do not.

## 3. Structured-output source status

Phase A direct verification is complete. The PDF first pages were rendered and
visually inspected; the validator independently checks first-page title and every
listed author, normalized quote occurrence and page, and exact full-file SHA256.
All eight corrected sources pass. See
`34_PhaseA_结构化输出原文核验与公式占位表_2026-08-08.md` and
`34a_phaseA_validation.json`.

| Source | Status | Formula-level use |
|---|---|---|
| DETR `2005.12872` | Verified | Eq. (1), printed p.5: Hungarian set matching; Eq. (2): matched classification/box loss. |
| ActionFormer `2202.07925` | Verified | Authors are Chen-Lin Zhang, Jianxin Wu, Yin Li. It predicts `C` per-class sigmoid scores with implicit background plus onset/offset distances; it is not a single binary event detector. |
| Levenshtein Transformer `1905.11006` | Verified | Eq. (8): target-relative expert edit; Algorithm 1 is printed p.12 and jointly learns deletion/insertion. |
| Ciliberto et al. `1605.07588` | Verified replacement | General structured loss, least-squares surrogate, decoding, and consistency/comparison results. |
| Ciliberto et al. `2002.05424` | Verified replacement | Implicit-loss embedding and loss-aware decoding for general structured outputs. |
| Yuan et al. `1704.04671` | Verified replacement | Absolute temporal-window prediction with start/middle/end scoring and structured maximal-sum decoding. |
| Rabiner IEEE `18626` | Verified replacement | Section IV.D and Eqs. (64)-(70): explicit state-duration HMM. |
| Original CRF, ICML 2001 | Verified replacement | Conditional label-sequence model; first-page title/authors and full-text quote verified. |

Forbidden records remain forbidden:

- Ciliberto `2302.09095`: astronomy paper, not structured prediction.
- SET `1705.08256`: InGaN solar-cell paper.
- Rabiner IEEE `217112`: wrong document; the verified tutorial is `18626`.
- The old CRF dead URL/quote row: superseded, not countable.

## 4. Negative-price duration source status

- Zamudio Lopez and Zareipour (2025) model duration with a Kaplan-Meier survival distribution for extreme negative spikes at `-60 EUR/MWh`, not `-10 EUR/MWh`.
- This is a neighboring duration-method prior, not a same-target prior for all `price < 0` spells.
- It does not report an out-of-sample operational experiment that forecasts future spell endpoints.
- Current database coverage remains incomplete; verdict stays `EVIDENCE INSUFFICIENT`.

## 5. Next gate

1. Phase A source verification and formula placeholder table are complete.
2. Define at most one candidate: base-relative episode correction under a non-additive event loss.
3. First attack the candidate by reducing it to absolute interval/set prediction plus residual/value regression plus standard selective risk control.
4. If the reduction preserves the candidate's object and guarantee, issue `NO-GO` and return to Route E.
5. No experiment is authorized until the candidate separately passes formula collision, formal-property honesty, and minimal-pilot gates.
