# v8 Commander Gate Status

> Date: 2026-08-08
> Scope: innovation discovery only. No paper-scale experiment is authorized by this gate.

## 1. Event-edit direction

| Gate | Status | Usable conclusion |
|---|---|---|
| Problem evidence | PASS | Negative prices are episode-structured and frozen backbones often miss whole episodes. |
| Algorithm novelty | NO-GO (evidence insufficient) | The problem is real, but no algorithm novelty may be claimed before a full-text collision audit. |
| Mathematical object | SUPPORTING PROPERTY ONLY | The corrected matching/editing definitions are usable as engineering definitions; no novel theorem exists. |
| Pilot experiment | IMPLEMENTATION BLOCKED | The current E0 has empty INSERT/SHIFT operations and invalid dataset mappings. Its STOP verdict is void. |

The event-edit direction is not an A-level main contribution at this stage. Preserve the P0 problem evidence, but do not present the editor as a validated method.

## 2. v8 fleet results

### `tac079092` (algorithm discovery): REJECTED

The report's `NOT READY` verdict may be directionally honest, but the search was too shallow to establish it. The candidates were generic labels rather than complete operators, C1 incorrectly used S4 in a training objective, and the required equations, pseudocode, pilots, and kill tests were missing.

### `t66bf1e66` (theorem-gap discovery): REJECTED

The report correctly suspected `THEORY NOT READY`, but its C6 metric inequalities were false because event recall is event-weighted while point recall is duration-weighted. C4 also used inconsistent correction notation (`delta=m` versus the correct `delta=m-b`). These claims are not usable until repaired.

### `t24aff6bd` (full-text frontier map): REJECTED

The CSV contains 20 rows but only 10 are marked `counted=yes`; the other 10 are merely locally available. Several supposed formula coordinates are prose or section labels, and ActionFormer is still cited from its abstract. Negative-price spell prediction, risk control, and selective abstention are not covered deeply enough. The valid verdict is `EVIDENCE INSUFFICIENT`, not `LANDSCAPE COMPLETE`.

### v8-D1/D2/D3 focused source packets: REJECTED

- D1 (`t90fafae3`): malformed CSV, only 5 parsed counted rows, truncated SHA256 prefixes, and missing source paths for downloaded PDFs.
- D2 (`t56d5f1c5`): required CSV missing; HSMM is abstract-only, CRF is standard-knowledge-only, and the ActionFormer bibliography is wrong.
- D3 (`t354a219e`): malformed CSV, only 6 counted full texts, several inaccessible candidates, and failed Semantic Scholar queries. Its valid status is evidence insufficient, not a direct-prior exclusion.

### v8-E repair attempt: REJECTED / IN REVIEW

- E1 (`t30bfbe97`): hashes and CSV round-trip were repaired, but required bibliography and six-field mechanism mappings were dropped. Its validator checks file identity only, not source content.
- E2 (`t5d234eb5`): rejected for wrong-paper evidence. `1705.08256` is an InGaN solar-cell paper, not SET; `2302.09095` is an astronomy paper, not structured prediction; the ActionFormer authors were also wrong. Hash matching therefore did not establish paper identity.
- E3 (`tf3ace032`): awaiting a narrowed evidence-insufficient repair; no gap claim is authorized.

All future source manifests must pass three independent checks: bibliographic identity from the PDF first page, normalized quote occurrence in extracted full text, and full-file SHA256 equality.

Independent content audits then established:

- D1/E1 has 8 identity-correct PDFs and matching hashes, but only 5 claimed quotes survive normalized text checks; all eight mechanism mappings contain at least one inaccurate field. The high-level observation `episode structure absent` survives, while the row-level map does not.
- D2/E2 is void because two arXiv IDs resolve to unrelated solar-cell and astronomy papers, and ActionFormer authors were wrong.
- D3/E3 remains `EVIDENCE INSUFFICIENT`. Zamudio Lopez and Zareipour (2025) use `-60 EUR/MWh`, not `-10`, and provide an extreme-negative-spike survival distribution rather than an out-of-sample operational spell-endpoint forecast.

## 3. Phase A authoritative-source repair

| Gate | Status | Usable conclusion |
|---|---|---|
| Correct structured-output identities | PASS (8/8) | DETR, ActionFormer, Levenshtein Transformer, two correct Ciliberto papers, Yuan et al., Rabiner `18626`, and the original CRF paper were verified from rendered first pages. |
| Normalized full-text evidence | PASS (8/8) | Every registered quotation occurs on the registered PDF page. |
| Full-file SHA256 | PASS (8/8) | Every actual 64-hex digest equals its registered digest. |
| Formula placeholder map | PASS as source repair only | Absolute set/interval prediction, edit-based refinement, general non-additive structured loss, sequence labeling, and explicit duration are established prior objects. |
| Algorithm novelty | EVIDENCE INSUFFICIENT | No verified source in this small set contains the whole proposed conjunction, but that does not establish a global gap or a new algorithm. |
| Experiment authorization | NO | Phase A does not authorize a pilot or a main matrix. |

Authoritative artifacts: `34_PhaseA_结构化输出原文核验与公式占位表_2026-08-08.md`,
`34a_phaseA_structured_sources.json`, and `34a_phaseA_validation.json`.

## 4. Current research decision

1. Do not start the main experiment matrix for a new innovation.
2. Do not revive graph/TAP, e-process, admin clamp, A4 fixed point, dual-market joint prediction, or the empty event editor.
3. Phase A source repair is complete; do not reinterpret that pass as an algorithm-novelty pass.
4. Treat the current signal as a hypothesis only: the relative-to-frozen-forecast episode-edit setting may be underexplored, but it is not yet a novelty claim.
5. Define at most one candidate and first try to reduce it to absolute interval/set prediction plus residual/value regression plus standard selective risk control.
6. If the reduction succeeds, issue `NO-GO` and retain BECH as an applied Route-E paper.
7. Only a candidate that survives reduction may proceed to fresh formula-collision, formal-property, and minimal-pilot gates.

## 5. Information needed from the user

None at this gate. The next uncertainty is source verification and method design, not data access or compute.
